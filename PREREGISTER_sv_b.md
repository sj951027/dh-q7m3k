# PREREGISTER — sv_b (공매도비중 + 신용잔고비율) **[초안 · 미등록 · 적용은 sv_a §11 판정 후]**

> 상태: **초안**. 스펙·골대는 아래로 동결하되 **배선 미적용·미등록**(wu_score.py 무변경).
> **적용 시점 규칙**: wu 계열 판정(sv_a ~9월 중 등)이 임박해 있어, 지금 등록하면 동시검정
> 분모가 6→7로 올라 기존 모델 판정 문턱이 상승한다(판정 직전 등록 회피 원칙 — lv_e 전례).
> → **sv_a §11 판정 완료 후** ① 패치 적용 → ② 다음 실행일 = REG_DATE(신규 모델 소급 적재
> 없음 — wu_score 관례) → ③ 본 문서 §2·MODELS_LEDGER·checkup.REG_DATE 갱신.
> 근거 연구: `research/RESEARCH_candidates_scan_20260829.md` §3·§4 · 배선: `research/sv_b_wiring_20260829.patch`

## 1. 스펙 (동결 대상)

| 항목 | 값 |
|---|---|
| model_id | `sv_b` |
| 트랙 | wu (`wu_scores`, 가중치 0 관측, 전체 유니버스 단일 순위) |
| 핵심(실측필수) | `svr5` = mean(short_vol_ratio,5,min3) ↑ — **sv_a 와 동일**(공매도비중 상위 선호) |
| 보조(NaN=0.5) | **`crb5`** = mean(short_flows.credit_bal_rate,**5,min_periods=1**) ↑ (신용잔고비율 상위) |
| crb5 지연 처리(스펙) | 신용잔고는 원천상 **~3거래일 지연 적재** — min_periods=1 로 최근 실측값 사용. 실측: 3일 지연 강제 시에도 IC 동일(h10 +0.10/h20 +0.15) → 룩어헤드 없음 |
| spec_hash | `95cb5aee2e83` (sv_a `e38efdfc260d`) |

**변수 1개 원칙**: sv_a(svr5 단독)에 crb5 **하나만** 추가 → sv_a 짝비교가 곧 crb5 의 순수 기여.

## 2. 등록·판정 골대

| 항목 | 값 |
|---|---|
| REG_DATE | **(미정 — sv_a 판정 후 첫 적재일)** |
| 판정 | §11 — OOS 40거래일, h20 주지표, Bonferroni(적용 시점 분모 실측=7) |
| post-hoc 가중 | 발견이 사후 스캔(2026-08-29, 반등 국면 12앵커) → **h10 별도 재현 요구**, h20 만 통과 시 '기움'까지 |
| 짝비교 | **sv_a 대비 같은 앵커 diff(h20·h10) 반드시 병기** — sv_a 우위 없으면 승격 불가 |
| 보류 조건 | crb5 커버리지 < 유니버스 90% 또는 신용 적재 지연 > 5거래일 지속 시 판정 보류·보고 |

## 3. 근거 (전부 in-sample — 가설)

- 단독: sh_credit_rate h5/h10/h20 모두 양(+), h10 +0.071·h20 +0.151 CI>0, 변동성·유동성
  통제 후 생존, 기존 축과 직교(연구 §3). **단 반등 단일 국면 · h20 앵커 12개(겹침)**.
- 결합: sv_a(이 창에서도 양+) 짝비교 diff h10 +0.045 [+0.010,+0.082] · h20 +0.098 [+0.070,+0.130]
  — 후보 결합 중 유일하게 "양+양→추가 개선"(연구 §4). sv_a점수↔신용 순위상관 −0.06(직교).
- 해석(가설): 공매도 상위 + 개인 신용매수 상위 = **포지셔닝 밀집의 두 독립 단면** — 숏커버·수급
  모멘텀 계열. 하락 국면에서 부호 반전 위험 명시(hv_a 전례).

## 4. 오프라인 검증 (2026-08-29, 사본 DB — 실 DB 무접촉)

- 동일 데이터에서 원본 vs 패치판 재적재: **기존 6모델 6,584행 완전 0-diff**, sv_b 1,102행 신규.
- sv_a↔sv_b 순위상관 +0.659 — 같은 계열이되 crb5 가 실제 서열 기여.
- 발견·교정 2건(패치에 반영): ① crb5 min_periods=3이면 적재 지연 탓 전부 중립화 → min1 로 스펙 확정
  ② 실 DB 의 8/28 sv_a 동결분은 공매도 지연 적재 전 계산이라 재계산과 상이 — 스펙의 '지연 허용'
  동작의 자연 결과(동결 원칙: 동결분이 정본, 재계산으로 덮지 않음).
