# PREREGISTER — 운용 채택 기준 (돈 기준) **[초안 — 2026-09-12 · 미등록]**

> 상태: **초안**. 사용자 승인 전에는 아무 판정에도 쓰지 않는다.
> 성격: §11(팩터 유효성)을 대체하지 않는다. **다른 질문에 답하는 두 번째 잣대**를 따로 못 박는 것.
> 근거 도구: `research/shadow_ops_portfolio.py`(기계적 권고 이행) · `research/ptw_live_review.py`(실거래 원장)

## 0. 왜 따로 만드나

지금 판정은 **전 종목을 점수 순으로 줄 세웠을 때 20거래일 뒤 수익 순서와 얼마나 맞나**(IC)를 본다.
실제 운용은 **시장별 상위 10을 같은 금액으로 사서 40거래일 들고 간다**.
둘은 다른 질문이다. lv_b 가 §11 에서 '기움'(채택 아님)인데 실거래·그림자 포트에서는 뚜렷한 플러스인 것은
모순이 아니라 **서로 다른 것을 재고 있어서**다.

- IC: 팩터가 살아 있나 → **개발·승격 판단**에 쓴다.
- 초과수익: 이 바구니로 돈이 되나 → **운용 판단**에 쓴다.

지금까지는 두 번째 잣대가 문서로 고정돼 있지 않아, "채택 안 된 모델을 왜 쓰나"라는 긴장이 남아 있었다.

## 1. 사전 고정 (숫자를 보고 바꾸지 않는다)

| 항목 | 값 |
|---|---|
| 대상 | 트랙별 표시 기준 모델(현재 `lv_b`·`v30`). 관측 전용 모델은 §11 판정 전까지 대상 아님 |
| 바구니 | 시장별 **상위 10**, 동일가중 |
| 진입 | 추천 다음 거래일 종가(ENTRY_LAG=1) |
| 보유 | **40거래일** 고정. 중간 신호로 팔지 않음 |
| 잣대 | 같은 시장 **전 종목 수익률 중앙값** (지수는 보조 표기) |
| 주지표 | 앵커 단위 초과수익 평균(%p) |
| 오차 | iid 부트스트랩 4,000회 95% CI + **주블록** 감도 병기 |
| 일관성 | 앵커 양(+) 비율 |
| 최소 표본 | 완결 앵커 **20개 이상** |
| **국면 조건** | 서로 다른 국면 **2개 이상** 포함(상승·하락·횡보 중). 한 국면만이면 라벨은 최대 '기움' |
| 비용 | 기본은 비용 0. **왕복 비용 0.35% 차감 버전을 반드시 병기**(매도세 0.18% + 수수료·슬리피지 가정) |
| 게이트 | §11 과 동일(부분실행·이중실행 run 제외) |

## 2. 라벨 (미리 적어 둔다)

| 조건 | 라벨 | 조치 |
|---|---|---|
| CI 하단 > 0 **그리고** 주블록 CI 하단 > 0 **그리고** 양(+) ≥ 60% **그리고** 국면 2개 이상 | **운용 채택** | 표시·권고 기준으로 삼는다 |
| CI 하단 > 0 이나 국면 1개 또는 주블록 걸침 | **운용 기움** | 써도 되나 "한 국면 결과"임을 화면에 병기 |
| CI 가 0 을 걸침 | **운용 노이즈** | 권고 근거로 쓰지 않는다 |
| CI 상단 < 0 | **운용 부적합** | 권고에서 내린다 |

- 어떤 라벨이든 §11 판정문(VERDICT)을 덮어쓰지 않는다. **두 라벨을 나란히 표기한다.**
- 비용 차감 후 라벨이 달라지면 **낮은 쪽**을 정본으로 한다.

## 3. 정직성 조항 (이 문서의 약점)

이 기준을 쓰기 전에 **이미 본 숫자가 있다**. 숨기지 않고 적는다(2026-09-12 실측, 공통 앵커 20개, h40, 중앙값 잣대, 비용 0):

| 모델 | 평균 | 표준편차 | 95% CI | 양(+) |
|---|---|---|---|---|
| lv_b | +9.40%p | 3.81 | [+7.72, +10.99] | 100% |
| v30 | +7.51%p | 4.26 | [+5.78, +9.37] | 100% |
| 반반 | +8.46%p | 2.70 | [+7.29, +9.57] | 100% |

- 전부 **2026-06~08 진입 앵커 한 국면**이다. 그래서 §1 의 국면 조건상 지금 값으로는 최대 '운용 기움'이다.
- 따라서 이 기준의 **첫 정식 적용은 새 창부터** 한다: 진입 앵커 **20260915 이후** 40거래일 완결분.
  기존 창은 '참고(사전관찰)'로 병기만 한다. 기준을 성적에 맞춰 깎지 않기 위한 장치다.

## 4. 실거래 원장과의 관계

그림자 포트(기계적 이행)와 실거래(사람이 매매)는 **따로 기록**한다. 실거래는 `research/ptw_live_review.py` 로 월 1회.
실거래 성적은 이 기준의 판정 입력이 **아니다**(매도 시점이 사람 판단이라 모델 성적과 섞인다). 관측·점검용이다.

2026-09-05 스냅샷 실측(참고): 저변동 청산 20건 지수대비 **+8.66%p** [+3.32, +13.80] · 스윙(재량) 36건 **−3.91%p** [−8.87, +1.10].
같은 스냅샷에서 40거래일 창이 닫힌 저변동 8건은 실제 평균 +3.98% 대 40일 보유 +10.41%(**+6.43%p**, 8건 중 5건에서 보유가 나음).

## 5. 바꾸면 안 되는 것

- 바구니 크기·보유기간·잣대·최소 표본·국면 조건. 중간 성적을 보고 조정하지 않는다.
- 라벨 문턱. 특히 "한 국면만이면 최대 기움" 조항.
- 이 문서가 §11 을 대체한다고 쓰지 않는다. 두 잣대는 **병기**한다.

## 6. 등록 절차

사용자 승인 시 상태를 **[등록]** 으로 바꾸고 `MODELS_LEDGER.md` 에 한 줄 추가한다.
첫 판정 예정일: 진입 앵커 20260915 + 40거래일 ≈ **2026-11-10**(공휴일로 밀릴 수 있음).
