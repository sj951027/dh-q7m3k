# PREREGISTER — 운용 채택 기준 (돈 기준) **[초안 v4 — 2026-09-13 · 미등록]**

> 상태: **초안 v4**. v3 에 외부 검토(`research/GPT_REVIEW_ops_adoption_v3_20260913.md`, 10건) 반영. 사용자 승인 전에는 아무 판정에도 쓰지 않는다.
> 성격: §11(팩터 유효성)을 대체하지 않는다. **다른 질문에 답하는 두 번째 잣대**를 따로 못 박는 것.
> 근거 도구: `research/shadow_ops_portfolio.py`(기계적 권고 이행 — 등록 전에 §1 규칙으로 배선·검증 완료해야 함)
> · `research/ptw_live_review.py`(실거래 원장, 판정 입력 아님) · `research/hold_horizon_lvb.py` · `research/regime_persistence.py`

## 0. 왜 따로 만드나

지금 판정은 **전 종목을 점수 순으로 줄 세웠을 때 20거래일 뒤 수익 순서와 얼마나 맞나**(IC)를 본다.
실제 운용은 **시장별 상위 10을 같은 금액으로 사서 40거래일 들고 간다**. 둘은 다른 질문이다.
lv_b 가 §11 에서 '기움'(채택 아님)인데 실거래·그림자 포트에서는 뚜렷한 플러스인 것은 모순이 아니라 **서로 다른 것을 재고 있어서**다.

- IC: 팩터가 살아 있나 → **개발·승격 판단**에 쓴다.
- 초과수익: 이 바구니로 돈이 되나 → **운용 판단**에 쓴다.

## 1. 사전 고정 (숫자를 보고 바꾸지 않는다)

| 항목 | 값 |
|---|---|
| 대상 | **`lv_b`(운용) · `v30`(챔피언) 두 개 고정 = 검정 family.** 다중검정: **Bonferroni α = 0.05/2** 를 처음부터 적용. 모델 추가·재판정은 v5 재등록 |
| 바구니 | 시장별 **상위 10**, 동일가중, 희석 배지(dilution_60d, 진입일 기준 PIT) 제외 |
| 진입 | 추천 다음 거래일 종가(ENTRY_LAG=1). **진입일에 가격 없음·거래정지·상한가 체결 불가 = 그 종목 현금(대체 없음)**, 바구니 평균은 체결분만 |
| 보유 | **40거래일** 고정. 중간 신호로 팔지 않음. 보유 중 정지·상폐 = **재개 첫 종가 또는 마지막 가격**으로 청산 |
| 잣대(주) | 종목 수익 − **같은 시장 전 종목 동일가중 평균 수익(PIT: 진입일 시세 있는 종목 전체)**. 앵커 = 그날 코스피10+코스닥10 바구니 평균 1개 |
| 잣대(보조) | 전종목 중앙값 대비 · 지수 대비 · 모델 자체 유니버스 동일가중 대비 — 표기만, 판정엔 안 씀 |
| 수익 정의 | 수정주가 종가 기준 가격수익(**배당 제외**). 분할·병합은 수정주가로 처리. 데이터 정정(재적재)은 판정 시점 스냅샷을 동결해 재현 |
| 주지표 | 앵커 단위 40일 초과수익 평균 E(%p) |
| **판정용 CI** | **40거래일 블록 부트스트랩** 95% (블록 길이 = 보유기간, 4,000회) 단 하나. iid·주블록 CI 는 표기만 |
| 일관성 | 앵커 양(+) 비율 |
| **최소 표본** | **비중첩 40일 코호트 4개 이상**(첫 진입일부터 40거래일 단위로 자른 구간마다 앵커 ≥1). 앵커 수(20)는 부수 조건 |
| **국면 정의** | 앵커 1개당 라벨 1개. **코스피·코스닥 지수의 보유 40일 수익 평균**이 +3% 이상=상승 · −3% 이하=하락 · 사이=횡보 (3년 실측 27/30/43%) |
| **국면 조건** | 서로 다른 국면 **2개 이상**, 국면당 **비중첩 코호트 ≥ 2** 그리고 **국면별 평균 초과 > 0**(국면별 블록 CI 하단 > −1.0%p) |
| **크기 조건** | 비용 c 차감 후 20거래일 환산 ≥ +0.5%p ⇔ **(E − c) × 20/40 ≥ 0.5 ⇔ E ≥ c + 1.0**. c=0.35 → **E ≥ +1.35%p**, c=0.5 → E ≥ +1.5%p |
| 비용 | 기본 왕복 **0.35%**, **0.5% 차감본 병기**. 낮은 쪽이 정본 |
| 게이트 | §11 과 동일(부분실행·이중실행 run 제외). 취지: **데이터 오염일** 제외이지 '거래 곤란일' 제외가 아님 — 거래 곤란은 위 체결 규칙으로 결과에 반영 |
| **관찰기간 고정** | 진입 앵커 **20260915 ~ 20270331**(그 뒤 40거래일 창 마감 = 2027-05 말 데이터 동결). **판정은 1회.** 조건 미충족이면 '**판정불가**'로 종료(라벨 없음) |
| **구현 동결** | 등록 시 `shadow_ops_portfolio.py`·`dilution_flag.py`·`leaderboard.py` 의 **commit hash** 를 §8에 기록. 계산 코드 변경 = 이 창 폐기·재등록 |

**국면을 지수로, 잣대를 동일가중 평균으로 정한 이유(실측)**: 중앙값 종목 40일 수익은 평소에도 음수(코스피 −1.3%·코스닥 −3.1%)라
'바구니 평균 − 중앙값'은 분포 비대칭만큼 부풀려진다(외부 검토 #1). 동일가중 평균은 실제로 살 수 있는 포트의 수익이다.
**진입일 라벨을 안 쓰는 이유**: 조정·반등 라벨은 며칠 안 간다(`regime_persistence.py`). 국면은 **분류용**이지 예측용이 아니다.

## 2. 라벨 — 우선순위 논리식 (위에서부터 첫 번째로 맞는 것)

판정 CI = §1 의 40일 블록 부트스트랩 CI(Bonferroni 보정, 비용 0.5% 차감본). E = 비용 차감 후 평균.

1. CI 상단 < 0 → **운용 부적합** (권고에서 내린다)
2. CI 하단 ≤ 0 → **운용 노이즈** (권고 근거로 쓰지 않는다)
3. CI 하단 > 0 이고 [코호트 <4 또는 국면 조건 미달 또는 E < c+1.0 또는 양(+) < 60%] → **운용 기움** (써도 되나 미달 항목을 화면에 병기)
4. 그 외 → **운용 채택** (표시·권고 기준)

- 어떤 라벨이든 §11 판정문(VERDICT)을 덮어쓰지 않는다. **두 라벨을 나란히 표기한다.**
- 관찰기간 종료 시 조건(코호트·국면)을 못 채웠으면 라벨 대신 **판정불가**.

## 3. 용량 조항 (자기 점검 — 게이트 아님)

비용 가정은 **종목당 투입 ≤ 진입일 직전 20거래일 평균 거래대금(ADV)의 1%** 일 때만 성립한다. 실측(2026-09-11 lv_b 상위 10): ADV 중앙 코스피 13억·코스닥 14억, 최소 5억.
이를 넘겨 운용한 기간의 실거래 성적은 이 문서의 근거로 쓰지 않는다. 판정 계산에는 들어가지 않는다(게이트 아님). 규모 확대는 유동성 하한 바구니로 별건.

## 4. 정직성 조항 (이 문서의 약점)

이미 본 숫자(2026-09-12, 중앙값 잣대·iid CI·앵커 20·주블록 6, 비용 0): lv_b +9.40%p · v30 +7.51%p · 반반 +8.46%p, 전부 '하락' 국면(88%).
**v4 잣대(동일가중 평균)·v4 CI(40일 블록)로는 아직 계산하지 않았다** — 등록 전 배선 검증에서 처음 계산되며, 그 값을 보고 문턱을 바꾸지 않는다(문턱 근거는 아래).
- ±3% = 3년 지수 분포 3등분 · 코호트 4 = 독립 창 4개(≈8개월) · +0.5%p/20일 = 3년 격자 K40 비용후 값 · 1% = 통상 참여율 상한 · α/2 = family 크기.
- 첫 정식 적용은 **진입 20260915 이후**부터. 기존 창은 '참고(사전관찰)' 병기만.

## 5. 실거래 원장과의 관계

그림자 포트(기계적 이행)와 실거래(사람이 매매)는 **따로 기록**한다. 실거래는 `research/ptw_live_review.py` 로 월 1회, 판정 입력이 아니다.

## 6. 시간표

- 관찰기간 진입 2026-09-15 ~ 2027-03-31, 마지막 창 마감 2027-05 말 → **판정 1회: 2027-06 초**.
- 코호트 4개는 2027-01 중순에 채워지나, 국면 조건은 시장이 준다. 종료일까지 못 채우면 판정불가.
- 중간 관측은 리더보드 접힘 패널로만. 이 문서의 판정이 아니다.

## 7. 바꾸면 안 되는 것

§1 전부(대상·바구니·체결 규칙·잣대·수익 정의·CI·표본·국면·크기·비용·관찰기간·구현 동결)와 §2 논리식. 중간 성적을 보고 조정하지 않는다. 바꾸려면 v5 재등록 + 새 관찰기간.

## 8. 등록 절차

1. `shadow_ops_portfolio.py` 에 §1 규칙(동일가중 잣대·40일 블록 CI·코호트·국면 라벨·체결 규칙) 배선 → 2026-06~09 참고창으로 계산 확인(값은 §4 에 '사전관찰'로 기록).
2. 사용자 승인 → 상태 **[등록]**, `MODELS_LEDGER.md` 한 줄, 여기 commit hash 기록.
3. 판정: 2027-06 초 1회. 결과는 `VERDICT_2027xxxx_ops.md`.
