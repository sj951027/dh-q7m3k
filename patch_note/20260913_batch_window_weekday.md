# 2026-09-13 — 배치 금지 시간대를 '평일'로 한정 (2026.09.7 · 판정·점수 0-diff)

## 그래서 뭐가 바뀌나
- `CLAUDE.md`·`AGENTS.md`의 "20:10~22:30 파일·DB 금지" 규칙이 **평일(월~금)** 로 한정된다. 토·일은 배치가 없으므로 제한 없음.
- 애매할 때의 판단 기준을 적었다: 그날 `logs/auto_run_YYYYMMDD_2010.log`가 생겼는지.

## 왜
일요일 20:11에 Codex가 REQUEST_001(리더보드 IC 독립 재현)을 처리하려다 이 규칙만 보고 작업을 중단했다. 규칙이 요일을 안 적어서 생긴 오독.

실측(2026-09-13 20:1x):
- 작업 스케줄러 `dh-q7m3k-daily` 트리거 DaysOfWeek=62(월~금), 다음 실행 09/14 20:10.
- `logs/`에 주말 날짜 auto_run 로그 없음(8/24~9/11 전부 평일).
- 일요일 20:00 `dh-q7m3k-weekly`(notify_weekly.py)는 1~2분 안에 끝남(20:00 실행, 결과 0).

## 어떻게
- 문서 두 줄 문구 수정만. 코드·배치·스케줄러 변경 없음. 판정·점수 0-diff(코드 미접촉).
- 평일 공휴일에도 스케줄러는 도니 그날은 그대로 금지.

## 영향 범위
- Codex·Claude Code가 주말에 handoff 작업을 할 수 있게 된다. 배치 동작 변화 없음.
