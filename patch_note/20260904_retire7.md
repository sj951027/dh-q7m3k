# 2026-09-04 — 판정 완료 모델 7개 은퇴(적재 중지·행 보존) + 리더보드 은퇴 플래그

## 뭐가
1. `lowvol_score.py` — `RETIRED = {lv_c, lv_d, lv_a3, lv_short, hv_a}` 추가. 적재 루프에서 건너뜀.
   `--run`/`--full` 재적재의 DELETE 도 `model_id NOT IN RETIRED` 로 제한(은퇴 행은 어떤 경로로도 안 지움).
2. `wu_score.py` — `RETIRED = {wu_a, wu_b}` 추가. 적재 루프·요약 출력에서 건너뜀.
3. `run_and_diversify.py` — 2.89단계(`build_wu_filter.py` wu_a CSV) 중단. `docs/latest_wu.csv` 는 마지막 적재일
   스냅샷으로 남김. 2.88(wu_score) 자체는 유지(sv_a·le_a·qs_a·px_a 적재).
4. `build_cross_sim.py` — 모델 목록·패널 3개에서 wu_a 제거. `build_alpha_beta.py` — wu 트랙 대표 wu_a → sv_a(등록 20260715).
5. `leaderboard.py` — 각 스크립트 RETIRED 를 읽어 `leaderboard.json` 모델 행에 `retired: true/false` 플래그 추가(표시 전용).
6. `docs/leaderboard.html` — 은퇴 판별을 json `retired` 플래그 + 폴백 목록(13개)으로. 배지 툴팁을 3개 정본 안내로 일반화.
   `docs/wu.html` — 배너에 "2026-09-04 은퇴·마지막 적재일 스냅샷" 문구와 sv/qs/px/리더보드 링크. 제목 변경.
7. `MODELS_LEDGER.md` — 은퇴 표를 트랙별로 통합(v3 6 + lowvol 5 + wu 2), 현역 표에서 7개 제거, 2·3차 판정 처분에 9/4 갱신 주석.

## 왜
- 7개 전부 §11 판정 완료(8/29·9/1)이고 현역 대조 역할·표시 사용이 없다(사용자 결정 2026-09-04).
  기대 이득: 리더보드·알림·공통잣대에서 노이즈 행 감소, lowvol/wu 적재 행 일 ~3.7k 감소, 판정 표에 '현역'만 남아
  가독성 향상. 8/29·9/1 '적재 유지' 결정의 근거였던 "분모 축소 부작용"은 행 보존으로 해소된다 — Bonferroni 분모는
  `COUNT(DISTINCT model_id)` 실측이라 은퇴 후에도 lowvol 11·wu 6 그대로(실측 확인).
- wu_a 를 cross_sim/alpha_beta 에 남기면 적재가 멈춘 날부터 공백일이 0% 로 채워져 창을 왜곡(8/29 px_a 사례와 같은 문제).
- 2.89 를 두면 wu.html 이 매일 같은 옛 데이터를 '최신'처럼 내보임(과거 lv_b 잔존표시 버그와 같은 부류) → 중단.

## 어떻게 + 검증 (전부 history.db 사본 · ohlcv.db 읽기전용, 실 DB 무접촉)
- lowvol `--run 20260904` 재적재: 현역 7모델 3,295행 중 index·spec_hash·n_universe·lowvol_score **0-diff**(frozen_at 만 갱신),
  은퇴 5모델 1,474행 그대로(삭제 0), 테이블 총행 204,964 불변.
- wu: 9/4 행 삭제 후 패치본 실행 → 현역 4모델 4,375행 적재, 은퇴 2모델 미적재. 같은 데이터에 원본 스크립트 실행과
  비교 → 현역 4모델 spec_hash·wu_score·wu_rank·n_universe **0-diff**.
  (관측: ohlcv 9/4 수정주가 재조정 이후라 재계산 유니버스 1,099 — 실 DB 에 적재된 9/4 행(1,093)과는 데이터 차이. 적재된 행은
  append-only 로 그대로 둠 — PIT 원칙.)
- leaderboard.py 원본 vs 패치본 같은 데이터로 실행 → `retired` 키 제외 json **완전 동일**(IC·CI·판정·분모·게이트 0-diff).
  retired=true 13개(v31×6·lowvol 5·wu 2).
- build_cross_sim / build_alpha_beta 사본 실행 정상(패널 3개, α/β v30·lv_b·sv_a).
- `python tests/run_tests.py` 전체 통과(lowvol/wu spec_hash 골든 — MODELS 사전은 그대로라 해시 불변).

## 영향범위
- 판정·점수: **0-diff**(현역 모델 점수식·테이블·분모 불변). 은퇴 모델은 9/4 행이 마지막(다음 배치부터 신규 행 없음).
- 표시: 리더보드 '현역' 표에서 7개가 은퇴(접힘)로 이동. wu.html 은 스냅샷 안내. 공통잣대 주력창 4모델(wu_a 빠짐),
  α/β 3번째 줄 sv_a — 텔레그램 α/β 한 줄도 sv_a 로 바뀜(자동).
- 부활: 새 model_id + PREREGISTER 만(§11 관례). 연구용 재계산은 RETIRED 임시 제외 후 사본 DB에서.
- 다음 배치(9/5)에서 확인할 것: `[적재] … | le_a … | sv_a … | qs_a … | px_a …`(wu_a·wu_b 없음), lowvol 요약에 은퇴 5모델
  runs 수가 73(=9/4까지)에서 멈춤, leaderboard.json 에 retired 키.
