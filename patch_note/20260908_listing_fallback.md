# 2026-09-08 — FDR 상장목록 404 사건 · 예비 캐시 폴백 (2026.09.2 · 판정·점수 0-diff)

## 뭐가 (사건)
- 20:10 배치 첫 단계 `FinanceDataReader.StockListing('KRX')` **HTTP 404**(FDR이 쓰는 KRX 시총 목록 서버 응답 없음, 우리 코드 아님).
  → universe_ohlcv 실패(9/08 시세 미수집) → 스크리너 유니버스 내장 리스트(수십 종목) → stage1 75/20행 → 완전성 게이트 **배포 보류**(설계대로)
  → lowvol/wu 적재 0, 대형 유니버스 실패. **하루 측정이 전 트랙에서 유실**(20260908 부분실행일로 IC 제외). 판정 오염은 없음.
- 유니버스 실패에 대한 자동 재시도·예비 소스가 없던 것이 단일 실패점.

## 어떻게 (수정)
| 파일 | 변경 |
|---|---|
| `listing_cache.py` (신규) | `save/load/seed_from_db` — 성공한 날 목록을 `../dh-q7m3k-data/listing_cache.json`에 저장, 실패한 날 그걸 사용. 캐시 없을 땐 ohlcv.db 최신일+history.db 이름+sector_cache 로 시드(2,635종목·이름 2,407) |
| `universe_ohlcv.py` | StockListing 실패 → 캐시(KRX) 로 진행(신규 상장은 다음 성공 시), 성공 시 캐시 저장 |
| `screener_fdr_v2_6.py` | 실패 → 캐시(시장별) + 같은 필터(코드 끝 '0'·금융/리츠/스팩) → 내장 리스트는 캐시도 없을 때만 |
| `large_universe.py` | 실패 → 캐시(시총·주식수는 캐시값, source 표시) |
| `tests/test_leaderboard_frozen.py` | 게이트 골든을 **동결창(≤20260729) 안**에서만 비교 — 동결 이후 새 부분실행일(20260908)은 정당한 게이트 대상 |
| `research/clean_partial_run.py` (신규) | 부분실행 run 행 삭제(백업 후, --yes 필요, 정상 run 보호) — 같은 날 깨끗한 재실행용 |

검증(오프라인, FDR을 404로 흉내): universe_ohlcv 예비 2,635종목 / 스크리너 KOSPI 789·KOSDAQ 1,715(금융 제외 41, 업종 2,257) / 대형 2,635.
성공 경로는 반환값 계산 뒤 save() 한 줄만 추가 → 정상일 0-diff(모의 성공 응답으로 확인). `tests/run_tests.py` 통과.

## 영향범위
- 점수·판정·게이트 로직 무변경. 정상일엔 캐시 파일 1개가 매일 갱신될 뿐. 실패일엔 전날 목록으로 정상 규모 실행(신규 상장 종목만 하루 늦게).
- 시드 캐시는 9/07 기준 — 이름 없는 228종목(최근 과매도·대형 유니버스에 안 들어온 종목)은 금융/리츠 이름 필터가 안 걸릴 수 있음(첫 FDR 성공 시 자동 교체).
