# [2026.08.4] 2026-08-16 — 모의계좌 직관화(누적 %p) · 표시 기준 명문화 · 버전 체계

## 그래서 뭐가 바뀌나
- 모의계좌의 "시장평균 대비/일(bp)" 컬럼 → **"시장 대비(누적%p)"**: "이 기간 시장평균보다
  총 몇 %p 더 벌었나" = 누적수익% − 시장평균 수익%. 표의 두 숫자 차이라 눈으로 검산 가능.
  (예: lv_a +28.9% − 시장 +12.3% = +16.6%p)
- 모의계좌 **표시 기준 명문화**: 각 트랙 대표 + 직접 비교쌍만(섀도우 변형 제외), 공통창
  유효 10거래일 미만 신모델은 자동 대기 — px_a가 현재 대기 중(유효 5일), 창이 차면 저절로 등장.
  기준 문구를 섹션 하단에 표시.
- **patch_note/ 체계 + CalVer 버전 도입**(연도.월.차수, v숫자 금지 — v2.6/v3/v30/PTW v5.x
  이름공간 충돌 방지). 도입 주간 3건 백필(2026.08.1~3). 리더보드 푸터에 버전 병기.

## 왜 바꿨나
- bp/일(92.8 같은 숫자)은 설명을 읽어도 감이 안 온다는 사용자 피드백 — 기간 누적 %p가
  직관적이고 검산 가능.
- 모의계좌에 "없는 모델"이 많은 이유(대표 선발)가 코드에만 숨어 있었음 — 기준을 화면과
  코드 양쪽에 명시하고, 신모델 편입을 수동 관리에서 자동 규칙으로 전환.
- 변경 이력이 커밋 메시지·주석에 흩어져 "언제 뭐가 왜"를 재구성하기 어려웠음(사용자 제안).

## 어떻게
- build_cross_sim.py: exc_cum 필드 추가, MODELS에 px_a 편입 + MIN_DAYS=10 대기 가드
  (가독성 목적의 표시 기준 — 판정과 무관, 근거는 "공통창이 짧으면 수치 자체가 무의미").
- docs/leaderboard.html: 컬럼·툴팁·설명 단락 교체(bp 용어 제거), 표시 기준 문구, 푸터 버전.
- patch_note/: README(규칙+현재 버전) + 백필 3건 + 본 노트.
- 검증: py_compile, cross_sim 실생성(px_a 대기 로그 확인), exc_cum 검산(cum−bench 일치),
  JS node --check, 렌더 시뮬.

## 영향 범위
- 판정·점수 0-diff — 전부 표시·문서. exc_bp 필드는 json에 유지(하위 호환).
