# lowvol §11 판정 + lv_e 사전등록 초안 (2026.08.5)

## 1. 그래서 뭐가 바뀌나

- lowvol 트랙 8개 모델이 OOS 40거래일에 도달해 **§11 판정 결과를 문서로 남겼다**
  (`VERDICT_20260829_lowvol.md` — **초안**, 확정은 사용자 확인 후).
  선두 lv_b는 **'기움'(채택 아님)**. 표시 기준은 lv_b 유지, 실거래 가중은 여전히 0.
- 예약돼 있던 **lv_e(저변동+ROE+저회전) 사전등록 초안**을 작성했다
  (`PREREGISTER_lv_e.md` — **미등록**, 코드 미반영).
- **사용자 화면·점수·추천은 아무것도 바뀌지 않았다.** 이번 세션은 문서 + 재현 스크립트만.

## 2. 왜 했나

- 예약 리마인더(lv_b 40거래일 도달 후속 A·B). 2026-08-28 리더보드 기준 lv_b OOS 41/40 도달 확인.
- lv_e는 `research/RESEARCH_lv_ablation_20260812.md`가 "lv_b 판정 이후 등록" 권고한 건.
  판정 직전에 등록하면 lowvol 동시검정 분모가 늘어 lv_b 판정이 불리해지기 때문 — 순서를 지켰다.

## 3. 어떻게 (+ 검증)

### 신규 파일

| 파일 | 내용 |
|---|---|
| `VERDICT_20260829_lowvol.md` | lowvol §11 판정문 **초안** — h20 주지표·95%/Bonferroni CI·주별일관·h10 재현·lv_b 짝비교 + 각주 2건 |
| `PREREGISTER_lv_e.md` | lv_e 스펙·골대·근거·기각조건·배선안 **초안(미등록)** |
| `research/verdict_lowvol_20260829.py` | 판정 재현 스크립트(읽기 전용). leaderboard.py 정본 함수 재사용 |
| `research/lv_e_wiring_20260829.patch` | `lowvol_score.py` 배선 제안 diff 63줄 — **미적용** |

### 검증

- **판정 수치 재현**: `research/verdict_lowvol_20260829.py`가 `docs/leaderboard.json`(08-28 생성)의
  lv 계열 수치와 일치. 예: lv_b h20 IC **+0.0687**, n=22, CI[−0.0162, +0.1542] — 리더보드와 동일.
  게이트도 동일(partial `20260608`, double `20260703`), 분모 10 실측 일치.
- **각주 근거 실측**(추정 아님):
  - lv_b h20 앵커 = **22개, 20260625~20260729** (8월 앵커 0건)
  - lv_b 유니버스가 시장별 MIN_GROUP(8) 미만인 run: 20260814(7/4)·20260819(11/7) 실측 2건
  - lv_b 종목의 v30 유니버스 포함비율 = **55 run 전부 1.0000** (완전 부분집합)
  - v30↔lv_b 앵커 IC 상관 **pearson +0.828 / rank +0.780**, 주간 평균 **+0.910**
- **lv_e 배선 0-diff**: 사본 DB(`/tmp` 복사본)에 원본판·패치판을 각각 `--full` 재적재 →
  기존 10모델 공통 173,656행 **점수 불일치 0 · spec_hash 불일치 0**. lv_e는 16,238행/55 run,
  lv_b와 행 단위 1:1 일치(유니버스 동일). **실 DB 무접촉.**

## 4. 영향 범위

- **판정·점수 0-diff.** 코드·DB·표시물 변경 없음(문서와 research/ 스크립트만 추가).
- lv_e 배선은 **미적용**. 적용 시 lowvol Bonferroni 분모 **10 → 11**로 올라
  기존 lowvol 모델 전부의 판정 문턱이 함께 상승 → **사용자 승인 필요**(PREREGISTER_lv_e.md §2).
- 역작동 판정 3종(lv_c·lv_d·hv_a)의 적재 중지 여부도 **미실행 — 사용자 결정 사항**
  (중지하면 분모가 줄어 남은 모델 판정이 유리해지는 부작용 있음).

## 5. 별건 발견 (운영 주의, 판정 무영향)

실 DB의 동결 `lowvol_scores` vs 현재 기준 `--full` 재적재 비교 시 173,656행 중 **14,501행 불일치**:
- `lv_short` 13,895행 — short_flows의 ohlcv.db 이전·백필 영향(§21-8 Phase1의 예상 부작용)
- `20260824` run 606행 — 그날 stage3_final이 lowvol 동결 이후 덮어써진 것으로 추정
- 나머지는 완전 일치 → 재현성 자체는 정상. 두 건 다 판정 앵커 구간(~20260729) 밖.
- 시사점: **`lowvol_score.py --full`은 과거 동결값을 바꿀 수 있다** — 판정 시즌에 사용 금지 권장.
