# 2026-09-04 — 텔레그램 완료 알림 v2 (리더보드 2안과 같은 순서)

## 뭐가
`notify_telegram.py` — 본문을 `_model_status_lines_v2()` 로 교체(종전 `_model_status_lines` 는 보존, 미호출).
① 🏆 지금 기준: v30 유의(8/09 정본)·lv_b 기움(8/29 정본) + 각 유니버스 크기
② 💰 최근 1개월 시장대비: v30·lv_b + 판정 캘린더 선두 1개(cross_sim trailing r20 − 시장평균)
③ 📅 판정: 현역 모델 D-day 순(은퇴 제외, large 는 h60), 40일 도달했는데 정본 없는 모델은 '도달(판정 대기)'
④ 🔔 달라진 것: leaderboard_history 마지막 2건 비교 — 판정 표본 도달 / 자동 라벨 변경(정본 없는 모델만, '참고' 표기) /
   v30·lv_b 유니버스 직전 run 대비 50% 이상 급감 ⚠️. 없으면 "없음".
α/β 한 줄은 본문에서 제거(코드·alpha_beta.json 생성은 유지). 리더보드 링크 문구 갱신.

## 왜
- 9/4 실제 메시지에 "과매도 v3 — 선두 v31a"(8/09 은퇴 모델)·"전체종목 — 선두 px_a IC +0.13(h5·n12)"(h20 없음)가 나감 —
  종전 로직이 은퇴를 안 거르고 IC 최대값으로 선두를 뽑았기 때문(버그). v2 는 json `retired` 제외 + 정본 우선.
- 사용자 요청: 리더보드 2안처럼 "지금 기준 → 돈 → 캘린더" 순서, 매일 같은 문장 대신 달라진 것만 강조, α/β 제거.

## 어떻게 + 검증 (오프라인, 전송 없음)
- `build_message()` 실측 출력(9/4 데이터): 지금 기준 v30 uni 823·lv_b uni 322 / 돈 v30 +14.3%p·lv_b +14.6%p·sv_a +5.5%p(시장 +2.7%) /
  판정 sv_a D-4 · le_a D-4 · mom_b D-8 · qs_a D-9 · px_a D-21 · lv_e D-36 · ls_t1 D-42(h60) / 달라진 것 없음. 790자.
- 이벤트 경로: history 사본에서 sv_a 라벨·le_a OOS 39→40 조작 → "sv_a 자동 라벨 기움→노이즈(참고) · le_a 판정 표본 40일 도달" 출력 확인.
- 점수·판정·파이프라인: 0-diff(알림 본문만). 실패 시 종전과 같이 비치명 문구로 대체.

## 영향범위
- 다음 배치(9/5 20:10)부터 새 형식으로 발송. 판정/은퇴가 생기면 SEALED_V2·RETIRED_FALLBACK_V2 를 leaderboard.html SEALED 맵과 함께 갱신.
- notify_weekly.py 는 미변경.

## 추가 (전체 점검, 같은 세션)
- `notify_weekly.py` — '트랙 선두'가 은퇴 모델(v31a 등)을 h5 IC 최대로 뽑던 같은 버그 → json retired + 폴백으로 제외(최소 수정).
  주간 리캡 오프라인 빌드 확인(과매도 v30·저변동 sm_a(h5 보조)·모멘텀 mom_a·전체종목 px_a).
- 세션 전체 점검: 변경 py 12종 py_compile·import OK, docs/leaderboard.html·wu.html 태그 정합 OK, run_and_diversify 호출 스크립트 전부 존재,
  tests/run_tests.py 통과, .git/index.lock 없음.

## 추가 2 — 주간 리캡(notify_weekly.py) 개편 (사용자 지적: 선두는 '이번 주 성적'이어야)
- '트랙 선두(등록 후 누적 h5 IC 최대)' 줄 삭제 → 📈 이번 주 성적: cross_sim trailing r5(최근 5거래일 상위20 따라사기) − 시장평균,
  현역 모델만 시장 대비 순 + "1주는 운 비중이 큼" 문구. 🔔 이번 주 달라진 것: leaderboard_history 7일 범위(판정 표본 도달·자동 라벨 변경).
- 판정 카운트다운: 판정 대기(40일 미만) 모델 중 최임박 모델명·D-day 표시, 대기 없으면 '전부 판정 완료'(종전엔 이미 끝난 모델의 누적 OOS로
  늘 '판정 가능'이 떠 정보가 없었음).
- 검증: 오프라인 빌드(9/4 데이터) — 과매도 전부 판정 완료 / 저변동 lv_e 4/40 / 모멘텀 mom_b 32/40 / 전체종목 sv_a 36/40 외 3;
  이번 주 성적 lv_a +2.3 · qs_a +2.0 · sv_a +0.7 · mom_a +0.4 · v30 +0.2 · px_a −0.2 · lv_b −0.7%p; 달라진 것 2건. 전송 없음, 점수·판정 0-diff.
