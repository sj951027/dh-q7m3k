# 2026-09-21 — 전수 점검: 표시 전용 단계의 실패 신호 + PTW 연동 표기 (2026.09.18 · 판정·점수 0-diff)

## 그래서 뭐가 바뀌나
- `build_daily_lists.py`(날짜 탭 JSON)·`build_scoreboard.py`(성적표 JSON)가 실패하면 **종료코드 1** 을 낸다 → `.bat` 의 FAILED 에 잡혀 텔레그램에 🔔 한 줄.
  종전엔 경고만 찍고 0 으로 끝나서, 실패해도 아무도 모른 채 페이지가 **조용히 낡을** 수 있었다.
- 두 단계는 점수·추천·판정과 무관한 표시 전용이라 `notify_telegram.py` 의 `OBS_ONLY` 에 추가 — 실패해도 첫 줄은 '확인 필요'로 안 바뀐다.
- `build_daily_lists.py` 머리말에 소비자 명시: **Position-Tracker-Web v5.24** 가 `hist/v30.json`·`hist/ls_t1.json` 의
  `days[0].date`·`rows[].rank/ticker/market` 을 읽어 매수 폼에 순위를 보여 준다. 키 이름·구조를 바꾸면 PTW 도 같이 고칠 것.

## 전수 점검 결과(실측)
| 항목 | 결과 |
|---|---|
| 9/18 배치 | 20:10~21:04 정상, 날짜 탭 6개·성적표 9/18 기준 갱신, 푸시·텔레그램 정상 |
| 지수 | FDR KS11/KQ11 정지 → 9/20 수정(.env 로드·KRX 예비)·9/17~18 정정 완료. 9/21 로그에서 `[예비 pykrx] 신규 1행` 확인 예정 |
| 종가 오염(애프터마켓) | 9/18 20:10 수집분 표본 147종목 공식 종가와 100% 일치. 9/14·9/16 은 1.5~2% 불일치(기록: research/RESEARCH_krx_after_market_20260918.md) — 배치 시각 이동은 관찰 후 결정 |
| `.bat` | ASCII 0 위반 · 191줄 전부 CRLF(혼합 없음) · 9/14·9/18 삽입은 각 3줄 |
| 리더보드 교체 | leaderboard.html(성적표)·leaderboard_full.html(전체)·scoreboard.html(이동) 모두 200, 텔레그램·주간 리캡 링크 유효, 인라인 SEALED 맵은 _full 에 있음(주석 갱신됨) |
| 로그의 KRX ID | pykrx 가 로그인 ID 를 로그에 찍지만 logs/ 는 gitignore — 외부 노출 없음(비밀번호는 출력 안 됨) |
| 테스트 | `python tests/run_tests.py` 통과 |

## 남은 위험(결정 대기 — 이번에 안 고침)
1. **시세 미수집 종목**(코스닥 글로벌 ~50·영문 신규코드 ~81): 리더보드·성적표·운용 채택 관찰·PTW 순위 비교에서 가격 없이 빠짐. lv_b KOSDAQ 상위 10 에 하루 3종목꼴.
2. **배치 시작 20:10**: 애프터마켓 종료(20:00) 직후라 확정 전 종가를 읽는 날이 있음 → `research/probe_close_finalize.py` 로 확정 시각 관찰 후 이동.
3. **리더보드(_full) 실시간 라벨**: IC<−0.03 이면 구간 무관 '역작동' — 정본 기준과 불일치(표시만 숫자로 바꾸는 안).
4. `_large_test.html` 상단 문구 "로컬 전용(docs/ 공개 금지)"가 실제(공개·링크됨)와 다름 — 문구 정리 필요.

## 추가(같은 날 · 외부 검토 반영) — `market_series.py --repair-from` 종료코드
- 정정 경로가 **한 행도 못 고쳤거나 예외가 나면 종료코드 1**(종전엔 경고만 찍고 0 → 자동 실행 쪽에서 성공·실패 구분 불가). 고친 행 수를 출력.
- 배치가 부르는 일반 실행(`main`)은 종전대로 비치명 0. 검증: 자료가 없는 날짜로 실행 → exit 1, DB 무변경. 테스트 통과.
