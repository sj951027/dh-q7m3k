# 2026-09-21 — 종가 정정 감시(측정만) + 미결 2건의 상태 정리 (2026.09.21 · 판정·점수·적재 동작 0-diff)

## 그래서 뭐가 바뀌나
- `universe_ohlcv.py` 증분 실행 끝에 **"[종가 정정 감시]"** 한 줄: 재확인 창(최근 7일)에서 *이미 저장된 종가*와 *방금 다시 받은 종가*가 다른 종목 수를 날짜별로 센다.
  전날 20:10 수집값이 확정 전(애프터마켓 영향)이었다면 다음 날 이 숫자로 드러난다. 직전 날짜에 5종목 이상이면 ⚠️ 한 줄. 누적은 `logs/close_revisions.csv`.
- **측정만 한다** — 적재(upsert)·점수·배치 순서는 그대로. ±5% 넘는 변화는 기존 '수정주가 재조정' 경로라 세지 않는다.

## 왜 (배치 시각 이동을 지금 하지 않는 이유)
- 실측 누계: 9/14 15종목(2.2%) · 9/15 2 · 9/16 11(1.5%) · **9/17 0/146 · 9/18 0/147 · 9/21 0/114**(표본, 네이버 공식 종가 대조). 개편 첫 주에만 나타났고 최근 3거래일은 깨끗하다.
- 시각 이동은 작업 스케줄러(시스템 설정 — 사용자가 직접)와 규칙 문구 변경이 따르고, "몇 시면 되는지"는 평일 저녁 관찰이 있어야 정할 수 있다.
  지금 옮길 근거가 약해져서, **매일 자동으로 재는 장치**를 먼저 넣었다. 다시 나타나면 그때 `research/probe_close_finalize.py` 로 확정 시각을 재고 옮긴다.
- 검토했다가 버린 대안: KIS `daily_flows.close` 를 기준값으로 삼아 자동 교정 — **못 쓴다.** 실측 9/14 이후 과거 날짜의 KIS 종가가 하루 2,000종목 이상 공식 종가와 다르다
  (예: 씨티케이 9/14 공식 2,495 / KIS 2,400). 재수집 때 애프터마켓 포함 값으로 바뀌는 것으로 보인다(추정). 이 값은 수급 '억' 환산에만 쓰여 영향은 1% 미만.

## 미결 1번(시세 미수집 약 130종목)은 왜 지금 안 하나
- 코스닥 글로벌·영문 코드 종목을 ohlcv 에 넣으면 **전체종목 트랙(wu: px_a·sv_b·sv_a·qs_a·le_a)의 유니버스가 바뀐다** — `wu_score.py` 의 유니버스 정의가 "ohlcv 전체 상장"이다.
  px_a 는 판정이 10일 남았고 sv_b 는 9/14 등록 직후라 **spec 동결 중 유니버스 변경**이 된다(불변 규칙 위반). lv_e 의 회전율 팩터(to20)도 ohlcv 에서 읽어 해당 종목 점수가 달라진다.
- 하려면 "평가용 시세만 추가하고 모델 유니버스는 동결"하는 설계가 필요하다: 추가 종목에 표식을 달고 wu/lowvol 채점에서 제외 → 점수 0-diff 증명 → 백필.
  리더보드·성적표·운용 채택 관찰의 lv_b·v30 수치는 (누락 종목이 채워져) 달라지므로 '데이터 정정'으로 기록해야 한다. **px_a 판정(~10/07) 뒤에 착수 권고.**

## 검증
- 합성 DB 단위 확인 + `tests/test_index_source.py` [E] 5체크(총 31): 확정 전 값 정정만 잡음 · 재조정 제외 · 오늘 행 제외 · DB 오류 무해 · 증분 모드에서 upsert 앞 읽기만.
- 실제 수집기는 돌리지 않았다(9/22 배치에서 첫 출력). `python tests/run_tests.py` 통과.
