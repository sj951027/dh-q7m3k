# 2026-09-14 — 배치 끝 텔레그램이 '토큰 없음'으로 건너뛴 버그 · push 계정 분리 (2026.09.9 · 판정·점수 0-diff)

## 그래서 뭐가 바뀌나
- 배치 맨 끝의 텔레그램 알림이 다시 발송된다. `notify_telegram.py` 가 단독 실행일 때도 `.env` 를 스스로 읽는다.
- 이 저장소의 git 원격 주소에 계정 이름을 붙여(`https://sj951027@github.com/...`) 배치의 자동 push 가 개인 계정 자격 증명을 쓴다. 같은 PC 의 회사 계정(다른 저장소)은 그대로.

## 왜 (09-14 첫 실전 배치, 실측)
- 배치 자체는 정상(20:10~21:43, 파이썬 12단계 모두 0, KIS 수급 당일치 9/14 확보 91%).
- 텔레그램: 로그 3200행 "텔레그램 토큰이 없어 알림을 건너뜁니다". 09-12 에 알림을 배치 끝으로 옮기며 `.bat` 이 `python notify_telegram.py` 를 **직접** 부르게 됐는데, 이 스크립트엔 `.env` 로더가 없었다(전에는 `run_and_diversify.py` 가 환경을 실어 준 뒤 안에서 호출). 09-12·09-11 재실행은 옛 경로(4단계 즉시 발송)로 성공해 드러나지 않았다.
- push: 로그 2987행 "Permission to sj951027/dh-q7m3k.git denied to seokjin-kim-cls (403)". Windows 자격 증명 `git:https://github.com` 이 회사 계정으로 저장돼 있었다.

## 어떻게
| 항목 | 변경 |
|---|---|
| `notify_telegram.py` | `_load_dotenv()` 추가(`notify_weekly.py` 와 동일 동작, 기존 환경변수는 안 덮음) · `main()` 첫 줄에서 호출. py_compile OK |
| git 원격(로컬 설정) | `git remote set-url origin https://sj951027@github.com/sj951027/dh-q7m3k.git`. 사용자가 push 하며 브라우저 로그인 → 자격 증명 `git:https://sj951027@github.com` 생성(실측). 회사 계정 항목은 보존 |
| 09-14 알림 | 수정 후 `python notify_telegram.py` 1회 수동 실행 → 22:29 "전송 완료"(실측). 본문은 배치가 만든 미리보기와 동일 |

## 검증·영향
- 점수·판정·게이트·DB 무변경(0-diff). 배치 커밋 2개(21:18·21:41) 는 사용자가 push 해 공개 페이지 반영.
- 같은 날 확인: DART 공시 이벤트 적재가 접속 끊김으로 실패(비치명·관측용). 이 하위 단계 실패는 배치 끝 [OK] 요약이 못 잡는다(요약은 .bat 레벨 12단계만) — 별건.
- 09-15 배치에서 확인할 것: 텔레그램 자동 도착 · push 성공("업로드 완료") · DART 이벤트 재시도.
