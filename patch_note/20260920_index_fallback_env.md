# 2026-09-20 — 지수 예비 경로(.env 미로드) 수정 + 9/17~18 지수 정정 (2026.09.17 · 판정·점수 0-diff)

## 그래서 뭐가 바뀌나
- `market_series.py` 가 이제 `.env` 를 읽는다 → FDR 지수(KS11/KQ11)가 멈췄을 때 **pykrx(KRX 로그인) 예비 경로가 실제로 동작**한다.
- 새 옵션 `python market_series.py --repair-from YYYYMMDD`: 그날부터 시세 마지막 날까지 KOSPI/KOSDAQ 를 KRX 값으로 덮어써 정정(REPLACE).
- 9/17·9/18 지수 정정 실행: KOSPI 9/17 6724.34→**6715.41**, 9/18 없음→**6894.23** · KOSDAQ 9/17 821.67→**822.18**, 9/18 없음→**827.12**. 네이버 지수 API 값과 일치(교차 확인). 정정 전 값은 `research/market_daily_before_repair_20260920.csv`.

## 왜
- 9/18 배치 텔레그램이 "⚠️ 확인 필요 — 지수 시계열 정지: KOSDAQ 09/17 · KOSPI 09/17". 원인 둘:
  1. FDR 지수 소스가 9/17 장중에 멈춤(9/20 현재도 9/18 값 없음, 9/17 값은 확정 전 수치). 종목 시세·환율은 정상.
  2. 9/9 에 넣은 pykrx 예비가 처음 발동했는데, 이 스크립트만 `.env` 를 안 읽어 `KRX_ID/KRX_PW` 가 비어 로그인 실패. (밸류에이션 수집기의 KRX 로그인은 9/7~9/18 매일 성공 — 계정 문제 아님. 9/14 텔레그램 토큰 건과 같은 종류의 배선 누락.)

## 어떻게
| 파일 | 변경 |
|---|---|
| `market_series.py` | `_load_dotenv()`(기존 값 안 덮음·값 미출력) 를 main/repair 앞에서 호출 · `KRX_INDEX` 모듈 상수화 · `repair(start)` + `--repair-from` |

## 검증(실측)
- `--repair-from 20260917`: KRX 로그인 완료, 4행 정정/보충, 값이 네이버 지수 API 와 일치.
- 일반 실행: FDR 지수는 여전히 빈 응답(건너뜀), 지수 마지막=시세 마지막(9/18)이라 예비 미발동, 환율 정상. `python tests/run_tests.py` 통과.
- 점수·판정·게이트 코드 미접촉. 영향은 지수를 쓰는 표시·국면(스크리너 regime 입력, 리더보드 지수선, 운용 채택 국면 구분)뿐 — 9/21 배치에서 정정값으로 재계산됨. 9/18 스크리너의 시장 국면 점수는 9/17까지 지수로 계산된 채 동결(영향 경미, 기록).

## 남은 것
- FDR 지수 소스가 계속 죽어 있으면 매일 예비(pykrx)로 채워짐 — 9/21 로그에서 "[예비 pykrx] 신규 1행" 확인. 네이버 지수 API 를 2순위 예비로 넣을지는 보류(사용자 결정).
