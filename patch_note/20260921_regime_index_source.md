# 2026-09-21 — 시장 레짐이 멈춘 지수를 쓰던 문제 + 지수 테이블 자동 정정 (2026.09.19 · 평소 결과 0-diff)

## 그래서 뭐가 바뀌나
- **스크리너의 시장 레짐 분석**이 지수를 고를 때, 원 소스(FDR KS11/KQ11)가 우리 DB(`ohlcv.db market_daily`)보다 **뒤처져 있으면 DB 값을 쓴다.**
  둘이 같은 날까지 있으면 종전대로 FDR — 그래서 평소 결과는 한 글자도 안 바뀐다. 대체했을 때는 로그에 한 줄:
  `ℹ️ 지수 소스: ohlcv.db market_daily (~2026-09-21) — FDR 는 2026-09-17 에서 정지`.
- **지수 테이블(`market_series.py`)이 임시값을 스스로 바로잡는다**: 증분 실행 때 최근 7일을 다시 받아 덮어쓰되, **소스의 마지막 행만은 덮어쓰지 않는다.**
- 지수 값 정정: 8/27 KOSPI 6879.87→**6912.37** · KOSDAQ 834.39→**837.65**(20:10 에 받은 확정 전 값이 그대로 남아 있었음. 네이버·FDR 현재값과 일치). 9/17·9/18·9/21 은 KRX 값으로 확인.

## 왜
- 9/21 배치 로그: 레짐 분석이 `KOSPI: 6,724.34 (+0.09% vs 어제)` — 9/17 장중에 멈춘 값. 실제 9/21 종가는 7,007.72(KOSDAQ 821.67 vs 836.27).
  9/20 에 고친 것은 **지수 테이블을 채우는 쪽**뿐이었고, 스크리너 본체는 같은 죽은 소스를 **따로 직접** 읽고 있었다. 9/18·9/21 이틀 레짐 입력이 낡았다.
- 영향(코드 확인): 레짐 점수는 시장 전체에 같은 값이 더해지므로 **시장 안 종목 순위·리더보드·성적표 무영향**. 낡은 것은 화면·텔레그램의 레짐 수치와
  PTW 상단 국면 칩. 이번 구간은 정정 전후 레짐 분류가 같아(코스피 강세 0 · 코스닥 반등 −3) **점수 변화는 0** 이었다.

## 0-diff 검증 (`research/verify_regime_index_source_20260921.py`, 적용 전 복사본으로 실행)
| 비교 | 결과 |
|---|---|
| ① 정상 모드(FDR·DB 같은 날까지) — 2026-06-01~09-16 매 거래일 as-of, 코스피·코스닥 | **75일 × 2시장, 다른 날 0** |
| ② 예비 모드(FDR 가 3거래일 멈췄다고 가정 → DB 사용) vs FDR 가 정상이었다면 냈을 값 | **72일 × 2시장, 다른 날 0** (8/27 정정 뒤. 정정 전엔 그 하루 때문에 3~4일 차이 → 그래서 8/27 임시값을 발견) |
| 두 소스의 과거 값 자체 | ~9/16 416일 중 불일치 0일(정정 뒤) |
| ③ 실제로 바뀌는 날 | 9/18: 6724.34→6894.23 · 9/21: 6724.34→7007.72(코스닥 821.67→827.12→836.27). 레짐 분류·점수는 동일 |
- 계산식은 `_regime_from_close` 로 **옮기기만** 했다(산식·반올림 동일 — 테스트가 산식을 다시 계산해 대조).

## 작업 중 낸 사고와 수정 (기록)
- 처음엔 "최근 7일을 전부 덮어쓰기"로 넣었다가, **멈춘 FDR 의 9/17 임시값(6724.34)이 KRX 정정값(6715.41)을 되돌려 버렸다.** 실행 직후 DB 조회로 발견 →
  규칙을 "소스의 마지막 행은 IGNORE, 그 앞만 REPLACE"로 고치고 9/17 을 다시 정정. 일반 실행을 두 번 더 돌려도 정정값이 유지되는 것을 확인. 이 사고를 테스트로 고정했다.
  (임시값은 '받는 시점의 당일 행'에서만 생긴다 — 소스가 살아 있으면 다음 날 그 행은 마지막이 아니게 되어 정정되고, 소스가 멈춰 있으면 영영 안 건드린다.)

## 어떻게
| 파일 | 변경 |
|---|---|
| `screener_fdr_v2_6.py` | `_index_close_db`(mode=ro) · `_pick_index_series`(순수: FDR 가 DB 만큼 최신이면 FDR) · `_index_close_series` · `_regime_from_close`(본문 이동). `_analyze_index_regime` 는 이들을 호출 |
| `market_series.py` | `REFRESH_DAYS=7` 재수집 · 마지막 행 IGNORE/그 앞 REPLACE · 신규 행 수는 '마지막 날짜 뒤'만 셈 |
| `tests/test_index_source.py`(신규 18체크) + `tests/run_tests.py` 등록 | 소스 선택 7 · 산식 2 · 배선 4(정지/정상/예외) · 지수 테이블 규칙 5 |
| `research/verify_regime_index_source_20260921.py` · `research/market_daily_before_repair_20260921.csv` | 0-diff 검증 스크립트 · 정정 전 값 백업 |

## 검증
- `python tests/run_tests.py` 전체 통과(신규 포함). 실제 함수 실행: 코스피 `강세 0 · 7007.72 · 전일 +1.65%`, 코스닥 `반등 −3 · 836.27 · +1.11%`.
- 배치 순서 확인: `universe_ohlcv` → `market_series`(지수 채움) → `run_and_diversify`(스크리너) — 스크리너가 읽을 때 DB 에 오늘 지수가 이미 있다.

## 추가(같은 밤, 사용자 지시) — 메인 페이지 IC 박스의 지수 소스도 같은 방식으로
- `validate_scores.PriceProvider.get_close` 가 **지수 코드(KS11·KQ11)에 한해**, 요청 구간에서 우리 DB(`market_daily`)가 FDR 보다 최신이면 DB 시리즈를 돌려준다.
  FDR 가 DB 만큼 최신이면 종전 반환값 그대로. **종목 코드는 예비 소스를 아예 조회하지 않는다**(경로 불변). `compute_ic.py` 는 이 제공자를 그대로 쓴다.
- 왜: '시장초과 수익'은 지수의 이후 종가가 있어야 나온다. 소스가 9/17 에서 멈춰 있으면 9/17 뒤 날짜가 필요한 앵커(20일 지평이면 8월 하순 진입분부터)의 초과수익이 비어 간다.
  §11 리더보드·성적표는 지수를 안 써서(같은 시장 종목 평균·중간값) 무관 — 영향은 메인 페이지 IC 박스(`ic_summary.json`·`ic_history.json`).
- 0-diff(적용 전 복사본, 임시 캐시 폴더로 실행): 끝 날짜 ≤9/16 인 과거 구간 14개 × 2지수 **반환 시리즈 완전 동일** · 종목(005930) 동일 ·
  오늘까지 구간은 마지막 9/17 6724.34 → **9/21 7007.72**, 9/17 값은 6724.34→6715.41(KQ11 821.67→836.27, 821.67→822.18).
- 테스트: `tests/test_index_source.py` [D] 8체크 추가(총 26) — 정지 시 DB · 최신이면 FDR 그대로 · 종목은 예비 미조회 · FDR 예외 시 지수는 계속/종목은 None.
- `compute_ic.py` 는 실행하지 않았다(돌리면 `ic_history.json` 에 오늘 줄이 하나 더 쌓인다) — 9/22 배치에서 자연 반영. 로그에 `지수 소스: ohlcv.db market_daily` 한 줄이 찍힌다.

## 남은 것
- FDR 지수 소스 복구 여부는 매일 로그의 `지수 소스:` 줄 유무로 확인(줄이 사라지면 복구된 것 — 그땐 세 곳 모두 종전 경로로 돌아간다).
- 지수를 직접 읽는 곳 전수: `market_series.py`(테이블 적재 — KRX 예비) · `screener_fdr_v2_6.py`(레짐) · `validate_scores.py`(IC 시장초과) — 세 곳 모두 조치 완료. 다른 스크립트는 지수를 DB 에서 읽거나 쓰지 않는다(grep 확인).
