# 2026-09-04 — 리더보드 2안(쉬운 버전) 이식 + v30/lv_b 짝비교 연구

## 뭐가
1. `docs/leaderboard.html` 전면 교체(표시 전용). 구조: 요약(지금 기준·판정 캘린더) → 읽는 법 3칸 → ① 돈 표(cross_sim trailing) →
   ② 현역 판정표(진행 막대·20일 초과 %p+CI·이긴 비율·IC·확신 점·**정본 판정 상태**·한 줄 번역) → 접힘: 판정 끝난 모델 / 은퇴 /
   원본 전체 컬럼(자세히 보기) / 쉬운 설명 / 요즘 폼 / 과거 고정 창 / α·β. 모델명 링크(MODEL_PAGES)·h5 추이 스파크 유지.
   은퇴 판별은 json `retired` 플래그 + 폴백 13개. 정본 판정(SEALED 맵)은 HTML에 봉인값으로 두고 판정·은퇴 시 갱신.
   종전 파일은 `archive/leaderboard_html/leaderboard_20260904_pre_v2.html` 보존.
2. `research/pair_v30_lvb_20260904.py` + `research/RESEARCH_pair_v30_lvb_20260904.md` — v30 vs lv_b 같은 창·같은 종목 짝비교(관측).
3. `research/leaderboard_decision_mock_20260904.html` — 3안(결정 보드) 시안, 미채택(사용자: 2안 써보며 검토).

## 왜
- 사용자 피드백: 기존 표는 IC 중심이라 "어떤 모델이 좋은지·뭘 골라야 하는지"가 안 읽힘. 특히 자동 라벨(누적 IC 재계산)이 정본
  판정(VERDICT 봉인)과 어긋나 lv_b가 '기움'인지 '노이즈'인지 헷갈림 → 상태 칸을 정본으로, 자동 라벨은 '자세히'로 내림.
- 짝비교 실측(연구 문서): v30 '유의'는 앞 13앵커(6/06~6/24 급락→반등, IC +0.187)가 만든 것이고 lv_b 와 겹치는 27앵커에서는
  v30 −0.027 / lv_b +0.031, 같은 종목 짝비교 lv_b − v30 = +0.063 iid CI>0·주블록 CI 0 걸침 → '우위 기움'. 원장 "v30 거취 재론" 재료.

## 어떻게 + 검증
- 실데이터(9/4 json 5종)로 로컬 렌더: 콘솔 오류 0, 현역 9·판정완료 3·은퇴 13 분류 정상, ①표 정렬(1개월 순)·링크·접힘 동작 확인.
- 점수·판정·json: **0-diff**(HTML만 교체, leaderboard.py 무접촉).

## 영향범위
- 표시만. 다음 배치가 json을 갱신하면 그대로 반영. 판정/은퇴가 생기면 HTML 의 SEALED/RET_* 맵을 같은 세션에서 갱신해야 함(체크리스트 항목).
- 후속 후보: 9월 중순 v30 2차 창(6/25 이후) 판정 + lv_b 짝비교 정본화 · 텔레그램 요약을 이 페이지 구조와 맞추기.
