# 2026-08-21 장중 캐치업 실행 오염 정리 (+ 8/17 skip_once 미작동 사후 기록)

## 뭐가
- 8/20(목): 컴퓨터 이상으로 배치 완전 미실행 → 관측일 1개 손실(8/13 전례와 동일 유형).
- 8/21(금) 10:30경: 부팅 직후 작업 스케줄러 '놓친 작업' 캐치업으로 배치가 장중에 실행됨.
  사용자가 2.6단계(v3_daily)에서 강제종료 → stage1(2,309)·stage2(506)·stage3(267)·runs(2)에
  run_id=20260821 이 **장중(10:30) 가격 기준**으로 적재됨. v3/lowvol/wu/large 동결은 미도달(0행).
- 정리: `_trash/cleanup_20260821/fix_win.py`(Windows 실행)로 위 4테이블 20260821 삭제 +
  ohlcv.db market_daily 20260821(장중 지수 3행) 삭제. 백업 CSV 동일 폴더에 보관.

## 왜
- 그대로 두면 저녁 배치의 중복 실행 가드(§24)가 (market, 20260821) 재적재를 스킵 →
  오늘 관측일이 장중 스냅샷으로 굳고, 그 위에서 v3/lowvol/wu 동결 → 판정 표본 오염.
- market_daily 는 INSERT OR IGNORE 적재라 장중값 자가치유 불가 → 수동 삭제 필요.
- daily_ohlcv 는 증분 윈도우 재조회 + INSERT OR REPLACE(universe_ohlcv.py 실측 확인)라
  저녁 실행이 종가로 덮어씀 → 건드리지 않음. universe_events 16행은 이벤트성이라 무해.

## 어떻게 + 검증 (전부 실측)
- Cowork 브릿지 FS는 SQLite 커밋 불가(저널 unlink/fsync 미지원 → disk I/O error) —
  VM측 삭제 시도는 핫저널 롤백으로 자동 복원됨(데이터 무손상 확인). **DB 쓰기는 반드시
  Windows 쪽에서** 실행해야 함(교훈).
- 정리 후 검증: 전 9테이블 20260821=0 / 주변 거래일(0814~0819) 행수 이전 실측과 완전 일치
  (rollback 부수손상 없음) / history.db·ohlcv.db PRAGMA quick_check=ok / 핫저널 소멸.
- fix_win.py 안전장치: v3_scores 에 20260821 동결이 이미 있으면 중단(저녁 배치 경합 방지).

## 8/17 skip_once 사후 기록
- 8/17 패치(run_auto_logged.bat + skip_once.flag)는 **작동하지 않았음** — 스케줄러가
  run_auto_logged.bat 를 쓰지 않고 옛 방식(단일 logs/auto_run.log 외부 리다이렉트)으로
  run_all_and_diversify.bat 를 직접 호출함을 로그로 확인. bat 주석과 실제 스케줄러 불일치.
- 결과: 8/17(광복절 대체휴일) 밤 유령 run_id=20260817 적재(stage1 2,312 · v3 94행 동결,
  wu 는 ohlcv 날짜 기반이라 미적재). 6/3 선거일 전례와 동일. 동결분 사후 수정 금지 원칙에
  따라 그대로 두고 leaderboard 게이트(비거래일→직전 거래일 앵커·정적 run 제외)에 위임.
- skip_once.flag 는 _trash/cleanup_20260821/ 로 이동(미소비 상태였음).

## 영향범위
- **판정·점수 0-diff**: 삭제분은 전부 동결 이전 stage 데이터 — 동결점수·판정 이력 무변경.
- 관측일 손실: 8/20 1일(복구 불가·복구 시도 금지 — 사후 실행은 당일 데이터가 됨).
  lv 계열 40거래일 도달은 8/17 휴장 + 8/20 미실행으로 당초 ~8/25 → **~8/27 전후로 밀림(추정)**.
  notify_verdict_ready 가 도달 시점 자동 판별하므로 별도 조치 불요.
- 재발 방지(미적용, 사용자 결정 대기): ① 스케줄러 '놓친 작업 즉시 시작' 옵션 해제(설정만,
  코드 무변경) ② 배치에 장중 시간대(09:00~15:30) AUTO 실행 가드 추가(코드 변경 — 판정
  시즌 후 검토 권장). ③ 스케줄러를 run_auto_logged.bat 로 재지정(skip_once 활성화 겸사).
