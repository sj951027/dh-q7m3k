# 2026-09-05 — 희석 공시 60거래일 배지 (표시 전용 · 점수·순위·유니버스 0-diff)

## 뭐가
1. `dilution_flag.py`(신규) — ohlcv.db `dart_events` 에서 직전 60거래일(market_daily KOSPI 달력) 내 유상증자·CB·BW·EB·유무상혼합
   '결정' 공시(정정 제외)를 종목별 최신 건으로 뽑아 `dilution_60d` 컬럼('유상증자 08/28' 또는 빈칸)을 붙인다. 실패 시 빈 플래그(비치명).
2. CSV 생성기 4곳에 부착 훅: `build_lowvol_filter.py`(lowvol·lva) · `build_mom_filter.py` · `build_wu_filter.py`(sv·qs·px) ·
   `diversify_picks.py`(v3 enriched — filter.html 원천). asof = 그 CSV 의 run_id(PIT).
3. 페이지: lowvol/lva/mom/sv/qs/px 에 '⚠️희석공시' 열(종목 옆, 툴팁에 근거) · filter.html 은 종목명 옆 💧 배지 + '⚠️희석공시' 열
   (기본 열·V3 추천 프리셋에 포함. 저장된 열 설정이 있으면 '초기화' 후 보임 — 배지는 항상 보임).
4. 현재 docs/루트 CSV 11개에 컬럼을 즉시 부착(다음 배치부터는 생성기가 자동).

## 왜
3년·PIT 이벤트 스터디(research/RESEARCH_fullscan_20260903.md §3): 희석 결정 공시 후 60거래일 같은 유니버스 대비 −1.4~−6.6%p/60일
(전체 −3.9, 대형 −6.6, 조용함 top50 −4.6 — 전부 CI<0), 걸리는 비율 3~9%. 9/4 표시물 실측: 코스닥 v3 상위20 중 2종목, 코스피 lv_b 상위20 중
1종목(207940), sv_a 상위20 중 1종목이 걸려 있었는데 표시가 없었다. 자동 컷(lv_e_x 등 새 모델)은 판정 시즌 뒤 사전등록으로 — 지금은 배지만.

## 어떻게 + 검증
- 사본 샌드박스에서 생성기 6회 실행: lowvol/lva/mom CSV 는 기존 컬럼 전부 0-diff + 새 컬럼(코스피 6·코스닥 25종목 플래그).
  sv/qs/px 는 점수·순위 0-diff(수급·섹터 열은 배치 이후 갱신된 ohlcv 데이터 차이 — 코드 무관), 플래그 78종목.
- 페이지 렌더(실 CSV): lowvol 배지 6 · sv 77 · filter 💧 11, 콘솔 오류 0.
- `dilution_flag.py 20260904` 단독 실행: 220종목.

## 영향범위
- 점수·판정·유니버스·분산 로직 무접촉. 표시 컬럼 1개 추가.
- 배치 순서상 dart_events 적재(2.896)가 CSV 생성(2.87~2.89) 뒤라 배지는 전일까지의 공시 기준(하루 지연). 순서 변경은 하지 않음(안정 우선).
- 미적용: 대형 트랙 페이지(_large_obs/_large_test) — 효과가 가장 큰 곳(−6.6%p)이라 다음 후보. wu.html 은 은퇴 스냅샷이라 제외.

## 추가 (같은 세션) — 연구·점검
- 노출 배너: 추가 점검(격자·연도별·비용·신규진입만·조건부)에서 기각 → **채택 안 함**(research/RESEARCH_overlay_entry_20260905.md ⑤). 코드 변경 없음.
- 진입 지연·경로 실측: v30·lv_b 는 다음날(장 시작 직후) 매수 유효, lv_a·sv_a 는 단기 신호(같은 문서 ②③).
- 표시 '행동 유도' 기능 감사: research/UI_NUDGE_AUDIT_20260905.md — 근거 없는 필터 4종(수급 반전·외인/기관 5일·떨어지는 칼날 배지 방향)을 확인, 처분은 사용자 결정 대기.
- 전체 점검: 변경 py 컴파일·import OK, 페이지 9개 태그 정합, CSV 11개 컬럼 확인, tests 통과, 줄바꿈 보존.
