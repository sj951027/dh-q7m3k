# 2026-09-04 — 운영 정비 4건: DART 캐시 TTL · .bat ASCII · compute_ic 게이트 통일 · ohlcv 수정주가 단절 교정

## 뭐가
1. `.env` — `DART_NODATA_TTL_HOURS=72` 추가(코드 무변경. 기본 12h → 72h).
2. `run_all_and_diversify.bat` 4~5행 — 한글 rem 2줄을 ASCII rem 으로 교체(동작 무변경, 파일 전체 ASCII).
3. `compute_ic.py` — 부분실행 게이트 기준 `stage3_final` → `stage1_oversold`(leaderboard·check_completeness 와 동일 원칙).
4. `universe_ohlcv.py` — 증분 재확인 창에서 기존 종가와 새 종가가 5% 넘게 어긋나면(`ADJ_TOL`) 수정주가 재조정으로
   보고 그 종목 전 기간을 다시 받아 교체(주식수는 기존 PIT 값 보존). `--tickers A,B,..` 옵션으로 지정 종목 전 기간 재적재.

## 왜 (실측)
1. KOSDAQ 3단계가 8/31부터 매일 ~61분(8월 유니버스 20종목일 땐 40초). 캐시 적중률 35%, 미스 1,905건/일 —
   '데이터없음(013)' 응답이 12시간만 캐시돼 매일 재조회. TTL 72h 면 재조회가 3일 1회로 줄어듦(단축폭은 다음 로그로 실측).
   부작용: 새 분기보고서가 최대 3일 늦게 반영(점수식 불변, 미스 경로 동일).
2. 로그 첫 줄 `'6]'은(는) 내부 또는 외부 명령…` — `chcp 65001` 뒤 cmd 가 배치를 다시 읽으며 한글 rem 의 조각 "6]"을
   명령으로 실행(8/31~9/3 매일). 실행엔 영향 없었으나 8/07 인코딩 사건과 같은 부류.
3. §28-2(8/07) 교정이 compute_ic 에는 미적용 → 대시보드 IC 카드(ic_summary.json)가 8/04~8/28 정상 17일을 부분실행으로
   제외 중. 판정 정본(leaderboard)은 무관. 재현: stage3 기준 floor 346 → 17일 제외 / stage1 기준 floor 682 → 20260608 만 제외.
4. **데이터 단절 발견**: FDR 은 감자·분할 뒤 전 기간 수정주가를 다시 주는데, 증분은 최근 7일만 덮어써 그 이전 행이 옛
   배수로 남음 → 재상장일에 +100%/+400%/+900%/+1,577% 가짜 급등. 8/28 백업 대비 15종목(8/21~8/27) 확인, 3년 전체
   스캔에선 |일수익|>60% 단절 116종목(2026-06 이후 102종목 — 반대매매·감자 시즌).
   영향: 판정 IC 는 32% 컷이 앵커 단위로 걸러 미세(v30 동결창 골든 +0.0584 → 실측 +0.0576, 코드 원인 아님 — 8/28 백업으로
   v3_scores 0-diff·ohlcv 15종목 종가 변경 확인). 팩터(lv60·mom12·nh252 등)는 단절 이후 60~252일간 해당 종목이 왜곡.

## 어떻게 + 검증
- 3: `python -c` 로 stage1/stage3 게이트 집합 재현(위). 판정·점수 0-diff(리더보드 미접촉). ic_summary.json 은 다음 실행부터
  8월 앵커 포함 → **IC 카드 수치가 한 번 바뀜(의도)**.
- 4: 오프라인 합성 테스트(임시 DB, fetch 가짜 함수): ① 재확인 창이 단절을 걸치면 감지 → 725행 전 기간 재적재, 단절 소멸,
  주식수 778행 전부 보존, 타 종목 0-diff ② `--tickers` 경로: 전 기간 재적재 후 최대 |일수익| 0.0, DB 최초일(2023-06-26)부터
  교체(백필 기준일 2023-08-30 보다 이른 행도 포함하도록 `full_start_for`) ③ 정상 종목은 감지 안 됨(rows 12).
  라이브 FDR 은 네트워크라 미검증 — 아래 1회성 실행 로그로 확인.
- 1·2: 구문·ASCII 확인. tests/run_tests.py: lowvol 17·wu 10 통과, leaderboard 골든은 위 4번 데이터 단절로 v30 만 −0.0008
  이탈(코드 변경과 무관, 골든 미수정). **재적재 후 재실행해 복귀 확인 필요.**

## 사용자 1회 실행 (네트워크)
```
python universe_ohlcv.py --tickers %READJ%   # research/fullscan_20260903/out/readjust_tickers_20260904.txt 의 102종목(쉼표) 
python tests/run_tests.py                    # v30 골든 +0.0584 복귀 확인
```
이후 매일 증분은 자동 감지(♻️ 로그) — 재조정 감지 시 그 종목만 전 기간 재수집(종목당 1회 추가 호출).

## 영향범위
- 판정·점수 0-diff(3·4 는 표시/데이터 정합 교정, 점수식 무변경). 재적재 후 리더보드 IC 가 미세하게 움직일 수 있음(정합 회복).
- research/fullscan_20260903 결과는 단절 포함 상태로 계산됨 — 가드(점프 21일 컷)가 대부분 흡수하나 재적재 후 재실행 권장.

## 추가 (2026-09-04 저녁 — 9/4 로그 확인 후)
- 9/4 실행 정상 완료(20:10→22:02). ②③④ 효과 확인: 로그 첫 줄 오류 소멸 · compute_ic 게이트 {20260608}만 제외 ·
  ohlcv 재조정 자동 감지 1건(175250, 779행 재적재).
- ① TTL 은 적용됐으나(적중률 35→37%) **KOSDAQ 3단계 61분의 진짜 원인은 캐시가 아니었음**: 9/1 63 → 9/2 69 → 9/3 68 →
  9/4 89종목이 DART 재무 호출에서 `RemoteDisconnected`(연결 끊김) 3회 재시도 후 실패 → `not_found` 로 펀더멘털·OCF 결측
  (KOSDAQ 3단계의 13%, 8/28엔 4건). 종목당 최대 6호출×재시도 대기라 이것이 소요시간 대부분 [추정: 89×6×~13s/2스레드 ≈ 58분].
  실패는 배치 전 구간에 고르게 분포(10분위별 3~15건). `research/dart_probe_20260904.py`(사용자 실행) — 문제 종목 3개를
  단독 호출하면 헤더 변형 4종 전부 정상(0.1~0.4s, 55~280KB) → **배치 부하 중에만 끊김**.
- 조치 5: `stage3_fundamental_momentum_v2_6.py` — 배치 끝에 REQUEST_ERROR 종목만 순차(0.3s 간격) 2차 시도, 복구분으로 교체.
  오류 없는 종목 결과 무접촉(0-diff). 복구분은 재무 캐시(14일)에 들어가 다음날 적중. 오프라인 모의(가짜 analyze_one):
  정상 종목 불변·복구 종목 교체·재실패 종목 유지·카운터 재계산 확인. 라이브 효과는 다음 로그의 `🔁 2차 시도 결과` 로 확인.
- 남은 의문: 왜 9/1부터 급증했는지(유니버스 20→600종목 복귀 + DART 측 변화 가능) — 2차 시도 후에도 복구율이 낮으면
  MAX_WORKERS 2→1 / REQUEST_SLEEP 0.05→0.2 검토.

## 추가 2 (2026-09-04 — 102종목 재적재 후 골든 처리)
- `--tickers` 재적재 실행(92종목 매칭, 10종목은 상폐로 목록 부재, 71,608행 교체). 그 후 v30 골든 +0.0584 → +0.0573.
  원인 [실측, 8/28 백업 DB 대조]: ① 032960·214330·446840·123010·335870 — 6월부터 있던 단절이 재적재로 이어지며
  JUMP_CAP 제외 → 포함으로 바뀜(데이터 정확도 상승) ② 210980 — 권리락(8/24) 종가가 원천에서 4,445→3,024 로 정정돼
  JUMP_CAP 제외. 8/28 백업 시세로 계산하면 +0.05836 = 골든 재현 → 코드 원인 아님 확정.
- 조치(사용자 승인 B안): 동결창 forward 수익 스냅샷 `tests/frozen_fwd_h20.csv`(49앵커·51,535행·JUMP_CAP 적용, 1.3MB)
  + 생성기 `tests/make_frozen_snapshot.py`. 테스트는 스냅샷을 읽어 코드만 검사. 골든 v30 +0.0573 재동결(lv_b·lv_c 불변).
  `python tests/run_tests.py` → 전체 통과(17+10+15).
