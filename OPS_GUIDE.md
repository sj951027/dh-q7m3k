# OPS_GUIDE — 운영 권고 (어떻게 사고 언제 팔면 유리했나) **[권고 — 2026-09-06]**

> 성격: **권고이지 규약이 아니다.** 사용자가 어떻게 매매하든 막지 않는다. 화면·알림은
> "지금 이렇게 권하고 있는데, 괜찮으세요?" 수준으로만 말한다. 사용자의 실제 실험(다른 보유일·
> 다른 종목·다른 모델)은 이 문서 밖에서도 언제든 가능하고, 그것이 데이터가 된다.
> 정말 아닌 것(코드가 실제로 막는 것)은 §2에 따로, **왜 막는지**와 함께 적는다 — 그 목록 밖은 전부 자유.
> (2026-09-06 사용자 결정: 강제하면 실험도 확장도 못 한다 → 권고로 격하. 이전 이름 PREREGISTER_ops 폐기.)
> 근거 연구: `research/RESEARCH_zoo2_ml_20260905.md` §4 · `RESEARCH_overlay_entry_20260905.md` ②③ ·
> `PTW_LOWVOL_RESCORE_20260905.md` B·D·E · `shadow_ops_portfolio.py` · `ops_overlap_20260906.py` · `fullscan_20260903/step26_tranche.py`

## 0. 쉬운 설명

- 저녁에 뽑힌 종목은 **다음날** 사면 된다(시가든 종가든 차이 없음).
- **시장별 상위 10 안**(코스피 10·코스닥 10)이 데이터상 유리했다. 희석 공시(⚠️ 배지)는 피하는 게 유리했다.
- 한 번에 다 사지 말고 **몇 주에 나눠** 사면(트랜치) 평균 수익은 같고 '시작일 운'이 없어진다.
- 산 뒤 **40거래일**은 둔화·익절·RSI 급락·약세 신호가 떠도 들고 가는 쪽이 유리했다. 40일째 교체.
- 손절 없음이 유리했다. 데이터 점검(⚠️) 알림만 예외.
- 지금 운용 중인 건 **lv_b·40일**(사용자 실행 기준). v30 병행은 데이터가 지지하지만 사용자 선택.

## 1. 권고 표 (안 따라도 된다 — 화면은 '괜찮으세요?'만 묻는다)

| 항목 | 권고 | 근거(실측) | 안 따르면 화면이 하는 일 |
|---|---|---|---|
| 대상 모델 | v30(유의)·lv_b(기움) — 리더보드 정본 둘 | VERDICT_20260809 · VERDICT_20260829_lowvol | 없음 |
| 후보 | 시장별 상위 10 | N10 > N20 > N40 (구성 격자, 비용 후) | PTW: "#N위 — 권고(상위 10) 밖입니다, 괜찮으세요?" 한 줄(저장은 됨) |
| 희석 배지 | 피하기 | −1.4~−6.6%p/60d | 화면 배지만(⚠️) |
| 진입 | 다음 거래일, 시가·종가 무관 | 40일 시가−종가 −0.4~−0.5%p, CI 0 걸침 | 없음 |
| 분할 | 4트랜치(주 1회 1/4) | 평균 불변, 시작일별 CAGR 1.5~12.7% → 6.0~8.0% | 없음 |
| 비중 | 동일가중(순위가중은 +2%p/년, in-sample) | construction_grid | 없음 |
| 보유 | 40거래일, 중간 신호는 매도 근거 아님 | 모든 중간 상태 뒤 20일 초과 양(+); 둔화·익절 매도 13건 40일 대비 −6.2%p | PTW: 신호 밑에 '참고 — 40일 유지가 우위' 한 줄, 40일째 리마인더 1회. 알림은 억제하되 화면 신호는 그대로 |
| 손절 | 없음 | −7% 상태 뒤 20일 +3%p | 없음 |
| 재진입 | 40일째 교체, 그날 상위 10이면 다시 | — | 없음 |
| v30 병행 | 데이터는 '둘 다' 지지 | 겹침 1.2종목, 앵커별 상관 −0.1, 합치면 편차 2.65/3.14→1.84 | 없음 |

## 2. 막는 것 (코드가 실제로 막는다 — 이 목록 밖은 전부 자유)

| 무엇 | 어디서 | 왜 |
|---|---|---|
| 보유수량보다 많은 매도 | PTW `POST /api/trades` 400 | 실현손익·현금흐름이 어긋나 대시보드 숫자가 전부 틀림(v5.7) |
| 미래 거래일 입력 | PTW 400 | 보유일수 음수·최고가 창 공백(v4.9.2) |
| 관측 팩터를 점수식에 넣기 | dh 프로젝트 규칙 | 검증 안 된 팩터가 판정 오염 — 새 model_id + 사전등록으로만 |
| 판정 전 스펙 변경·판정 직전 신규 등록 | dh §11 | 동시검정 분모·동결 원칙 |
| 내부자(elestock) 신호 사용 | dh 규칙 | 사유(장내매수/무상증자/상속) 구분 불가 |

**막지 않는 것(명시)**: 상위 10 밖 매수, 40일 전 매도, 손절, 트랜치 없이 한 번에 매수, 다른 모델 매수, 다른 보유일 실험.

## 3. 관측 (평가는 하되 '무효'는 없다)

- 그림자 포트: `research/shadow_ops_portfolio.py` — 권고를 기계적으로 따랐을 때의 성적(앵커 단위 n≈40·5거래일 트랜치 단위).
  현재 기준선(6~8월 단일 국면): h40 v30 +7.2%p [+5.6,+9.0]·lv_b +9.4%p [+7.8,+11.0], 잣대 = 같은 시장 전종목 중앙값
  (⚠ 시총가중 지수 대비는 급락 국면에서 +20%p 착시 — 쓰지 않는다).
- 장부 대비: 실현 포지션 40일 초과 vs 그림자 포트. 차이가 크면 "권고와 다르게 했더니 이랬다"는 정보일 뿐 — 사용자의 실험 결과다.
- 부수 관측(가중 0): 권고 준수율(상위 10 안·40일 유지), 트랜치별 분산, 희석 배지 종목 매수 수. **준수율이 낮아도 평가 무효 없음** — 다만
  "권고 검증"이 아니라 "실험 결과"로 라벨만 바꿔 읽는다.
- 권고 자체를 바꿀 때: 새 실측(장부 20건 이상 또는 3년 패널) + 이 문서 개정 + patch_note. 이전 수치는 지운다 대신 '이력'으로 남긴다.

## 4. 이 문서가 아닌 것

- §11 모델 판정이 아니다(그건 리더보드·VERDICT). 장부 성적으로 모델을 바꾸지 않는다.
- 사전등록이 아니다 — 동결·골대·무효 조건이 없다. v30 2차 창 같은 판정 사전등록은 PREREGISTER_* 문서가 맡는다.
