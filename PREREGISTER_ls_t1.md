# PREREGISTER — ls_t1 (대형 트랙 테스트 모델, 사전등록)

- **등록일**: 2026-08-06 (이후 spec 동결 — forward-only)
- **정체**: 대형 가치 트랙(B)의 **테스트 모델**. 라이브 점수·v3·lowvol·wu에 무영향(표시·관측 전용).
- **스펙(동결)**: run 내 `large_final`의 4개 팩터 백분위 랭크 **동일가중 평균**:
  ep=1/PER(PER>0), bp=1/PBR(PBR>0), rim_spread, div_yield. 결측 팩터 제외, **최소 2개** 필요.
  가중치 탐색 없음(매직넘버 금지 — 동일가중 고정). 변경하려면 새 id(ls_t2…)로.
- **동결 방식**: 별도 스코어 테이블 없음 — `large_final`이 run별 동결 적재이므로 위 스펙으로
  언제 재계산해도 동일(PIT 재현 가능). 계산 구현은 leaderboard.py(large 블록)와
  build_large_test.py 두 곳 — 스펙 원문은 이 문서가 단일 출처.
- **정직성 고지**: 팩터 선택은 2026-08-06 팩터 zoo 스캔(사후 관측)에서 나옴 → **post-hoc**.
  등록 전 백필 구간의 in-sample IC(참고: h20 ≈ +0.5 수준, 하락장 단일 국면)는 **증거가 아니라 가설**.
  판정은 등록일 이후 OOS만.
- **판정(사전 고정)**:
  - 정본 호라이즌 **h=60·120거래일**(LARGE_SCORE_DESIGN §9), 주간 리밸런스 기준. 표본 작아 CI 넓음 → 보수적.
  - leaderboard.py의 h20 수치·라벨은 **참고 관측**(판정 아님) — why에 명시됨.
  - 채택/가중 부여는 §9 검증 통과 후에만. 통과 못 하면 폐기 — 정상 결과.
- **표시**: docs/_large_test.html (메인/필터 미링크 비공개 경로, 텔레그램만 링크,
  '테스트·검증 전·매수신호 아님' 배너 고정).
