# PROJECT_KNOWLEDGE — dh-q7m3k (한국주식 과매도 스크리너)

> **용도**: 새 Claude Project의 "프로젝트 지식(파일)"으로 올려, 매번 맥락을 다시 설명하지 않고
> 작업을 이어가기 위한 핸드오프 문서. 코드 작업 시엔 **이 문서 + 최신 repo zip**을 함께 올린다
> (문서=무엇을/왜, zip=실제 현재 코드). 행동 규칙은 별도 "프로젝트 지침"에 둔다(PROJECT_INSTRUCTIONS).
>
> **최종 갱신 기준**: run_id 20260606. 반영 = 3.0(v3 표시), 3.1(챔피언/챌린저 섀도우), D2-②(DART
> 재무 캐시), catalyst_observe(인덱스+증분), A(촉매 수집 자동화 — 현재 **자사주 소각만**), B(섹터
> 전량 분류), 내부자(elestock) 비활성 결정, **§11 실험 판정 기준(40거래일) 사전등록**.
>
> **추가 갱신(2026-06-22)**: F1 챌린저 v31f(macd 이격도), F2 챌린저 v31g(거래량팽창), 관측팩터 2종
> (vol_1w_vs_1m_ratio·realized_vol), 파이프라인 2.72단계(observe_vol), 성숙도 팩터 보류, compare_models
> `--since`+h=20d. 골대는 별도 **PREREGISTER_v31f_maturity.md**. 점수 변경분 전부 챔피언 0-diff 검증.
>
> **추가 갱신(2026-06-23, 외부 코드·퀀트 감사 대응)**: 견고성 5종(점수·유니버스 불변=정상일 0-diff, 또는 가산). ① **git push allowlist**(`git add -- docs`). ④ **OOS 점수 동결저장**(`freeze_scores.py`+테이블 `v3_scores`, append-only·spec_hash; compare_models 동결값 읽기→드리프트 면역). **stage2 DART UNKNOWN**(에러/예외를 '위험없음'과 구분→추천 배제, fail-open 차단). **완전성 게이트**(행수 비정상↓이면 공개배포 보류). **종료코드 전파**(치명 단계/스테이지 실패 시 배포 보류). 상세 **§12**.
>
> **추가 갱신(2026-06-23 저녁, §12-2 후속 핫픽스 + 첫 §12 가동)**: §12 첫 프로덕션 런에서 §12-2 의
> `fetch_disclosures` **2-튜플 반환**(`list`→`(list, fetch_status)`) 변경이 두 곳의 미갱신을 드러냄 → 둘 다 수정.
> ① **accumulate 스키마 자가치유**: `stage2_filtered`·`stage3_final` 에 `dart_status` 컬럼이 없어 append
> 크래시 → `write_to_sqlite` 가 "CSV엔 있고 테이블엔 없는 컬럼"을 자동 `ADD COLUMN`(TEXT). **0-diff 검증**
> (수동 ALTER와 4테이블 바이트 동일·기존 run 불변), **수동 DB 작업 불필요**. ② **catalyst 튜플 언팩**:
> `catalyst_insider`·`catalyst_large` 가 튜플을 리스트로 받아 전 종목 실패(`'list'…get'`) → `discs,_=fetch_disclosures(...)`
> (자사주 소각=관측·비차단, **라이브 DART 미검증**). 부수: 게이트(§12-3)가 이 크래시의 부분적재(kosdaq stage1=0)를
> 잡아 push 보류 = 설계대로. 상세 **§12-6**.
>
> **추가 갱신(2026-06-25, 저변동 트랙 신설)**: v3·large 와 분리된 **제3 트랙 lowvol** 1단계 배포.
> 18일 진단서 발견한 신호(저변동·단기반전·ROE)를 순위합 5모델(lv_a~d·lv_a3)로 **관측 적재**(테이블
> `lowvol_scores`, 가중치 0). 견고성 1위 **lv_a** 만 v31g처럼 **테스트 텔레그램+전용 HTML(`docs/lowvol.html`)**
> 노출, 나머지 shadow. 전부 검증 전 섀도우(post-hoc→forward-only, 판정 OOS 40거래일). 파이프라인
> 2.86(적재)·2.87(CSV)·4b(텔레) 추가, **전부 비치명**. v3·large 산출물 **0-diff 불변** 검증. lv점수는
> "오른다"가 아니라 "시장 방향대로 증폭"(상승장 더 오름·하락장 덜 빠짐). 상세 **§13** + LOWVOL_TRACK_DESIGN.md.
>
> **추가 갱신(2026-06-25, 탐색/배포 분리)**: 도전적 가설 탐색이 검증 게이트에 막혀 죽지 않도록
> **research(게이트 면제·`research/` 산출물 한정·docs/·텔레·점수테이블 유출 금지) vs production(관측→40거래일
> OOS→CI/Bonferroni→0-diff) 분리** 원칙 명문화(§14). 탐색 백로그(우선순위): ①거래비용·세금 현실점검
> ②알파/베타 분해 ③베타 오버레이 ④트랙 앙상블 ⑤호라이즌 분리. **핵심 인식 = 병목은 종목선택이 아니라
> 시장 베타.** 점수·코드 0 건드림(지식 문서 추가, 0-diff).
>
> **추가 갱신(2026-06-27, 상승포착 관측 모델 + 정기 점검 도구 + 베타 병목 실증)**:
> ⓐ **mom_a 추가**: lowvol_score.py 에 상승포착(momentum) 대조 관측 모델 1종 추가(테이블 `lowvol_scores`
> 공유, 가중치 0). vs_SMA20·1개월모멘텀·거래량팽창 순위합. lowvol과 정체성 반대라 `mom_` 접두사로 구분.
> `--full` 적재 검증: mom_a 11,496행/27run, **기존 lv 5모델 점수 최대차 0.00(0-diff)·spec_hash 불변**.
> ⓑ **checkup.py 신설**(research 모드, 읽기전용): 주 1회 정기 점검 패널. 세 감시축(①IC 안정성 ②베타 ③레짐
> 신호)+§11 자동 판정(노이즈/기움/유의/측정불가)+직전 JSON 대비 **기준변화 자동 감지**(부호·방향 반전).
> 산출은 `research/checkup_*.json` 만(docs/·점수 0-diff). 인코딩 UTF-8 고정(Windows cp949 한글깨짐 수정).
> ⓒ **핵심 실증 = 병목은 시장 베타**(§14 백로그 결론 일부 선확정): lv/v3 바스켓 **베타 1.0~1.19·시장상관
> 0.99**. regime 필터로 하락회피 시도 → **역효과(regime↔forward 상관 −0.84, 약세진입이 오히려 +)**.
> 트랙 분산도 lv↔v3 상관 0.996이라 **무효**. → cross-sectional(종목·트랙·레짐) 안에서 '절대 마이너스
> 줄이기' 불가, 비대칭은 롱 바깥(시장중립·옵션)에서만. 단 측정구간 하락편중·OOS<40 = 가설. 상세 **§15·§16·§17**.
> ⓓ **운영 메모**: 주말 새벽 실행 시 run_id가 토요일로 찍혀도 정상(내용=직전 거래일 종가, 판정=run 순번
> 기반이라 무영향). 마이그레이션 금지(append-only 동결 이력과 어긋남). 상세 **§17-1**.
>
> **추가 갱신(2026-06-27 저녁, KIS 수급 대폭 확장 + 공매도 챌린저)**:
> ⓐ **KIS 수급 6종 세부 + 공매도/신용/대차 적재**(`kis_flows.py` 확장): 기존 외인/기관/개인에 더해
> 기관을 6종(**연기금·투신·증권·사모·보험·은행**)으로 분해(`daily_flows`, API `investor-trade-by-stock-daily`).
> 별도로 **공매도·신용(융자)·대차** 적재(`short_flows` 테이블, 공매도=기본/신용·대차=`--with-credit --with-loan` 옵션).
> `--verify N` 모드로 신규 API 외인/기관 값이 기존과 **100% 일치(0-diff)** 확인 후 교체. 상세 **§18**.
> ⓑ **combined 유니버스**(large 500 + lv_a 중소형 → 706종목, 중복제거): 기존 large 500만 받던 걸
> lv_a 종목도 포함하도록 확장(`--universe combined` 기본). lv_a 수급 커버리지 45%→**100%**(최신 run). 연기금+공매도 ~13분.
> ⓒ **lv_short 챌린저 추가**(`lowvol_score.py`): lv_a + **공매도비중(낮을수록 좋음)** 4팩터(가중치 0 관측).
> 오프라인 페어비교서 **ΔIC +0.034**(CI[+0.014,+0.056], 코스닥 위주)로 공매도가 lv_a에 더해지는 신호 발견 — 단
> **run 10개 가설**. `docs/lowvol.html`에 lv_a와 **나란히 표시**(텔레그램은 lv_a 유지, 교체 안 함). 0-diff 검증. 상세 **§19**.
> ⓓ **수급/공매도 분석 결론(전부 가설, OOS<40)**: 연기금·신용·대차는 lv_a에 약함(연기금=대형주 현상,
> 대차=공매도에 흡수, 신용=비단조). **공매도만 유일하게 더해짐**(코스닥). 네 "공매도 감소=숏커버 반등" 가설은
> 약함(핵심 "높음+감소"가 데이터에 안 나타남). 절대수익 형태: "외인·기관 5d 동반매수"가 덜 빠짐(단 5d>20d, 차이작음,
> 시장베타 지배). 상세 **§19**.
>
> **추가 갱신(2026-06-27 야간, 베타 심화 + hv_a 고변동 챌린저)**:
> ⓐ **"지속상위인데 절대수익 낮다=베타" 종목 단위 확증**: lv_a 지속 상위10% 종목 절대 −5.15% = 시장 −5.41%(베타)
> + 시장초과 +0.25%(알파). **마이너스의 96%가 베타**, 알파는 양수(종목선택 옳음). β=0.85. 찐상위일수록 알파↑.
> ⓑ **"오른 종목 특성=lv_a인데 왜 절대수익 낮나" 해소**: lv_a는 "오를 종목"이 아니라 **"덜 빠질 종목"**을 고름.
> lv_a 상위10%와 실제 절대수익 상위10% 겹침 **12%뿐**. 저변동=큰하락 회피+큰상승 포기(안정·밋밋). 방향은
> 같아도 종목은 다름. lv_a 5분위 단조(점수↑→덜빠짐, 종목선택 작동). 상세 **§20-1·20-2**.
> ⓒ **ROE 가중 강화 기각**: ROE×1(원본) IC 0.217 → ×4 0.163 단조 하락. 페어 ΔIC −0.022★. lv_a 내 ROE(+0.110)는
> 저변동(+0.199)보다 약해 가중 주면 손해. **원본 1:1:1 최적 재확인. ROE 포함 정당·강화 부적절.** 상세 **§20-3**.
> ⓓ **hv_a 고변동 대박 챌린저 추가**: lv_a 저변동을 **고변동으로 뒤집은 대척점**(highvol+ROE+반전, 가중치 0).
> 상승 run(4): hv_a +4.10%(오른비율 61%) vs lv_a +3.95%(80%) — 대박↑·명중↓. 하락 run: hv_a −11.92% vs
> lv_a −8.00% — 더 깨짐. 고변동 σ 1.8배(예측 어려움). **상승장 표본 부족=가설.** 0-diff. 순수 관측(텔레·HTML
> 미노출). Bonferroni 분모 8. lv_a(방어)·mom_a(주도주)·hv_a(대박) 3관점 관측 체계. 상세 **§20-4·20-5**.
>
> **추가 갱신(2026-06-27 야간②, sm_a 초소형 트랙 + B설계)**:
> ⓐ **"lv_a 우물 밖" 전수 탐색**: stage3 130컬럼 forward IC + lv_a 페어 ΔIC 스캔 → 공매도만 유일하게
> 더해짐(나머지 중복/무효). **구조적 발견**: stage3는 과매도 종목만 저장 → 모멘텀/가치 전략은 데이터 확장 필요.
> ⓑ **유동성 프리미엄 발견**: 거래대금 작을수록 단조롭게 시장초과↑(h20 초소형 +6.41% vs 대형 −6.10%).
> ⓒ **sm_a 초소형 트랙 신설**(가중치 0): lv_a 점수식 동일, 유니버스만 거래대금 1~5억. uni 4번째 원소=유동성
> 상한(기존 모델 0-diff 호환). **베타 0.68**(lv_a 1.01보다 낮음=시장 덜 동조, "베타 병목" 우회 단서). 관찰·소액 전용.
> Bonferroni 분모 9. 관측 체계 5개(lv_a·lv_short·hv_a·sm_a·mom_a). 상세 **§20-6**.
> ⓓ **B 방향(모멘텀/주도주) 설계 청사진**: 핵심=`daily_ohlcv`(과매도 무관 전체 일별 OHLCV) 토대 구축 →
> 모멘텀·매집(OBV)·장기백테스트의 공통 raw 기반(확장성). 3계층(수집/팩터/표시), 4단계 로드맵. 상세 **§21**.
>
> **추가 갱신(2026-06-27 야간③, B 1단계 완료)**: §21 청사진의 **1단계(데이터 토대) = 실물 완료.**
> 소스 **FDR 확정**(수정주가·시총·거래대금 완비, 실측). **`universe_ohlcv.py`** 신설. **`ohlcv.db`는 레포 바깥**
> (`../dh-q7m3k-data/`)에 격리 — 270MB라 핸드오프 불가 + 재생성 가능. **history.db 는 레포 안 그대로**(핸드오프 대상).
> **3년 백필 완료: 2645종목·184.7만행·731거래일·270MB·10분.** 무결성 검증 통과(NULL/음수/모순 0), 활발거래 96%.
> 생존편향·거래정지·시총 대책 다 구현. `.gitignore`·`.bat`(매영업일 증분) 반영. **2단계(모멘텀 IC)는 forward
> 1~2개월 누적 후** — 과거 백필은 룩백용, 정직한 검증은 지금부터 쌓이는 데이터로. 상세 **§21-7**.

---

## 0. 한 줄 요약
KOSPI/KOSDAQ에서 **과매도+턴어라운드 후보**를 매일 자동 발굴해 점수화·등급화하고, GitHub
Pages 대시보드 + 텔레그램으로 보는 개인 파이프라인. 점수는 **v2.6(원본)** 위에 **v3(등급/버킷)**
를 얹은 2층 구조이고, 그 위에 **챔피언/챌린저 섀도우 실험틀**과 **가중치 0 관측 팩터**(나중에
데이터로 승격 판단)가 붙어 있다.

---

## 1. 환경 / 실행

- **repo**: `dh-q7m3k` · GitHub `github.com/sj951027/dh-q7m3k` · Pages `https://sj951027.github.io/dh-q7m3k/` (필터 `docs/filter.html`)
- **실행 주체**: 사용자 Windows PC. 매일 `run_all_and_diversify.bat`(= `python run_and_diversify.py`).
- **언어**: 모든 대화/주석/로그 한국어.
- **run_id 규칙**: 날짜 `YYYYMMDD`(예: 20260606). 누적 테이블·아카이브 폴더 키.
- **텔레그램**: 그룹 "screener", chat_id `-1004202649026`, bot `@sjk_screener_bot`.
- **소요(20260606 실측)**: 스크리너 ~14분 + v3 7초 + 섀도우 1초 + diversify 3초 + 촉매(2.68)
  ~5분(내부자 포함 시) + catalyst_observe 1초 + IC 2.4분 + 대시보드/푸시/텔레그램.
  → **자사주 전용이면 2.68이 ~절반(~2.5분)**.

### Claude(나)의 협업 제약 — 중요
- **나는 네트워크가 없다.** DART/KRX/네이버 호출 스크립트(stage1~3, fetch_valuation, v3_daily,
  compute_ic, catalyst_insider)는 실행 못 한다. 결과가 필요하면 사용자에게 '실행 + 로그·zip'을 요청한다.
- **오프라인 검증 가능**: history.db만 있으면 되는 것 — build_dashboard, diversify(캐시 히트 시),
  v3_backtest, v3_rescore(재계산 동일성), compare_models, catalyst_observe, 점수/IC 로직 단위검증.
- **zip 주의**: 이 repo zip은 **zip64**라 리눅스 `unzip`이 실패한다. **항상 Python `zipfile`로 추출**
  (`.git/`까지 풀면 `git ls-files`로 추적상태 확인 가능). history.db ≈ 46MB.

---

## 2. 파이프라인 (단계 순서 = run_and_diversify.py)

1. **1단계** `run_all_v2_6.py` — 스크리너 3스테이지(코스피+코스닥) → 누적(accumulate_history) → 1차 대시보드.
   - stage1 `screener_fdr_v2_6`: 과매도+레짐+환율+외인+수급(네이버) → `stage1_oversold`
   - stage2 `stage2_risk_filter_v2_6`: DART 공시 위험필터(가짜악재 방어) → `stage2_filtered` + `v2_{mkt}_filtered_safe_*.csv`
   - stage3 `stage3_fundamental_momentum_v2_6`: 분기 YoY + OCF + v2.6 `final_score` → `stage3_final` (**stage2 SAFE 생존분만**)
2. **2.6단계** `v3_daily.py` → `fetch_valuation.py` → `v3_rescore.py`(final_score_v3/grade/bucket) → `v3_merge.py`
3. **2.65단계** `shadow_run.py` — 챌린저(v31a~d·**f·g**) 섀도우 누적(조용, ~0 네트워크). MODELS 자동 순회라 새 챌린저 자동 포함.
3b. **2.66단계** `freeze_scores.py --latest` — 방금 만든 archive(챔피언+챌린저)의 **그날 얼린 점수를 DB `v3_scores`에 append-only 적재**(드리프트 면역 OOS). 점수·로직 불변(가산). *(2026-06-23)*
4. **2단계** `diversify_picks.py` — 섹터 쏠림 방지 추천(업종당 최대 N, top개)
5. **2.68단계** `catalyst_insider.py` — **촉매 수집(현재 자사주 소각만)** → `catalyst_{mkt}_{run_id}.csv` *(A에서 추가)*
6. **2.7단계** `catalyst_observe.py` — 관측 팩터를 stage3_final에 배선(가중치 0, 점수 불변)
6b. **2.72단계** `observe_vol.py` — `realized_vol` 관측 컬럼 채움(가중치 0, 점수 불변, 증분). *(2026-06-22 추가)*
7. **2.5단계** `compute_ic.py` — 점수 적중도(IC) 계산
8. **2.8단계** `build_dashboard.py` — v3 반영 대시보드 재생성
9. **3단계** git push  →  **4단계** `notify_telegram.py`. **단, push 직전 배포 게이트**(§12): 완전성(행수)+치명단계 rc 점검 → degraded/실패면 push·평소텔레 **보류** + '보류' 알림(DB 기록은 남김). *(2026-06-23)*

### 누적 DB (history.db, SQLite)
- 테이블: `stage1_oversold`, `stage2_filtered`, `stage3_final`, `runs`. 아카이브 CSV는 `archive/날짜/`.
- **인덱스**: `idx_stage3_mrt ON stage3_final(market, run_id, ticker)` — catalyst_observe가 1회 생성. UPDATE 풀스캔 방지. **observe_vol도 이 인덱스를 쓴다**(키 (market,run_id,ticker) — market 빠지면 풀스캔이라 주의).

---

## 3. 점수 체계

### v2.6 `final_score` (원본 엔진 — 절대 삭제 금지)
과매도+매집+추세+수급+펀더+모멘텀+OCF+레짐. **v3가 이것을 입력으로 계산**하므로 지우면 안 됨.

### v3 (현재 사용자 노출 기준)
`final_score_v3` + `grade`(A+/A/B/C/WATCH/EXCLUDE) + `bucket`(BUY/WAIT/OBSERVE/WATCH/EXCLUDE).
- **사용자 표시는 모두 v3**(텔레그램/대시보드/필터). v2.6 final_score는 보존(필터 '옛최종' 오염 방지)하되 표시는 v3.
- **regime_score는 (run,market)마다 상수** → 같은 날 종목 간 랭킹 영향 없음 → v3는 제외.
- 텔레그램 정렬 = 의도된 2키(bucket BUY→WAIT, 그다음 final_score_v3 내림차순).

### 관측 팩터 (가중치 0 — 점수식에 안 들어감)
`catalyst_observe.py`가 stage3_final에 채움. **final_score_v3 계산에 절대 미사용**(v3_rescore/v3_merge
참조 0회, 검증됨). 대시보드/텔레그램 미표시 → 사용자 오해 없음. IC가 좋으면 **v32 승격** 후보.
- `smartmoney_score`(0~15): 과매도+거래대금폭발+양봉+수급. stage3 컬럼만으로 계산 → 전 run 채움.
- `roe_value`(%)=EPS/BPS×100(자본잠식 BPS≤0=NaN). `roe_gate`=PBR<1 & ROE≥8%. valuation_*.csv 있는 run만.
- `insider_score`/`insider_source`: **현재 비활성(OFF)** — §4-A 참고.
- `buyback_cancel_flag`(0/1): 자사주 **소각** 공시. catalyst_*.csv 있는 run만.
- `vol_1w_vs_1m_ratio`(거래량 팽창 = 5일/21일 평균거래량): **기존 stage3 컬럼**, FACTOR_COLUMNS 등록만(채움 불필요). 가설: 팽창→+IC(거래량 동반 반등). **발견=낚시(post-hoc)** → PREREGISTER E-1. *(2026-06-22)*
- `realized_vol`(trailing 21 활성런 종가수익률 std): `observe_vol.py`(2.72)가 채움. low-vol 가설 검정(부호는 데이터에 맡김). 커버리지 ~43%(가격이 과매도 패널에만 → 최근 윈도우 머문 종목; 신규 pending). *(2026-06-22)*
- validate_scores의 `FACTOR_COLUMNS`에 위 6개 등록 → IC 하니스 자동 측정.

### IC 현실 인식 (기대치 조정)
- v2.6 final_score 단기 IC는 음(–)에 가까웠고, v3는 ≈0~약간+. v3는 "방향이 거꾸로였던 것"을
  바로잡았지만 **절대 엣지는 ≈0**(나쁜 종목 걸러내기엔 좋고, 버킷 내 미세 랭킹 예측력은 약함).
- 13일치로는 **표본 부족** → IC 카드 "데이터 쌓는 중". **판정은 §11(40거래일) 기준.**

---

## 4. 주요 모듈 & 의사결정

### 4-A. `catalyst_insider.py` — **현재 '자사주 소각'만 (내부자 매수 비활성)**
- 산출 `catalyst_{mkt}_{run_id}.csv`. catalyst_observe(2.7)가 읽어 insider/buyback 컬럼 채움(병합 로직 존재).
- run_id 기본값 = **최신 stage3 run**(누적/자정경계 정합). 종목당 DART: 자사주만 1회(공시목록),
  `--with-insider` 켜면 2회(+elestock). 병렬 2스레드, sleep 0.05.

**왜 내부자 매수를 끄는가 (institutional knowledge — 되살리지 말 것):**
- elestock(임원·주요주주 소유보고) 요약은 **소유수 '증감'만** 줄 뿐 *사유*를 구분 못 한다:
  장내매수(호재) / **무상증자·주식배당(중립)** / 상속·증여 / 스톡옵션행사 / 분할이 전부 "증가"로 찍힘.
- 무상증자는 최대주주 포함 전원의 보유수를 늘려 "내부자 매집"으로 **오판**된다(흔해서 오염 큼).
- 비율(%)로 거르면 무상증자는 빠지지만, **대형주의 진짜 장내매수는 반올림 0.00**으로 같이 사라진다.
- → elestock 요약만으로는 호재성 매수를 깨끗이 정제 불가. **사용자가 이전에 같은 이유로 뺐던 판단이
  옳다.** 그래서 기본 비활성, 해석 여지 없는 '자사주 소각'만 사용.
- (과거 "전 종목 insider 0"의 직접 원인은 **별개의 날짜 파싱 버그**: elestock `rcept_dt`가
  `YYYY-MM-DD`(하이픈)인데 코드가 `YYYYMMDD`로 가정해 `[:8]` → 파싱 실패로 전부 스킵. **수정 완료**,
  `--with-insider` 경로 정상. 단 위 정제불가 한계로 **기본은 여전히 OFF**.)
- 자사주 소각 탐지는 검증됨(20260606 실측 100건). `buyback_cancel_flag`는 유지 가치 큼.

### 4-B. `catalyst_observe.py` — 인덱스 + 증분 (성능)
- 과거 8분55초 원인: stage3_final 인덱스 없음 → UPDATE 풀스캔 + 매번 전 run 재백필(O(n²)).
- 수정(결과 불변, 검증): ① `idx_stage3_mrt` 인덱스(1회) ② **이미 채워진 run은 건너뛰기**. 매일 새 run
  1개만 처리 → **~0.7~1초**.
- 채움 판정: smartmoney(전부 채워짐=완료), roe(roe_gate 1개라도 있으면 완료), catalyst(insider_source
  1개라도 non-null이면 완료). **함정**: insider_source가 "NONE"/"OFF"여도 non-null → "채워짐"으로 봄.
  과거 run의 catalyst를 **덮어쓰려면 `python catalyst_observe.py --full`** 필요.

### 4-C. `diversify_picks.py` — 섹터 전량 분류 (B)
- 과거 "미분류 1493개"는 버그 아님 = **의도된 절약**(점수 상위 `max(top*6,60)`개만 FDR/DART 조회).
- B 변경: override/`sector_cache.json`에 **있으면 전 종목 공짜로 채우고**(네트워크 0), 남은 미분류 상위만 네트워크.
- 효과(검증): 미분류 1493→~4, **top-20 추천 불변**, 필터 섹터 토글 ~14%→~100%.
- `sector_cache.json`은 현재 universe 100% 커버. `sector_overrides.csv`(수동)는 없어도 됨.

### 4-D. `dart_cache_util.py` — DART 재무 캐시 (D2-②)
- stage3 `_request_json` 래핑. **키 = url + (crtfc_key 제외) 정렬 params**(연도/보고서코드 포함).
- '000' 길게(`DART_FIN_TTL_DAYS` 기본 14일), '013' 짧게(`DART_NODATA_TTL_HOURS` 기본 12h),
  **에러는 캐시 안 함**. 미스 경로는 캐시 이전과 100% 동일. 실측: 같은 날 재실행 stage3 172s→51s.
- 환경변수: `DART_NO_CACHE=1`(끄기), `DART_CACHE_DIR`. `dart_cache/`는 gitignore.

### 캐시 안전성 요약 (질문 재발 방지)
- **재무 캐시**: 키에 보고서 기간 포함 → **제출 보고서는 불변** → 14일 캐시해도 정확. 미제출은 12h마다
  재확인 → 새 보고서 곧 반영. **안전.**
- **corp_code 매핑 캐시**(`dart_cache/corp_code.csv`): **30일 미만 재사용**(로그 "기업코드 매핑 사용 (N일 전)").
  거의 안 변함 → 6~7일 캐시 **OK**. 유일 영향 = 최근 며칠 신규상장 누락(IPO 직후는 과매도 후보 아님,
  30일 내 자동 갱신). 강제 갱신 = `dart_cache/corp_code.csv` 삭제.
- **sector_cache.json**: TTL 없음(영구), 현재 universe 100% 커버.

### 4-E. `observe_vol.py` — realized_vol 관측 컬럼 (2026-06-22)
- 가격패널(stage1_oversold) trailing 21 **활성런**(v3_backtest와 동일 정의) 종가수익률 std → `realized_vol`. 점수 미사용(가중치 0).
- ALTER 1회 + `(market,run_id,ticker)` 키 UPDATE(idx_stage3_mrt 사용) + `executemany`. idempotent(미채움 run만 증분, `--full` 전체).
- 동결값 WINDOW=21·MIN_OBS=8. PIT 안전(run R 시점까지만). 커버리지 ~43%(과매도 패널 한정 — 신규 편입 pending).
- **초기 버그(수정됨)**: UPDATE WHERE 에 market 누락 → §4-B와 같은 풀스캔으로 멈춤. 3키 WHERE로 인덱스 사용 → ~3초.

---

## 5. 챔피언 / 챌린저 섀도우 실험틀 (3.1)

목적: 점수 모델 변경을 **실거래/실표시 영향 없이** 그림자로 누적해, 충분한 표본 뒤 IC/수익으로 채택 판단.

- **v3_rescore.py**: spec 주도. `SPEC_V30`=현재 챔피언 상수. `MODELS` 레지스트리. `rescore(df, run_id,
  market, spec=None, ...)` — spec=None이면 SPEC_V30 → v3_daily/v3_merge/v3_backtest는 **현 챔피언과 동일**.
- **챌린저(각각 v30 + 변수 1개)**:
  - `v31a` E2 반전확인 진입게이트(BUY는 실제 반전 플래그 충족 시만)
  - `v31b` E3 수급가중↑(w.supply 1.0→1.6)
  - `v31c` E4 유동성하한(LIQ_FLOOR=5.0억)
  - `v31d` E5 섹터중립화(섹터 내 demean) — **B 덕에 섹터 전량 채워져 공정 평가됨**
  - `v31f` **F1 macd 이격도 가산**(vs_SMA20−vs_SMA50 셀내 z(±3)×`MACD_TILT_W=6.0`) — 발견=영상룰 번역, post-hoc *(2026-06-22)*
  - `v31g` **F2 거래량팽창 가산**(vol_1w_vs_1m_ratio 셀내 z(±3)×`VOLEXP_TILT_W=7.0`) — 발견=관측팩터 낚시, post-hoc *(2026-06-22)*
- **shadow_run.py**: v3_daily 직후 최신 run을 챌린저별 재점수 → `{model}_archive/` (v30 제외, ~0 네트워크).
- **compare_models.py**: history.db 전체 재계산, 3개 표 + `docs/model_compare.json`. **`--since YYYYMMDD`**(OOS 판정용)·**h=20d 컬럼** 지원(2026-06-22). **(2026-06-23) 점수 동결값(`v3_scores`) 우선 읽기**(없으면 재계산 폴백·`--recompute` 강제·출처 표기) → 재계산이 입력 드리프트(value_score=0 등)에 오염되던 문제 해소.
  1. 전 종목 IC(final_score_v3) — E3/E5 / 2. BUY-only 시장초과수익+n — E2 / 3. BUY+WAIT 수익+n — E4
- **검증 사실**: 리팩터 v30 == 원본 **0 diff**(전 run). v31f·v31g 추가도 v30·v31a~d **0 diff**(32,373행). 단기치는 **순수 노이즈** → **§11(40거래일)으로 판정**. 챌린저 **6개**(v31a~d·f·g) → 다중검정 Bonferroni 분모 6. **v31f/g·거래량팽창은 post-hoc라 forward-only·소급 금지**(PREREGISTER 참조).

---

## 6. 불변 규칙 (작업 시 반드시 지킬 것)

1. **v2.6 엔진 절대 삭제 금지**(v3가 그 위에서 계산).
2. **챌린저 spec은 시작하면 동결**(과거 가중치 변경 = look-ahead → 새 모델 id로).
3. **챌린저 1개 = 변수 1개만**.
4. **캐시 미스 경로는 캐시 이전과 100% 동일**(가속만, 에러 캐시 금지).
5. **사용자 표시는 v3 유지**.
6. **관측 팩터는 점수식에 절대 투입 금지**(승격은 데이터로만, 새 모델 id).
7. **출력 동일성 우선**: 성능/리팩터 변경은 "결과 0 diff" 오프라인 증명 후 적용.
8. **post-hoc(낚시)로 찾은 팩터/챌린저는 forward-only·소급 금지** — in-sample 수치는 가설이지 OOS 증거 아님(같은 표본 재사용 금지). *(2026-06-22)*
9. **단, 관측 컬럼 백필(--full)은 허용**(점수 불변·판정은 forward만). 이는 챌린저 archive의 '소급 금지'(archive를 OOS로 *세는* 문제)와 차원이 다름. *(2026-06-22)*

---

## 7. .gitignore (비대화 방지)
- 데이터 CSV는 history.db에 값이 들어가므로 ignore: `valuation_*.csv`, `catalyst_*.csv`(추가됨) 등.
- 챌린저 아카이브 `v31a_archive/`~`v31d_archive/` + **`v31f_archive/`·`v31g_archive/`**(2026-06-22)도 ignore. 이미 추적된 건 `git rm -r --cached`로 1회 정리 완료.
  **새 챌린저(v32a 등) 추가 시 같은 식으로 한 줄 추가.**
- **(2026-06-23) git push allowlist**: `run_and_diversify.py` git_push 가 `git add -A` 대신 **`git add -- docs`**(PUSH_ALLOWLIST). docs/만 커밋 → 작업중 코드·잡파일 자동커밋 방지. **코드 변경은 `git add <파일>` 명시 커밋 필요.**
- `dart_cache/`도 ignore.

---

## 8. 향후 작업 (우선순위)

| 우선 | 항목 | 게이트/메모 |
|---|---|---|
| ★ 주력 | **E1 관측 팩터 IC 수확** | **§11 기준(40거래일)** 충족 시, `smartmoney_score`/`roe_value`/`buyback_cancel_flag`/`vol_1w_vs_1m_ratio`/`realized_vol` 중 일관 양(+)인 팩터를 IC비례 가중으로 **v32 승격**. (insider는 OFF라 제외.) FACTOR_COLUMNS 등록 완료. **다음 메인 마일스톤.** |
| ★ | **챌린저 판정(E2~E5·F1·F2)** | 주 1회 `compare_models.py`로 추세만, **§11 기준(40거래일)**에서 채택/폐기. F1/F2(v31f·v31g)는 **post-hoc → `--since` 로 OOS만** 봐야 정직. |
| 중 | E6 단기반등 vs 중기턴어라운드 이중 모델 | 더 큰 작업. E1로 어떤 팩터가 먹히는지 안 뒤에. |
| 중 | **lowvol 가중치 재탐색** | **데이터 더 쌓이면(OOS run 10→20→30+) 다시 돌려보기.** 챌린저로 박을 필요 없음 — 원재료(realized_vol·roe·return_1w·short_vol_ratio)가 stage3·short_flows에 있어 가중치 조합은 history.db만으로 즉석 재계산(§14 탐색=게이트 면제). **20260627 1차 탐색 결과(run 10, §20-3)**: lv_a 1:1:1·lv_short 공매도1 이미 거의 최적, "덜하락(저변동↑)↔절대상승(반전↑)"은 트레이드오프라 동시달성 불가, 개선폭 노이즈 수준(IC ±0.007, CI로 구분 안 됨)이라 **과적합 위험 > 개선**. 돌려볼 것: ① (저변동:ROE:반전) 그리드 — 절대수익·하락장/상승장 분리 평가 ② lv_short 공매도 가중(0.5/1/2). **일관되게 나은 가중 나오면 그때 새 id로 챌린저화→§11 검증→교체**(lv_a 직접 변경 금지, 동결 챔피언). |
| 중 | **lv_short·hv_a 판정 추적** | 주간 `checkup.py`로 OOS 추적(REG_DATE·order 반영 완료). 등록일 20260627 + 40거래일 후 판정. hv_a는 상승장 표본도 필요(상승 run 4개=가설). lv_short는 공매도 ΔIC+0.034(코스닥) 유지 여부, hv_a는 베타 1.24·종목선택력 확인. |
| 중 | E7 내부자/자사주 이벤트 스터디 | 자사주 소각 누적 후 가능. 내부자는 OFF라 데이터 미축적 — 하려면 `--with-insider`로 켜야 하나 §4-A 한계로 비권장. |
| 낮 | sector_overrides.csv 수동 보정 | B로 대부분 해결. 상위 후보 중 미분류 보일 때만. |
| 낮 | D2 동시성 튜닝 | 네트워크라 내가 검증 불가, 캐시로 일상 빨라 우선순위 낮음. feature-flag + 직렬 폴백 + before/after 0 diff. |
| 운영 | 일일 `.bat` / 주간 `compare_models.py` | 챔피언 v30 운영, 챌린저·관측 자동 누적. |

---

## 9. 운영 치트시트

```bash
# 매일 (Windows)
run_all_and_diversify.bat            # = python run_and_diversify.py
# 주 1회 — 챌린저 비교(추세만, 판정은 §11/40거래일)
python compare_models.py
python compare_models.py --since 20260622   # §11 OOS 판정용(등록 이후만; h=20d 포함)
# 실현변동성 관측 컬럼 최초 백필(이후는 .bat 2.72가 증분 자동)
python observe_vol.py --full
# 촉매(자사주 소각만) 단독 재수집
python catalyst_insider.py                 # 기본: 자사주 소각만, 최신 run
python catalyst_insider.py --with-insider  # (비권장) 내부자 평가도 켜기
# 과거 run catalyst 강제 덮어쓰기
python catalyst_observe.py --full
# 캐시 강제 갱신
del dart_cache\corp_code.csv          # 기업코드 매핑 새로 받기
set DART_NO_CACHE=1                    # 재무 캐시 임시 끄기
```

---

## 10. 변경 이력
- **3.0** 사용자 표시 v2.6 → v3 전환 + 버그픽스.
- **3.1** 챔피언/챌린저 섀도우 틀(v3_rescore spec화, shadow_run, compare_models). v30 == 원본 0 diff.
- **D2-②** DART 재무 캐시 — stage3 가속, 결과 불변.
- **catalyst_observe** 인덱스 + 증분 — 8분55초 → ~1초, 0 diff.
- **A** 촉매 수집 자동화(2.68). 현재 **자사주 소각만**(§4-A 근거). run_id=최신 stage3.
- **B** diversify 섹터 전량 분류 — 미분류 1493→~4, top-20 불변.
- **버그픽스** elestock 날짜 파싱(하이픈) — `--with-insider` 정상화(기본 OFF 유지).
- **§11 사전등록** 실험 판정 기준 = **40거래일** 고정.
- **F1/v31f**(2026-06-22) macd 이격도 챌린저(섀도우, 0-diff). **F2/v31g** 거래량팽창 챌린저(섀도우, 0-diff).
- **관측팩터 2종**(2026-06-22) vol_1w_vs_1m_ratio·realized_vol + `observe_vol.py`/2.72단계. (observe_vol 초기 풀스캔 버그 → 3키 WHERE로 수정.)
- **성숙도(maturity) 보류**(2026-06-22): maturity proxy vs_SMA200 IC −0.06(평균회귀 레짐 부호 반대) → 추세장 전환 시 재검토(새 id·새 사전등록).
- **compare_models `--since`+h=20d**(2026-06-22). **PREREGISTER_v31f_maturity.md** 신설(골대 고정).
- **견고성 5종 (2026-06-23, 외부 감사 대응)** — §12:
  - **#1 git push allowlist**(`git add -- docs`).
  - **#4 OOS 동결저장**: `freeze_scores.py` + 테이블 `v3_scores`(append-only·spec_hash) + 파이프라인 2.66 + compare_models 동결읽기. 백필 완료(7모델 86,457행).
  - **stage2 DART UNKNOWN**: dart_status(SAFE/RISK/UNKNOWN)·UNKNOWN 추천배제·커버리지 게이트. fail-open 2종(에러응답·검사예외) 차단. 정상일 0-diff.
  - **완전성 게이트**: stage1/stage3 행수 < 최근 중앙값×0.5면 공개배포 보류(6/8형 차단). 정상일 무영향.
  - **종료코드 전파**: run_all `sys.exit(0 if all_success else 1)` + 치명 단계(v3_daily·diversify·build_dashboard) rc→배포보류. 비치명은 로그+계속.
- **§12-2 후속 핫픽스 (2026-06-23 저녁, 첫 §12 가동일 발견·수정)** — §12-6:
  - **accumulate 스키마 자가치유**: `write_to_sqlite` 가 CSV 신규 컬럼을 자동 `ADD COLUMN`(TEXT) → `dart_status` 누락 append 크래시 해소. **0-diff**(수동 ALTER와 stage1/2/3/runs 4테이블 바이트 동일·기존 run 불변), 수동 DB 작업 불필요·동종 컬럼 추가 영구 흡수.
  - **catalyst 2-튜플 언팩**: `catalyst_insider`·`catalyst_large` 가 `discs,_=fetch_disclosures(...)` 로 언팩 → `'list'…get'` 전 종목 실패 해소(관측·비차단, 라이브 DART 미검증).
  - **첫 §12 가동 검증**: 완전성 게이트가 부분적재(kosdaq stage1=0) 공개배포 정상 차단 → §12-3·§12-4 라이브 1회 확인분 충족.
- **KIS 수급 확장**(2026-06-27 저녁) — §18: 기관 6종 세부(연기금·투신·증권·사모·보험·은행) + 공매도/신용/대차(`short_flows`). `--verify` 0-diff 확인 후 API 교체. 신용 `FID_INPUT_DATE_1`·대차 `CTS`/`MRKT_DIV_CLS_CODE=3` 필수파라미터 디버깅. combined 유니버스(large+lv_a 706종목)로 lv_a 수급 커버리지 100%.
- **lv_short 공매도 챌린저**(2026-06-27 저녁) — §19: lv_a+공매도비중(가중치 0 관측). 오프라인 ΔIC +0.034(코스닥, run 10 가설). lowvol.html 나란히 표시(텔레그램 lv_a 유지). 기존 lv 모델 0-diff. Bonferroni 분모 7.
- **수급/공매도 분석 결론**(2026-06-27 저녁): 연기금=대형주 현상(lv_a 약함), 신용·대차 부적합, 공매도만 lv_a에 더해짐(코스닥). "공매도 감소=숏커버" 가설 약함. 절대수익은 시장베타 지배(§17 재확인). 전부 OOS<40 가설.
- **베타 심화 + hv_a 챌린저**(2026-06-27 야간) — §20: lv_a 지속상위 절대마이너스 96%가 베타(β=0.85, 알파+0.25%=종목선택 옳음). "오른종목=lv_a인데 절대낮음"은 lv_a가 '덜빠질 종목'을 고르기 때문(겹침 12%). ROE 가중강화 기각(ΔIC −0.022★, 원본 1:1:1 최적). hv_a 고변동 챌린저 추가(lv_a 대척점, 가중치 0, 상승장 가설, 순수관측). Bonferroni 분모 8.
- **sm_a 초소형 트랙**(2026-06-27 야간②) — §20-6: lv_a 점수식·유니버스만 거래대금 1~5억. Bonferroni 분모 9.
- **wu 트랙 신설**(2026-07-03) — §23: 전체 상장 관측 모델 2종(wu_a/wu_b, 등록 20260702, 가중치 0). 새 테이블 wu_scores + 2.88/2.89 단계 + docs/wu.html + 텔레그램 푸터 링크(테스트 라벨). v3·large·lowvol 0-diff 검증. 골대 PREREGISTER_wu.md(분모 11 — sm_a 누락 정정 포함). Bonferroni 분모 11.
- **이중실행 사고 규명·재발방지 4종**(2026-07-03~04) — §24: 7/3 새벽+저녁 이중실행 → stage 덮어쓰기 vs append-only 스킵 불일치(lv 60종·v30 frozen_at 2종 혼재). 조치: ①run_id 거래일 기준(accumulate) ②중복실행 가드(FORCE_REINGEST) ③lowvol --run idempotent ④compute_ic 이중실행 게이트(20260703 판정 제외). lv_a@0703 재적재로 stage3 정합(밖 0). 표시 폴백 4겹(build_lowvol/wu_filter)은 안전망로 잔존.

---

## 11. 실험 판정 기준 (사전등록 — 골대 고정용)

> 나중에 결과 보고 기준을 바꾸지 않으려 **미리** 못 박는다. **40거래일은 '가장 이른 판정 시점'**이지
> 유의성 보장이 아니다 — 경계값은 '채택'이 아니라 '기움(lean)'으로만 본다.

- **판정 시점**: 챔피언 도입(20260606) 이후 **OOS 40거래일**(약 2개월) 누적 후 1차 판정. 그 전엔 노이즈로 간주.
- **주력 지표**: 전 종목 Spearman IC. **사전 고정 호라이즌 = h=20d**(보조로 h=5d 같이 본다). 데이터가
  가장 많아 검정력이 있음.
  - 보조(부호 일치 확인용): BUY+WAIT 평균 시장초과수익(h=20d).
- **채택 조건(챌린저별, 모두 충족)**:
  1. 40거래일 OOS에서 `평균(IC_챌린저 − IC_챔피언)`의 **부트스트랩 95% CI(날짜 리샘플)가 전부 0 초과**(h=20d).
  2. 주별 방향 일관성 **≥ 60%**.
  3. 보조 지표(BUY+WAIT 수익) 부호가 주력과 **상충하지 않음**.
- **다중검정(챌린저 6개: v31a~d·f·g)**: 위 CI를 **≈99%(=0.05/6, Bonferroni)**로 올리거나, **별도 보유기간에서 재현**될
  때만 **최종 채택**. 둘 다 못 넘으면 '기움'까지만 표기. *(v31f·v31g 추가로 4→6 갱신, 2026-06-22)*
- **BUY 희소성**: 20260606 BUY=1개 → **E2(v31a)는 BUY-only 수익으로 수개월간 판정 불가**. E2는
  'BUY+WAIT 집합 개선' + 정성검토로만 보고, BUY-only 숫자에 의존하지 않는다.
- **무판정도 결론**: 40거래일(필요시 연장)에 아무도 기준을 못 넘으면 **챔피언 v30 유지** — 정상적이고
  바람직한 결과(과적합 방지).
- **채택 절차**: 기준 충족 시에도 즉시 교체하지 않고, 그 챌린저를 **새 챔피언 후보로 1주 관전** 후 반영.
  반영 시 변경분은 불변 규칙 2(spec 동결)를 따른다.

### 11-A. post-hoc(낚시) 챌린저 정직성 + compare_models 주의 (2026-06-22)
> v31f·v31g·거래량팽창은 **사후 발견**(여러 후보 훑어 고름)이라 일반 챌린저보다 엄격히 다룬다.
> 상세 골대는 **PREREGISTER_v31f_maturity.md**(A·E·F절).

- **forward-only·소급 금지**: 발견 표본(≈~0619까지)의 in-sample IC는 **증거가 아니라 가설**. OOS 판정엔 등록일(≈20260622) 이후만 쓴다.
- ⚠️ **compare_models 해석**: 이 스크립트는 IC를 **전체 history(발견 기간 포함)** 로 계산한다 → 리더보드에서 v31f/v31g가 챔피언보다 높아 보이는 건 **부분적으로 발견표본 때문**(판정 아님). **반드시 `python compare_models.py --since 20260622`** 로 OOS 구간만 보고 판정한다. (실측: --since 로 발견기간 빼면 v31f/g 우위가 사라짐.)
- **h=20d 주력 미성숙**: 현재 h=20d 계산 가능 셀 **0개**. 그 전 모든 숫자(h≤5d)는 보조이고 노이즈로 간주.
- **낚시 출신 추가 기준**: Bonferroni 0.05/6 + **별도 호라이즌(h=10d) 재현**까지 봐야 '유의'.

---

## 12. 견고성 레이어 (2026-06-23 외부 코드·퀀트 감사 대응)

> 외부 감사 지적 중 **타당하고 적용 가능한 것**만 반영. 전부 **점수·유니버스 불변**(정상일 0-diff)이거나 **가산**(새 테이블/컬럼)이라 챔피언 spec 동결을 안 깬다.
> (감사의 "팩터 중복 가중" 지적은 v3엔 오진 — v3 실제 6컴포넌트 상호상관 최대 0.33, reversal↔oversold −0.46으로 직교. 높은 상관은 v3컴포넌트↔그 v2.6 원시입력 간이며 최종점수에 중복 합산 안 함.)

### 12-1. OOS 점수 동결저장 (감사 #5 — 가장 강한 지적)
- **문제**: v3 점수가 archive CSV(gitignore)에만 있고, compare_models 가 과거를 **현재 코드로 재계산** → 입력(valuation 등) 드리프트 시 재계산 ≠ 그날 값 → OOS 판정 오염.
- **해결**: `freeze_scores.py` 가 그날 archive 원본(=실제 표시·판정값)을 DB 테이블 **`v3_scores`**(키 run_id·market·ticker·model_id, **append-only**, spec_hash 동반)에 적재. 재계산이 아니라 **원본 보존 → 드리프트 면역**.
  - 이미 있으면 안 건드림(최초 동결값=진실). spec_hash 불일치 시 경고(동결 스펙 변경 탐지).
  - 플래그: `--backfill`(전체 1회)·`--latest`(매일 2.66)·`--verify`. **백필 완료**: v30 22,133 / v31a~d 각 15,430 / v31f·g 각 1,302 = **86,457행**, 전 모델 spec_hash 1종.
- **compare_models 페이로프**: 점수 **동결값 우선 읽기**(없으면 재계산 폴백·`--recompute` 강제·출처 표기) → §11 판정이 드리프트 면역. (단 §11-A 의 `--since`(발견/OOS 분리)는 여전히 필요 — 동결읽기는 *드리프트*를, --since 는 *발견기간 제외*를 담당.)

### 12-2. stage2 DART UNKNOWN (fail-open 차단 — 안전)
- **문제**: `fetch_disclosures` 가 DART 에러(010/020/800/900)·네트워크 예외 시 **빈 목록 → "위험 없음" → SAFE 통과**. "위험 없음"과 "확인 불가"가 안 구분됨.
- **해결**: fetch 가 `(disclosures, fetch_status)` 반환(000/013=ok, 그 외/예외=unknown). `dart_status`=SAFE/RISK/**UNKNOWN**. **UNKNOWN 은 추천 유니버스(safe_with_caution)에서 배제** — 검사 예외로 merge 에서 빠진(NaN) 종목까지 포함(2종 fail-open 차단). 정상 응답일엔 UNKNOWN 0 → **기존과 0-diff**.
- **커버리지 게이트**: UNKNOWN 비율 > `UNKNOWN_GATE_FRAC`(0.10)면 "DART 불안정 — 재실행 권장" 경고. 개수 매 run 로그 → stage2 실패율 자동 측정(과거는 기록 없어 측정 불가, 프록시 stage3 재무 99.7% ok → 드묾).

### 12-3. 완전성 게이트 (degraded 배포 보류 — 안전)
- **문제**: 6/8 처럼 stage1 이 75/20행(평소 720/1550)인데 **종료코드 0(degraded 성공)** 으로 저장·배포됨. 종료코드만으론 못 잡음.
- **해결**: `run_and_diversify.check_completeness()` 가 push 직전 stage1·stage3 행수를 **시장별 최근 중앙값과 비교**, `floor_frac`(0.5) 미만이면 degraded → push·평소텔레 **보류** + '보류' 알림(침묵 금지), **DB 기록은 남김**(어제 대시보드 유지). 점검 자체 에러는 통과(게이트 버그가 정상 run 안 막게). **정상일 무영향**(검증: 6/8 보류·0622 통과).

### 12-4. 종료코드 전파 (하드 크래시 — 완전성 게이트 보완)
- **문제**: run_all 이 `all_success` 추적만 하고 `sys.exit` 안 함 → 스테이지 하드 실패해도 0 반환 → run_and_diversify 1단계 검사 무력.
- **해결**: run_all `return 0 if all_success else 1` + `sys.exit(main())`(조기 실패도 `return 1`). run_and_diversify 는 **치명 단계**(run_all 하드중단 / v3_daily·diversify·build_dashboard rc→`deploy_ok=False`)만 배포 보류, **비치명**(shadow·freeze·catalyst·observe·compute_ic·v3_backtest·build_v31g)은 **로그+계속**(챌린저 버그로 챔피언 배포 안 멈춤). 배포 게이트 = `치명실패 OR degraded`.
- **남은 1회 확인(사용자)**: 실제 종료코드 전파(실패한 날 push 보류)는 라이브 1회 확인 — 정상일엔 전부 0 → 기존과 동일.

### 12-5. 보류/미적용 (낮은 우선순위)
- **#2 결측→플래그**: valuation 결측 → value_score=0 은 "결측"과 "중립"을 섞음. **챔피언 점수 변경**이라 챌린저/관측으로만. 라이브 결측률 측정 먼저(매일 valuation 받으니 작을 듯) → 십중팔구 `value_source` 플래그 노출로 충분(Bonferroni 낭비 방지). **미적용.**
- **stage3 재무 REQUEST_ERROR 전파**: 영구 에러→`not_found` 로 뭉개짐(약한 fail-open). 단 재시도(1→3→9초)로 완화 + 결과가 *점수 중립*(안전 우회 아님) + 99.7% ok → **급하지 않음**(선택).
- **kis_flows(대형 트랙)**: 에러 미저장·다음날 재시도·토큰실패 raise = **fail-safe 모범**. 손댈 것 없음.

### 12-6. §12-2 후속 핫픽스 (2026-06-23 저녁 — 첫 §12 가동일 발견·수정)
> §12 첫 프로덕션 런에서 §12-2 의 `fetch_disclosures` 반환 시그니처 변경(`list` → `(list, fetch_status)` 2-튜플)이
> **두 곳의 미갱신**을 드러냄. 둘 다 수정. **교훈: 반환형을 바꾼 함수는 모든 호출부 + 적재 DB 스키마를 함께 본다.**

- **증상 A (차단)**: `accumulate_history.py` 가 stage2 append 에서 크래시 — `table stage2_filtered has no column named dart_status`. §12-2 가 stage2/3 산출 CSV 에 `dart_status` 컬럼을 추가했는데 history.db 의 `stage2_filtered`·`stage3_final` 테이블엔 그 컬럼이 없었음(스키마 미마이그레이션). stage3 CSV 도 stage2 SAFE 출력을 이어받아 `dart_status` 를 들고 옴 → **stage2 만 ALTER 하면 stage3 에서 2차 크래시**(오프라인 시뮬레이션으로 확인).
  - **해결(스키마 자가치유)**: `write_to_sqlite` 가 적재 직전 **CSV 엔 있고 테이블엔 없는 컬럼을 `ALTER TABLE … ADD COLUMN "<col>" TEXT` 로 자동 추가**(기존 행 NULL). dart_status 뿐 아니라 **앞으로의 동종 컬럼 추가도 자동 흡수** → 이 부류 크래시 영구 차단, **수동 ALTER 불필요**.
  - **검증(0-diff, 오프라인)**: 패치본(자동 ADD) 적재 결과 == 수동 ALTER 2개 후 적재 결과, `stage1/stage2/stage3/runs` **4테이블 바이트 동일**, 기존 run 총행수 불변. 정상일(컬럼 누락 0)엔 ALTER 미발생 → 기존과 동일. (TEXT 어피니티 — 현재 추가 컬럼 dart_status 는 문자열이라 적합. 향후 수치 컬럼이면 affinity 만 유의, SQLite 동적타입이라 적재는 정상.)
- **증상 B (비차단·관측)**: `catalyst_insider.py`(404)·`catalyst_large.py`(122) 가 `discs = stage2.fetch_disclosures(...)` 로 튜플을 리스트처럼 받음 → `score_buyback_cancel` 이 튜플을 순회하다 첫 원소(리스트)에 `.get` → **`'list' object has no attribute 'get'` 전 종목 실패**(0623 자사주 소각 0건; 0606 표본은 100건). stage2 자기 호출부(라인 312)는 이미 올바르게 `disclosures, dart_fetch = …` 로 언팩돼 있었음.
  - **해결**: 두 파일 모두 `discs, _dart = stage2.fetch_disclosures(...)` 로 언팩. **자사주 소각=관측(가중치 0)** 이라 v3 점수 무관(관측 컬럼 백필은 §6-9 허용). ⚠️ **라이브 DART 미검증**(네트워크 없음) — 재실행 로그에서 `'list'…` 소거 + 소각 재검출 확인 필요.
- **부수 확인 (게이트 정상 동작)**: 0623 부분적재(stage1 kospi 715 만 적재) 상태에서 **완전성 게이트(§12-3)가 stage1 kosdaq=0(DB)** 을 잡아 push·평소텔레 보류, 어제 대시보드 유지, '보류' 알림(침묵 없음). **데이터 손상의 공개 배포를 정상 차단** → §12-3·§12-4 의 "라이브 1회 확인"분이 사실상 충족(실제 degraded 일을 게이트가 막음). 게이트가 운 이유는 stage1 MAX(run_id)=0623·kosdaq 0 단 하나(stage3 는 MAX run_id 가 아직 0622라 통과). **복구**: 패치본 적용 후 디스크의 0623 CSV 로 `python accumulate_history.py --date 20260623`(재스크리닝 불필요) 또는 `.bat` 재실행 → 클린 적재 시 게이트 4항목(stage1 715/1519·stage3 530/1080) 전부 통과(오프라인 검증).

---

## 13. 저변동 트랙 (lowvol) — 제3 트랙 (2026-06-25 신설)

> 상세 설계·검증은 **LOWVOL_TRACK_DESIGN.md**, 골대는 **PREREGISTER_lowvol.md**.

**정체성**: v3=중소형 과매도 반등, large=대형 가치, **lowvol=적당히 빠진 우량 저변동주**.
세 트랙은 데이터/인프라(stage3_final·history.db·대시보드·텔레그램·git)만 공유, 유니버스·점수·표시 분리.

**발견 경로**: 18거래일(0602~0624) 오프라인 분석에서 v3 단기 칼날(BUY 5일 −4.7%, 단 시장초과는
+2.4%p로 방향은 옳음)을 추적하다, 절대수익과 단조관계인 신호 3종 발견:
- realized_vol IC −0.23(저변동 선호), return_1w IC −0.21(단기반전), roe_value IC +0.12(우량).
- **반전은 h=5d만 유효**(h=20d서 부호 반전 +0.09 = 모멘텀). 호라이즌 주의.
- 학술 일치: 단기반전(Jegadeesh 1990), 단 유동성 프리미엄(Nagel)이라 **유동성 하한 필수**.

**모델(테이블 `lowvol_scores`, 가중치 0 관측, PIT 안전)**:
- 순위합 = run 내 cross-sectional 백분위 합. 핵심팩터 실측 필수(NaN 제외), 보조 NaN=0.5.
- lv_a 저변동+ROE+반전 / lv_b 저변동+ROE / lv_c 낙폭+ROE+반전 / lv_d 낙폭+ROE / lv_a3=lv_a(상한60).
- 유니버스: 과매도 30~70(극단 제외=반전 살림) + 유동성 ≥5억(분포 근거). 일평균 ~439종목.

**검증(전부 18일 in-sample=가설, §11 노이즈 구간)**:
- IC(h5 시장초과): lv_a 0.226, lv_a3 0.234, lv_b 0.191, lv_c 0.142, lv_d 0.158.
- lv_a 견고성 1위: 시장분리(코스피 0.251/코스닥 0.211)·시간분할(전반 0.242/후반 0.226)·5분위
  단조(−1.02→+2.13 무결점)·유니버스민감도(0.2~0.25) 전부 최상위.
- **상승장/하락장 양방향 작동**(시장 방향대로 증폭): 상승장 5일 Q5 1일 +2.0%(상승확률 73%),
  하락장 Q5 −1.6%(Q1 −2.6%). 단 이 기간 23일 중 18일 하락 → 전체 평균은 마이너스(장 탓). 상승장 표본 5일=유보.
- **realized_vol 커버리지 43% → lv_a/b의 h=20d(장기)는 표본 0, 현재 미검증.** forward 필요.
- 결합 교훈: 저변동·반전 **AND 금지(모순집합=추가하락), 순위합으로 가산.** 낙폭·OCF·모멘텀
  추가는 가산가치 0(중복) → 3팩터가 스위트스폿.
- v3·large 산출물 0-diff 불변 / lv_a3 추가 시 원본 lv_a~d 0-diff(최대차 4e-16) 검증.

**배포(v31g 패턴)**:
- `lowvol_score.py`(적재) → `build_lowvol_filter.py`(lv_a CSV → docs/latest_*_lowvol.csv) →
  `docs/lowvol.html`(전용 페이지, fetch, 점수해석·수급·양방향 설명 포함). `notify_lowvol_test.py`(테스트 텔레, 경고 도배+수급).
- 파이프라인: 2.86 적재(증분) · 2.87 CSV · 4b 텔레(정상 배포일만). **전부 비치명**.
- 노출은 lv_a만(테스트·관측·매수신호 아님 명시). lv_b~d·lv_a3는 shadow(점수만 누적).
- HTML 표시: lv점수·실현변동성·외인/기관 수급(5d·20d)·v3 bucket 비교. 섹터는 sector_cache.json에서 채움(stage3 sector 100% 빈칸).

**불변 규칙(상속)**: 관측 팩터 점수 미투입(검증 전), v3·large 불변, 매직넘버 금지(유동성/컷=분포),
PIT 엄수, post-hoc forward-only(판정 OOS 40거래일), 출력 0-diff 오프라인 검증 우선.

**운영 주의**:
- 최초 1회: `python lowvol_score.py --full`(36178행) + `python build_lowvol_filter.py`.
- 코드는 PUSH_ALLOWLIST(docs)에 안 걸림 → **수동 `git add` 커밋 필요**. docs/것만 자동 push.
- 판정: 등록일(0625) 이후 OOS 40거래일. 그 전 노이즈. Bonferroni 분모 5(모델 5개).
- **점수 해석(중요)**: lv점수↑ = "오른다"가 아니라 "시장 방향대로 증폭"(상승장 더 오름·하락장 덜 빠짐). 매수신호 아님.

**다음**: forward 누적 → 40거래일 후 lv_a vs lv_a3 vs 나머지 OOS 비교(발견기간 제외). 그때
검증 통과 모델만 가중 판단.

---

## 14. 탐색(research)과 배포(production)의 분리 — 규율은 유지, 사고는 확장 (2026-06-25)

> **왜 이 절을 추가하나**: 지금까지의 불변 규칙(관측 우선·40거래일·post-hoc forward-only·
> 0-diff·Bonferroni)은 **과적합 방지를 위한 흉터**이고 유효하다. 단 이 규칙이
> "검증 안 된 건 *점수에* 넣지 마"를 넘어 "검증 안 된 건 *시도조차* 마"로 읽히면 탐색이 죽는다.
> 둘은 다르다. 이 절은 그 경계를 명문화해, 도전적 시도를 과적합 누출 없이 허용한다.

### 14-1. 두 모드
- **탐색 모드 (`research/` 산출물 한정)**: **게이트 면제.** 어떤 가설·조합·ML·이상한 팩터든
  history.db로 자유롭게 굴린다. ML(트리/부스팅), 비선형 결합, 팩터 상호작용, 레짐 분기,
  대체 라벨(h별·국면별) 등 전부 허용. **단 산출물은 `research/`(또는 노트북)에만** — 절대
  `docs/`·텔레그램·점수 테이블(v3_scores·lowvol_scores·large_final)로 나가지 않는다.
- **배포 모드 (사용자 노출·가중)**: **기존 모든 게이트 적용.** 탐색에서 유망한 게 나오면
  → 새 model_id로 **관측 적재(가중치 0)** → **OOS 40거래일**(가치 트랙은 60~120일)
  → **부트스트랩 CI·Bonferroni** 통과 → **0-diff 검증** → 그때만 가중/노출.

### 14-2. 다리(승격 규칙) — 이게 있어야 탐색이 화려해도 안 샌다
- 탐색의 **in-sample 수치는 영원히 "가설"**. 발견 기간 IC가 아무리 좋아도 증거가 아니다.
- 배포 승격은 **등록일 이후 OOS 통과로만**. (PROJECT §11·§11-A·불변규칙 8 그대로 상속.)
- ML·복합모델일수록 자유도가 커 과적합 위험↑ → **OOS 문턱을 더 높이고**(예: 별도 호라이즌
  재현 필수), 해석가능성 낮으면 **관측 기간을 더 길게** 둔다.
- 탐색→배포 전환 시 반드시 **PREREGISTER 문서 신설**(골대 고정), 발견 기간 명시.

### 14-3. 지침(PROJECT_INSTRUCTIONS)에 추가할 문구
> "**새 가설 탐색은 게이트 면제로 자유롭게 제안·실험한다**(research/ 산출물 한정, docs/·텔레·점수
> 테이블 유출 금지). **배포·가중·사용자 노출만** 기존 검증 게이트(관측→40거래일 OOS→CI/Bonferroni
> →0-diff)를 반드시 통과시킨다. '검증 전이라 점수에 못 넣음'과 '검증 전이라 시도도 못 함'은 다르다
> — **후자는 금지가 아니다.** 데이터 갭이 있으면 상상 팩터를 만들지 말고 갭을 명시하되, 갭이
> 없는 조합·방법론 탐색은 적극 제안한다."

### 14-4. 열린 탐색 백로그 (research 모드 — 전부 가설, 배포 전 게이트 필요, 우선순위순)

> 핵심 인식: **현 시스템의 병목은 종목 선택이 아니다.** 랭킹 방향은 맞는데(시장초과 +) 절대수익이
> 마이너스인 건 **시장 베타 탓**(메모 일치). 그런데 세 트랙 전부 "오늘 *어떤* 종목"(cross-sectional)만
> 다루고 "*언제·얼마나* 노출"은 없다. 가장 큰 레버는 여기다.

1. **거래비용·세금 현실 점검** [1순위·전제]: 한국 거래세(매도 ~0.18~0.23%)+슬리피지+호가단위를
   백테스트에 넣어 기존 IC/수익이 **비용 후에도 남는지**. 저유동 반전 엣지는 비용에 먹히기 쉬움
   (Nagel 유동성 프리미엄). **이게 음성이면 아래 다수가 신기루** → 반드시 먼저. (db만으로 가능.)
2. **알파/베타 분해** [측정 인프라]: BUY+WAIT 손익을 시장(equal-weight proxy 또는 지수) 대비
   **베타 × 시장수익 + 잔차알파**로 분해. "절대 마이너스=베타 탓"을 정량 확인. **회전율·MDD·
   국면별(상승/하락 레짐) 성과**를 IC와 함께 상시 측정 패널로. (db만으로 가능.)
3. **베타 오버레이** [최대 레버 후보]: 레짐 하락 구간에서 **노출 축소 또는 인버스/선물 헤지**로
   베타를 죽이고 알파만 잔존. cross-sectional이 아닌 **직교 축**이라 기존 트랙 **0-diff 유지하며
   위에 얹음**. (백테스트는 db, 실집행은 사용자 — 단 이 트랙도 "발굴·관찰", 자동매매 금지.)
4. **트랙 앙상블** [국면 배분]: v3(반등)·large(가치/방어)·lowvol(저변동)은 서로 다른 국면에서
   켜진다 → **레짐별 동적 가중 메타모델**을 관측. "섞지 마"는 *점수 오염* 금지일 뿐, 트랙 위
   **메타 배분**은 별개 축(점수 테이블 안 건드림).
5. **호라이즌 분리 포트폴리오** [구조]: 반전=5d·가치=60d를 이미 아는데 한 화면에 섞고 있음.
   **보유기간 다른 두 바스켓**으로 나누는 것 자체가 구조 개선(LOWVOL §4 호라이즌 표 활용).

### 14-4-R. 백로그 1~4번 착수 결과 (2026-06-27 오프라인) — 전부 가설(OOS<40)

> 핵심: **백로그 대전제("병목은 시장 베타")가 세 각도에서 실증됨.** 상세 §17. 측정구간 하락편중이라 가설.

- **[2번 알파/베타 분해] 완료**: lv/v3 상위 바스켓 베타 **1.0~1.19**, 시장상관 **0.99**. "저변동" 표방하나
  실제 베타 1 근처 → 절대수익은 베타가 좌우, 알파는 얇음(+0.5%p/주 수준). checkup.py ②축에 상시화.
- **[3번 베타 오버레이] 1차 기각**: regime_score로 하락회피('약세 시 노출 끄기') → **역효과**(상시노출
  −3.53% vs 약세차단 −4.74%). regime↔forward **상관 −0.84**(역행지표) — 이 평균회귀 장에선 regime
  약세=바닥 근처. **베타 스위치 역효과, 추세장 전환 시 재검토**(maturity 보류와 동일). 컨트래리언(거꾸로)도
  전반 +4.87%/후반 −9.47%로 무너짐=과최적화 경계, 채택 안 함.
- **[4번 트랙 앙상블] 기각(이 구간)**: lv↔v3 수익 상관 **0.996**. 50:50 앙상블 표준편차(7.32)=단일(7.37)
  → **분산효과 없음**. 같은 베타 공유라 국면 바뀌어도 동조. "다른 국면용 트랙" 가설 미관측.
- **[1번 거래비용] 보류**: 자동매매 계획 없으면 코드화 불필요(일회성 검산용). lv 상위가 코스닥 급락주라
  비용 취약(Nagel) → "이 신호로 실매매?"를 정할 때 1회 점검.

### 14-5. 불변(탐색해도 안 건드리는 것)
- v2.6 엔진·v3 점수·large_final·lowvol_scores **테이블/표시 0-diff**.
- 탐색 산출물의 **docs/·텔레그램·점수 테이블 유출 금지**(14-1).
- KIS/실집행은 **조회·관찰 전용, 자동매매·주문 금지**(트랙 불문).
- post-hoc forward-only·매직넘버 금지·PIT 엄수(전 트랙 상속).

---

**다음 실증(예고)**: 위 백로그 1·2번(거래비용 점검 + 알파/베타 분해)을 perf 핸드오프의
history.db(115MB)로 **오프라인 착수**. v3_scores·lowvol_scores의 BUY+WAIT 수익에 한국 거래비용을
반영하고, equal-weight 시장 proxy 대비 베타/알파로 분해한다. 이는 `research/` 분석이며 점수·docs/
**0-diff**(읽기 전용 분석, 산출은 research/ 리포트만).

---

## 15. 상승포착(mom_a) 대조 관측 모델 (2026-06-27)

> 상세 설계는 lowvol_score.py 주석. lowvol 트랙의 **대조군**이지 새 트랙 아님.

**왜**: lowvol(저변동·반전)은 상승장 주도주를 구조적으로 놓친다(저변동·이미 빠진 종목을 고르므로).
오프라인 분석에서 '오를 종목'을 가리키는 신호 발견(forward 5d 시장초과 IC):
`vs_SMA20_%`(+0.141)·`return_1m_%`(+0.125)·`vol_1w_vs_1m_ratio`(+0.089). realized_vol(저변동)은
상승포착엔 **−0.204**(반대 방향). → lowvol과 **정반대 축**을 같은 인프라에서 대조 관측.

**모델**: `mom_a` = vs_SMA20(핵심) + 1개월모멘텀 + 거래량팽창 순위합. 전부 '큰값=좋음'(ascending=True).
- 테이블 `lowvol_scores` 공유, **model_id 접두사 `mom_`으로 정체성 구분**(lv_*와 섞지 않음).
- 유니버스: lowvol과 동일(과매도 30~70 + 유동 5억) — 대조라 같은 모집단서 비교.
- 핵심팩터 vs_SMA20 커버리지 **100%**(lv의 realized_vol 43% 병목과 달리 NaN 문제 없음).
- 가중치 0 관측. post-hoc·하락장 in-sample 발견 → **forward-only, 등록일 20260627**. Bonferroni 분모 6(lv 5 + mom 1).

**검증(0-diff)**: `--full` 시 기존 lv_a~d·lv_a3 점수 **최대차 0.00**, spec_hash 불변(모델별 계산 구조).
증분/idempotent 정상(재실행 0행). v3·large 산출물 불변.

**⚠️ 현재 한계(중요)**: 이 측정구간은 하락장이라 **'상승 주도주'가 거의 없다**. mom_a 상위 종목도 vs_SMA20이
전부 음수(20일선 아래) = "그나마 덜 빠진 종목"을 고름. 즉 **지금은 '상승포착'이 아니라 '하락장 상대방어'로
작동**. 진짜 주도주 포착력은 **상승장이 충분히 낀 뒤 forward로만** 판정. 그 전엔 노이즈. 또한 mom_a↔lv_a 점수
상관 **+0.07(거의 직교)** — "정반대"가 아니라 독립. (당초 '정반대' 표현은 정정.)

**운영**: 적재는 기존 2.86단계(`python lowvol_score.py` 증분)가 자동 처리(추가 작업 0). 최초 1회 `--full` 권장.
표시·텔레 미노출(순수 관측). docs/lowvol.html은 여전히 lv_a만.

---

## 16. 정기 점검 도구 checkup.py (2026-06-27)

> research 모드(§14-1): 읽기전용, 산출은 `research/` JSON만. 점수·docs·텔레 0-diff. 네트워크 불필요.

**목적**: "수치를 정기적으로 보며 잘 되나/안 되나/기준이 바뀌었나"를 **감이 아니라 규칙으로**. 그동안 수동
오프라인 분석으로 하던 것을 1개 스크립트로 묶어 매주 재현.

**세 감시축**(사용자 정의):
- **① IC 안정성**: 점수 적중도가 시간 가며 무너지나/살아나나 (전반 IC → 후반 IC).
- **② 베타**: 절대수익이 시장과 얼마나 붙어 가나 (바스켓 베타·상관, 전/후반 추세).
- **③ 레짐 신호**: regime_score가 forward를 맞히는 방향이 뒤집히나 (상관 부호, 전/후반).

**판정 룰**(§11 상속, 골대 고정 = 사후 변경 금지):
- OOS 거래일 < 40 → **무조건 '노이즈'**(기본값). 그 전 수치는 추세 참고이지 판정 아님.
- 40+ 도달 시: 부트스트랩 95% CI 0 위 + 방향 ≥60% → **'유의 후보'**. 경계값은 **'기움'**까지만. CI가 0 포함이면 **'미달'**(챔피언 유지).
- **'기준변화 의심'**: 직전 점검 JSON 대비 ①IC 부호 / ②베타 0.3↑ / ③레짐상관 부호가 뒤집히면 `⚠️ shift_flags`.

**사용법**:
```bash
python checkup.py                  # 콘솔 패널만 (추세 확인)
python checkup.py --json           # research/checkup_YYYYMMDD.json 저장(다음주 비교 기준)
python checkup.py --since 20260606 # 발견기간 빼고 OOS만(40일 도달 후 판정용)
```
**운영 권장**: 매주 `--json`. JSON이 쌓여야 다음 점검이 직전과 비교해 기준변화를 잡는다(안 쌓으면 '첫 점검'으로만 표시).
이 JSON은 분석결과라 docs 무관 — git 안 올려도 되고, 올리려면 수동 add. 코드는 PUSH_ALLOWLIST 밖이라 `git add checkup.py` 수동 커밋.

**검증**: history.db로 라이브 실행 = 오프라인 분석과 숫자 완전 일치. 기준변화 감지는 부호 조작 테스트로 IC반전·베타급변·레짐반전 3종 다 포착 확인. DB md5 무변경(읽기전용). 인코딩 UTF-8 고정(Windows 콘솔 cp949 한글깨짐 수정 — stdout.reconfigure + open(encoding) 3곳).

**첫 베이스라인(20260627)**: 활성 24run. 전 모델 노이즈(최대 v30 OOS 15일). 감시축에서 2개 신호 관찰
(전부 가설): ⓐ **후반 IC 상승**(거의 전 모델 — v30 0.026→0.108, v31a −0.040→+0.135, lv_a 0.151→0.321;
단 mom_a는 0.201→0.042로 식음=하락장 탓). ⓑ **레짐상관 후반에 음으로 깊어짐**(lv_c −0.001→−0.863,
mom_a +0.050→−0.893) = 최근 평균회귀 장세 심화. 두 신호 일관(평균회귀↑ → 반전점수 IC↑ + 레짐역행↑).

---

## 17. 핵심 학습 — 절대수익의 병목은 시장 베타 (2026-06-27, 세 각도 실증)

> **이게 오늘의 가장 중요한 결론.** 세 트랙(v3·lowvol·mom)이 전부 "오늘 어떤 종목"(cross-sectional)만
> 다루고 "언제·얼마나 노출"(베타)을 안 다룬다. 절대수익을 좌우하는 건 후자다.

**세 각도가 같은 벽을 가리킴**:
1. **베타 측정**: lv/v3 상위 바스켓 베타 1.0~1.19, 시장상관 0.99. "덜 빠짐"의 정체는 낮은 베타가 아니라 얇은 알파.
2. **레짐 필터 역행**: regime으로 하락 회피 시도 → 손해(상관 −0.84). 종목 타이밍을 regime으로 잡는 것 실패.
3. **트랙 분산 무효**: lv↔v3 상관 0.996. 트랙을 바꿔도 같은 베타라 분산 안 됨.

**함의**:
- **cross-sectional 안(종목 선택·트랙 선택·레짐 필터)에서 '절대 마이너스 줄이기'는 불가.** 셋 다 같은 시장에 롱으로 묶여 베타 공유.
- **비대칭(downside↓ upside↑)은 롱 바스켓 바깥에서만**: ⓐ 시장중립(인덱스 숏 헤지 → 베타 제거, 알파만; 단 upside도 깎임) ⓑ 옵션(풋으로 하방보험 + 롱 유지). **둘 다 현 시스템 '발굴·관찰' 범위 밖**(실집행=자동매매 금지). 연구는 백테스트로만.
- **lv/v3/mom의 올바른 용도 재확인**: "사면 오른다"가 아니라 **"꼭 들고 있어야 한다면 시장보다 덜 빠질 후보 고르기"**(상대 신호). 절대수익은 시장이 오를 때 따라옴. 현 마이너스는 종목 탓이 아니라 하락장 탓.

**단서/한계**: 측정구간 하락 편중(23일 중 18일 하락), OOS<40, 진짜 상승장 표본 거의 없음 → **전부 가설**.
상승장이 끼거나 추세장 전환 시 그림이 바뀔 수 있음(특히 레짐 역행은 평균회귀 장 한정일 가능성). checkup.py
③축이 레짐 부호 반전을 감시 → 양수 전환 시 이 결론 재검토.

---

### 17-1. 주말 새벽 실행 시 run_id 처리 (2026-06-27)

**상황**: 금요일 장 마감분을 토요일 새벽 실행하면 run_id가 토요일 날짜로 찍힘(run_id = 스크리너 실행일 =
`datetime.now()`, 거래일 아님). 예: 6/26(금) 장 → 6/27(토) 새벽 실행 → run_id 20260627.

**문제없는 이유(검증)**:
- **내용물 정상**: FDR이 토요일 요청에도 마지막 거래일(6/26 금) 종가 반환. 6/25 run 대비 price **97.5% 갱신** 확인 = 금요일 종가 정상 적재.
- **판정 무영향**: v3_backtest·checkup이 run을 `sorted(run_id)` **순번(run-step)**으로 처리(캘린더 날짜 아님). `filter_active_runs`가 정적 주말 run 자동 제외(주석에 "주말 run 포함" 명시 = 설계가 주말 실행 상정). 날짜 라벨이 토요일이어도 "직전 거래일 다음 관측" 한 칸으로 올바르게 들어감.
- **적재 완전성**: stage1/2/3·v3_scores·lowvol_scores 전부 평소 범위, 완전성 게이트 통과.

**조치**: **마이그레이션 금지.** 이미 적재된 토요일 run을 금요일로 개명하면 v3_scores append-only 동결 이력과
어긋나 오히려 위험. 라벨만 토요일일 뿐 데이터·판정 모두 정상이므로 **그대로 둔다**. (다음부터 금요일 장 마감
후 돌리면 라벨도 맞지만, 안 맞아도 무해.)

---

## 18. KIS 수급 대폭 확장 — 6종 세부 + 공매도/신용/대차 (2026-06-27 저녁)

> 상세 코드는 `kis_flows.py` 주석. 대형(large) 트랙 §7(수급 리버설)의 재료를 실제로 적재하면서,
> lowvol 분석용으로도 쓸 수 있게 확장. **전부 조회 전용**(자동매매·주문 절대 금지, 불변규칙 상속).

### 18-1. 기관 6종 세부 분해 (`daily_flows`)
- **기존**: 외인/기관/개인 net_qty 3종만. **확장**: 기관을 6종으로 분해 — 연기금(`pension_net_qty`)·
  투신(`trust`)·증권(`secfirm`)·사모(`prveq`)·보험(`insu`)·은행(`bank`).
- **API 교체**: 기존 `inquire-investor` → `investor-trade-by-stock-daily`(TR `FHPTJ04160001`,
  url `/uapi/domestic-stock/v1/quotations/investor-trade-by-stock-daily`). output2에 ~30거래일 반환.
  외인/기관/개인 필드명 동일 → **하위호환**.
- **0-diff 검증**: `--verify N` 모드 신설 — 새 API의 외인/기관/개인 값이 기존 `daily_flows`와 같은
  날짜에서 일치하는지 대조. 실측 **300/300 일치(0 불일치)** 확인 후 교체. 스키마 자가치유(ALTER ADD COLUMN).

### 18-2. 공매도·신용·대차 (`short_flows` — 별도 테이블, daily_flows 0-diff 유지)
- **3종 각각 별도 KIS API**:
  - **공매도** `FHPST04830000` `/quotations/daily-short-sale` out=output2 → short_qty·short_vol_ratio·short_val
  - **신용(융자)** `FHPST04760000` `/quotations/daily-credit-balance` out=output → credit_bal_qty/amt/rate
  - **대차** `HHPST074500C0` `/quotations/daily-loan-trans` out=output1 → loan_bal_qty/amt·loan_chg
- **기본=공매도만**(종목당 1회). **신용·대차는 `--with-credit --with-loan` 옵션**(종목당 3회, 시간 3배).
- **디버깅 이력(파라미터 필수값 누락 — 단계별 에러표시로 추적)**:
  - 신용: `FID_INPUT_DATE_1`(결제일자) 필수 → 추가.
  - 대차: `MRKT_DIV_CLS_CODE="3"`(종목기반, 코스피/코스닥 무관) + **`CTS=""`(연속조회키, 빈값이라도 필수)** → 추가.
  - 공매도 호출이 "실패"로 찍힌 건 collect_short가 공매도→신용→대차 순차 실행 중 **신용 예외가 함수 전체를
    실패로 잡아서** — 실제 공매도는 정상. 에러에 단계명("공매도:/신용:/대차:") 실어 구분.
- **신용 날짜 특성(버그 아님)**: 신용은 결제일 기준이라 **최근 1~2거래일은 비어 옴**(정상). 검증 출력이
  최근일만 봐서 None처럼 보였을 뿐. 융자 30일치·대차 61일치 정상 적재 확인.
- **위생(기존 원칙 상속)**: 에러는 저장 안 함(다음 실행 보충), rate limit 0.55s 보수적, INSERT OR REPLACE
  전 `--verify`로 0-diff 확인.

### 18-3. combined 유니버스 (large + lv_a)
- **문제**: kis_flows가 `large_universe`(대형 500)만 받아서 lv_a 중소형 종목 수급이 45%만 커버.
- **해결**: `load_combined_tickers`(large 500 + lv_a 중소형 과매도, 중복제거 → **706종목**). `--universe combined` 기본,
  `--universe large`로 기존 동작 복귀 가능. 연결순서 안전(유니버스 로딩이 con 열기 전, 각자 with절).
- **효과**: lv_a 최신 run 수급 커버리지 45%→**100%**. 연기금+공매도 ~13분(매일 .bat 감당 수준).
  신용·대차까지면 ~26분(수동 시).
- **0-diff**: `--universe large`는 기존과 정확히 500종목·동일. combined가 large 전부 포함(누락 0).

### 18-4. 운영
- **.bat 무변경**: 기존 `python kis_flows.py` 한 줄이 이제 combined 706종목 연기금+공매도 자동 적재.
  신용·대차는 수동(`--with-credit --with-loan`)일 때만.
- **수급 관측 적재 = 점수 미투입**(검증 후에만). large 트랙 §7 수급 리버설 + lowvol §19 공매도 챌린저의 재료.

---

## 19. lv_short 공매도 챌린저 + 수급/공매도 IC 분석 (2026-06-27 저녁)

> 18-2 공매도 데이터를 lowvol 트랙에 연결. 상세 설계는 lowvol_score.py 주석. **전부 노이즈 구간(OOS<40) 가설.**

### 19-1. lv_short 챌린저 (lowvol_scores 공유, 가중치 0)
- **정의**: `lv_short` = lv_a(저변동+ROE+반전) + **공매도비중(낮을수록 좋음)** 4팩터.
  - 핵심팩터=realized_vol(lv_a와 동일, 실측필수). **공매도는 보조 팩터**(NaN=0.5 중립) — short_flows
    커버리지가 lv 종목의 일부라 핵심으로 쓰면 공매도 없는 종목이 전부 제외돼 유니버스가 깨짐. 보조라 유니버스 lv_a와 동일.
  - 데이터: stage3_final에 short_flows를 **LEFT JOIN**(date==run_id)으로 short_vol_ratio 확보. short_flows 부재 환경서도 안전.
- **0-diff 검증(history.db)**: lv_a~d·lv_a3·mom_a 점수 **최대차 0.00**(공매도 추가가 기존 안 건드림).
  lv_short 6,360행/15run(lv_a와 동일 유니버스). 증분 재실행 0행(idempotent). v3·large·daily_flows·short_flows 전부 불변.
  lv_short vs lv_a 상관 0.922(공매도 반영 순위조정 — 코스피 16·코스닥 7종목 ±1~2위).
- **표시(`build_lowvol_filter.py`·`docs/lowvol.html`)**: CSV에 lv_short_score·lv_short_rank 추가.
  HTML에 lv점수 옆 "lv+공매도"·"공매도순위" 컬럼 + 설명. **텔레그램은 lv_a 유지(교체 안 함)** —
  검증 전이라 나란히 비교만. 40거래일 후 lv_short가 나으면 그때 승격 판단.
- **판정**: post-hoc·forward-only. 등록일 20260627, OOS 40거래일. Bonferroni 분모 갱신(lv 5 + mom 1 + lv_short = 7).

### 19-2. 수급/공매도 IC 분석 결론 (lv_a 유니버스, 22 run 겹침, 전부 가설)
> 공매도 115거래일·연기금 등 적재 후 lv_a forward와 조인(가격 겹침 22 run, h5라 ~10run). **§11 미달 노이즈.**

- **단독 IC(h5)는 기존 lv 팩터가 압도**: 저변동 −0.208★·낙폭 +0.168★·ROE +0.110★·거래량팽창 +0.095★.
  신규 수급(공매도·연기금·신용·대차)은 **단독 IC 다 약함**(0 근처).
- **공매도만 "더하면" 오름(핵심 발견)**: lv_a 페어비교 IC +0.176→+0.210, **ΔIC +0.034 CI[+0.014,+0.056]★**.
  롱숏 +2.21→+3.29%p. 시장별로 **코스닥 +0.070★ / 코스피 +0.000**(코스닥 한정). 메커니즘: 공매도×고변동
  −1.08%(최악) — 공매도가 "변동성 크고 하락베팅 몰린" 종목 걸러냄(lv_a 사각지대). **단 run 10개 가설.**
- **다른 팩터 추가는 효과 없거나 해**: 거래량팽창 ΔIC −0.019★(해), 대차증감 −0.017(무). 공매도만 유일.
- **연기금=대형주 현상**: lv_a(중소형)서 IC +0.022(0포함). large 유니버스선 −0.074★(역신호). 연기금이
  대형주 위주 거래 → 중소형엔 신호 약. **연기금은 large 트랙용**(§7 수급 리버설). "기관 IC≈0=연기금(역)+증권(중립)
  상쇄" 가설은 **대형주서만 성립**.
- **신용·대차는 lv_a에 부적합**: 신용(융자잔고율) 분위수 비단조(Q3까지↑ Q4↓). 대차는 공매도와 같은 결이라
  **공매도에 흡수**(따로 가치 없음).
- **"공매도 감소=숏커버 반등" 가설(사용자) — 약함**: 공매도 3일변화 IC −0.050★(약하게 방향맞음)이나 5일변화 +0.001(무).
  결정적으로 **"공매도 높음+감소(숏커버 후보) +0.40% vs 높음+증가 +0.34%"=차이 +0.06%p** — 숏커버 효과 안 보임.
  → 챌린저엔 감소추세 대신 **공매도 수준(낮을수록 좋음)** 채택.
- **수급 5d×20d 형태 vs 절대수익**: "외인·기관 5d 동반매수"가 덜 빠짐(둘다 5d+ −5.41% vs 둘다 5d− −7.26%,
  +1.85%p). 단 **20d는 미미**(5d+조건서 오히려 20d매도가 살짝 유리=반전논리, 차이<1%p). **절대수익 차이의
  대부분은 시장 베타**(시장초과로는 ±0.5%p로 작음) → "이 형태면 산다"보다 "5d 동반매도(약세) 회피" 수준.

### 19-3. 함의
- **수급은 여러 각도로 봐도 lv_a엔 일관되게 약함** — 절대수익은 시장 베타가 지배(§17 재확인), 종목선택 신호로는 미미.
- **유일한 예외=공매도(코스닥)**: ΔIC +0.034로 유의했으나 run 10개. lv_short로 관측해 forward 검증.
- **연기금은 large 트랙에서** 의미 가능성(대형주 현상). lowvol 아님.
- 다음: forward 누적 → lv_a vs lv_short OOS 비교(checkup에 비교축 추가 가능). 40거래일 전 노이즈.

---

## 20. lv_a 종목특성·베타 심화 + hv_a 고변동 챌린저 (2026-06-27 저녁)

> §17(병목=베타)을 종목 단위로 더 깊이 검증하고, 그 과정서 나온 "고변동 대박" 가설을 챌린저로.
> 상세는 lowvol_score.py 주석. **전부 노이즈 구간(OOS<40, 상승 run 4개) 가설.**

### 20-1. "지속 상위권인데 절대수익 낮다 = 베타 탓" 종목 단위 확증
- **질문**: lv_a 상위권에 계속 머무는 종목이 절대로 계속 빠지는 게 베타 탓이냐, 종목이 나빠서냐.
- **분해(h5, 지속 상위10% 종목 = 5회+ 등장 중 절반이상 상위10%, 61개)**:
  절대 −5.15% = **시장 −5.41%(베타) + 시장초과 +0.25%(알파)**. **절대 마이너스의 96%가 베타.**
  알파는 오히려 양수 → 종목 선택은 옳음("나쁜 종목"이 아니라 "좋은 종목인데 시장 따라 빠짐").
- **찐상위(80%+) 알파 +0.94%** > 지속상위(50%+) +0.25% → **상위권에 꾸준할수록 알파 큼**(종목 선택 작동 확증).
- **베타 직접 추정**: 지속상위 종목수익 = α + β×시장수익 → **β=0.85**(저변동답게 1.0보다 약간 낮음, 단 0 아님).
  → 시장 −5%면 −4.2% 빠지는 게 '정상'. 롱인 한 시장 빠지면 같이 빠짐. §17 재확인.

### 20-2. "오른 종목 특성 = lv_a인데 왜 절대수익 낮나" — 핵심 오해 해소
> 사용자 질문: 절대수익 큰 종목 특성(저변동·ROE·반전)이 lv_a와 같은데 왜 lv_a 절대수익이 낮나?
- **답: lv_a는 "오를 종목"이 아니라 "덜 빠질 종목"을 고른다. 방향은 같아도 종목은 다름.**
- **겹침 측정**: lv_a 상위10% 중 실제 절대수익 상위10%였던 건 **12%뿐**. 실제 오른 종목의 lv 백분위
  평균 **0.49(딱 중간)**. → "많이 오른 종목"은 lv_a가 특별히 고르는 게 아님.
- **저변동의 본질(변동성 5분위, 절대수익)**: 최저변동 −2.29%(>+5% 14%·<−5% 37%) vs 최고변동 −5.18%
  (>+5% 20%·<−5% 50%). **저변동=큰하락 회피+큰상승 포기(안정·밋밋)**. 고변동=대박·쪽박 공존(평균 더 나쁨).
- **lv_a 5분위 단조**: Q1 절대 −6.80%(초과 −2.44%) → Q5 −3.78%(초과 +0.59%). **점수 높을수록 덜 빠짐**
  (종목선택 작동) — 단 "덜 빠지는" 방향. lv_a 상위10%조차 34%만 오름, 평균 −3.54%(하락장 베타).
- **절대수익 승자(상위10%, +12.1%) 특성(전종목 사전 IC, h5)**: 저변동 −0.245★(최강)·52주낙폭 +0.190★·
  20일선대비 +0.171★·1개월모멘텀 +0.159★·거래대금 −0.155★(소형 유리)·ROE +0.127★·거래량팽창 +0.107★.
  기관5일 −0.038★(역). 승자 68% 코스닥(소형). → 승자=저변동+많이빠짐+우량+소형. **lv_a가 잡는 특성과 방향 일치.**

### 20-3. ROE 가중 강화 검증 → 기각(원본 1:1:1 유지)
- **질문**: 승자 특성서 ROE 유의 → lv_a의 ROE 가중 높이면?
- **결과(h5 시장초과)**: ROE×1(원본) **0.217** → ×2 0.195 → ×3 0.176 → ×4 0.163. **단조 하락.**
  페어비교 ΔIC(ROE2배−원본) **−0.022 CI[−0.036,−0.008]★유의** = 강화는 유의하게 나쁨.
- **이유**: lv_a 유니버스 내 단독 IC = 저변동 +0.199 > ROE +0.110 > 반전 +0.091. ROE는 셋 중 최약 →
  가중 주면 강한 저변동 비중 줄어 손해. **"동등가중 최적, 튜닝 실패" 재확인. ROE는 포함 정당·강화 부적절.**

### 20-4. hv_a 고변동 대박 챌린저 (lowvol_scores 공유, 가중치 0)
- **정의**: `hv_a` = lv_a의 저변동을 **고변동으로 뒤집은 것**(highvol=realized_vol ascending=True).
  핵심팩터만 highvol로 교체, ROE·반전·유니버스는 lv_a 동일 = **정확한 대척점 대조 실험**.
  FACTORS에 `highvol`(realized_vol 같은 컬럼·반대 방향) 추가.
- **동기**: 사용자 질문 "큰 수익 노리면(고변동) 반등장에서 어떻게 되나 — 예측 가능한가?"
  → 데이터로 "고변동은 상승장 대박 가능하나 예측 어렵고 하락장 취약" 확인, 상승장 표본 부족(run 4개)이라
  **forward 관측 필요** → 챌린저화.
- **0-diff 검증(history.db)**: 기존 lv_a~d·lv_a3·lv_short·mom_a 점수 **최대차 0.00**. v3·large·stage3·
  daily_flows·short_flows 불변. hv_a 6,360행/15run(lv_a 동일 유니버스). 증분 0행(idempotent).
- **작동 확인**: hv_a 상위10% 평균 변동성 **0.0709**(고변동 고름) vs lv_a 0.0261(저변동). 변동성축 상관
  **−1.00**(정확히 반대). 전체 점수 상관 +0.297(ROE·반전 2축 공유 탓 — 변동성축만 보면 완전 반대).
- **장세별 성과(상위10% 절대수익, 전부 가설)**:
  - 📈상승 run(4): hv_a +4.10%(오른비율 61%) vs lv_a +3.95%(80%) — **고변동이 약간 높으나 들쭉날쭉**(명중↓·대박↑).
  - 📉하락 run(6): hv_a **−11.92%** vs lv_a −8.00% — **고변동 훨씬 더 깨짐**(방어 안 됨).
  - 전체: hv_a −5.95% vs lv_a −3.54%(하락 편중이라 고변동 더 나쁨).
  - 예측가능성: 고변동 σ 13.0%p(범위 −35~+44) vs 저변동 σ 7.2%p — **고변동 1.8배 불확실**.
- **판정**: post-hoc·forward-only. 등록일 20260627, OOS 40거래일 **+ 상승장 표본 충분 시**(상승 run 4개=가설).
  Bonferroni 분모 갱신(lv 5 + mom 1 + lv_short 1 + hv_a 1 = 8).
- **표시**: 순수 관측 — docs/lowvol.html·텔레그램 **미노출**(lv_a·lv_short만). 검증 안 된 약한 후보라
  사용자 표시 부적합. 필요 시 추후 HTML 비교축 추가 검토.

### 20-5. 세 관점 관측 체계 정리
- **lv_a**(저변동=하락 방어) · **mom_a**(모멘텀=상승 주도주) · **hv_a**(고변동=상승 대박·하락 취약).
- 장세 전환(특히 상승장 도래) 시 forward로 셋 비교 → checkup 비교축 추가 가능.
- **공통 결론(§17 재확인)**: cross-sectional(종목·변동성·트랙) 안에서 절대수익 부호는 못 바꿈 — 시장 베타 지배.
  종목 선택은 "덜 빠지기/더 튀기" 방향 조절일 뿐. 절대 플러스는 롱 바깥(헤지·시장중립)에서만 — 발굴 범위 밖.

### 20-6. sm_a 초소형 트랙 + "lv_a 우물 밖" 탐색 결론 (2026-06-27 야간)
> 사용자 요청: lv_a 개선이 아니라 **완전히 새로운 신호** 탐색. 130컬럼 전수 스캔 + 유니버스 변경.
- **같은 틀에선 lv_a 개선 거의 막힘**: stage3 130컬럼 전수 forward IC + lv_a 페어 ΔIC 스캔 →
  공매도(+0.034, 코스닥)만 유일하게 더해짐. 나머지(52주저점·볼린저·추세·현금흐름·실적·거래량·변동성수축)
  전부 **기존팩터와 중복이거나 ΔIC 음수**(해롭거나 무효). "동등가중·저변동 최적"의 또 다른 확인.
- **구조적 발견**: stage3_final은 **과매도 종목만 저장**(oversold_score 40~100). "정상 우량주·상승 주도주"는
  데이터에 아예 없음 → 모멘텀/가치 등 새 전략은 **데이터 확장 없이는 불가**(B 방향의 근거).
- **유일하게 강한 미개척 신호 = 유동성(거래대금)**: 작을수록 단조롭게 시장초과↑.
  **h20: 초소형(<5억) +6.41% vs 대형(100억+) −6.10%**(격차 12%p). lv_a가 유동성 5억 하한으로 *버리는* 영역.
  유동성 프리미엄(거래불가·슬리피지 위험의 대가). 단 절대수익은 여전히 −(하락장 베타), 오른비율 낮음.
- **sm_a 트랙 신설**(lowvol_scores 공유, 가중치 0): lv_a 점수식 동일(저변동+ROE+반전), **유니버스만
  거래대금 1~5억 초소형**. uni 튜플 4번째 원소=유동성 상한(optional, 기존 3-튜플 모델 0-diff 호환).
  1억 미만 제외(거래 거의 불가). 3,450행/15run(lv_a 6,360보다 적음=좁은 풀). lv_a와 종목 거의 분리(겹침 140/652).
  - **초소형+저변동 결합**이 변동성·꼬리위험 완화(변동성 9.8→8.6%, 수익 유지). 핵심팩터=realized_vol 실측.
  - **checkup 첫 관측(OOS 1일, 가설)**: IC +0.161, **베타 0.68**(lv_a 1.01보다 훨씬 낮음!) — 초소형이
    대형주 시장과 덜 동조. "베타가 병목"(§17)의 잠재적 우회 단서 → forward 검증 가치 큼. 집중도 10%우위.
  - ⚠️ **실거래 제약**: 중앙값 거래대금 2.5억 → 소액만 가능(큰돈은 슬리피지로 못 삼). **관찰·소액 전용**.
  - ⚠️ post-hoc·하락장 in-sample → forward-only. 등록 20260627, OOS 40거래일(h20 위주). Bonferroni 분모 9.
  - **표시 안 함**(HTML·텔레그램 미노출, 순수 관측). checkup REG_DATE·order 반영 완료.
- **관측 체계 5개로**: lv_a(저변동)·lv_short(공매도)·hv_a(고변동)·**sm_a(초소형)**·mom_a(모멘텀).

---

## 21. B 방향 설계 — 모멘텀/주도주 트랙 (상승 포착) 청사진 (2026-06-27)

> **설계 문서**(아직 미구현). §20-6에서 확인: 현 파이프라인은 과매도 종목만 수집 → 모멘텀/가치 등
> "오르는 종목" 전략은 데이터 확장이 선행돼야 함. 효율·효과·확장성을 고려한 청사진. 구현은 별도 세션.

### 21-1. 문제 정의 (왜 B가 지금 안 되나)
- **현 데이터 한계**: stage1/stage3는 "과매도 게이트 통과" 종목만 적재. oversold_score 40~100.
  → 상승 추세주·신고가 근접·정상 우량주가 **데이터에 없음**. 모멘텀 IC를 재려 해도 표본이 과매도에 갇힘.
- **mom_a의 한계**: 현재 mom_a(sma20+모멘텀+거래량팽창)는 *과매도 유니버스 안에서* 모멘텀을 봄 —
  "덜 빠진 것 중 상대적 모멘텀"이지 진짜 "상승 주도주"가 아님. 진짜 주도주는 과매도 밖에 있음.
- **결론**: B = "과매도 밖 종목(전체 시장)을 별도 수집 → 모멘텀/추세 팩터로 점수화"가 본질.

### 21-2. 설계 원칙 (기존 시스템에서 배운 것 상속)
1. **완전 분리 (§13 lowvol·§B large 패턴 답습)**: 별도 유니버스·별도 점수·별도 테이블·별도 탭.
   v3·lowvol·large 코드/표시 0-diff. 새 트랙이 기존을 절대 안 건드림.
2. **관측 우선 (§14)**: 팩터 먼저 적재(가중치 0) → forward IC → 검증 후 가중. "돈 버는 모델" 확정 금지.
3. **수집과 계산 분리**: raw(시세·거래량)는 최대한 모으고, 점수·팩터는 그 위에서 나중에 자유 탐색.
   데이터 갭 있으면 '상상 팩터' 만들지 말고 명시 후 다음 단계로.
4. **포인트-인-타임 정직성**: 모멘텀은 룩어헤드에 특히 취약(미래 가격이 새면 IC 거짓 양수).
   시점 t 점수는 t 시점에 알 수 있던 가격/거래량만. backtest는 t+h 가격으로만 평가.

### 21-3. 아키텍처 (3계층, 확장성 핵심)
**계층 A — 유니버스 수집 (가장 중요·가장 큰 작업)**
- 현 `stage1`은 과매도 필터가 박혀 있음. **두 갈래 중 택1**:
  - (A-1) **별도 수집 스크립트** `universe_full.py` 신설 — 과매도 게이트 *없이* 전체 KOSPI/KOSDAQ
    일별 OHLCV+거래량 적재(FDR 또는 KIS). 새 테이블 `daily_ohlcv`(ticker,date,o,h,l,c,vol).
    → 가장 깨끗·확장성 최고. 모멘텀·매집(OBV)·가치 등 미래 모든 전략의 공통 raw 기반.
  - (A-2) stage1의 과매도 게이트를 플래그화(`--no-oversold-gate`)해 전체 적재 모드 추가.
    → 코드 적게 들지만 stage1 오염 위험, 0-diff 깨지기 쉬움. **A-1 권장**.
- **권장 = A-1**: `daily_ohlcv` 테이블이 B의 토대이자, 매집(OBV)·time-series 신호·장기 백테스트의
  공통 인프라. 한 번 만들면 여러 미래 실험이 재사용(확장성). 일 1회 증분 적재(없는 날짜만).

**계층 A 상세 — daily_ohlcv 스키마 (실측 점검 반영)**
> 철학: **완성품(RSI·이동평균) 저장 ❌, 원재료(OHLCV) 저장 ✅.** RSI·모멘텀·OBV·52주고저 등 stage3의
> 가격파생 45개 지표는 전부 OHLCV에서 *계산*됨 — 원재료만 두면 미래 어떤 계산도 재현(예: 14일 RSI로 바꿔도
> 즉석 재계산). 완성품 저장은 "그것만" 쓰게 가둠.
- **필수 컬럼**: `ticker, date, open, high, low, close, volume` (7개) + 아래 2개 = **실질 9개**:
  - **adj_close(수정종가) 또는 adj_factor(수정계수)** — ★가장 중요. 액면분할·병합·유상증자 시 가격이 점프
    (실측: 하루 ±30% 초과 41건, ±50% 초과 1건 — 상한가 30%인데 50%+ = 수정 미반영 의심). 수정 안 하면
    모멘텀이 거짓("−98% 폭락"으로 오인). FDR/KIS는 수정주가 옵션 제공 → 받을 때 켜기.
  - **market_cap(시총) 또는 shares(상장주식수)** — 종목 크기. 소형주 효과(sm_a)·시총가중에 필수.
    시총=당시 close×shares(포인트-인-타임, 현재 시총 금지 — §B 룩어헤드 원칙).
- **선택 컬럼(여유 시)**: `value(거래대금=close×volume, 유동성)`, `is_suspended(거래정지 플래그)`,
  `listed_shares 변동`. 거래대금은 volume×close로 계산 가능하니 저장 불필요(원재료 철학).
- **OHLCV로 안 되는 것(별도 소스, daily_ohlcv 밖)**:
  - **재무 27개**(ROE·실적YoY·영업이익·현금흐름): DART 전자공시, 분기 1회. 순수 가격모멘텀엔 불필요,
    "모멘텀+우량주" 결합 시만. 기존 stage 파이프라인이 이미 DART 수집 → 별도 테이블 재사용.
  - **수급 15개**(외인·기관·연기금·공매도): 이미 daily_flows·short_flows 보유. 신규 수집 불필요.

**계층 A 실무 함정 (모멘텀 백테스트 정직성 — 실측 근거)**
- **① 생존편향(survivorship)**: 종목이 중간에 사라짐(실측: 첫~끝 run 사이 110종목 소멸 = 상폐/거래정지/
  과매도탈출 혼재). 모멘텀은 "사라진 종목" 빼고 살아남은 것만 보면 수익 과대평가. → daily_ohlcv는 **상폐 종목도
  마지막까지 적재**하거나 시점별 유니버스 동결. 신규상장 종목은 상장일 이전 데이터 없음을 명시(룩백 부족).
- **② 시계열 구멍**: run이 **매 거래일이 아님**(실측: run 간격 1~3일, 주말·미실행 포함, 28run이 35일 구간).
  현 stage는 "돌린 날"만 존재 → 일봉 시계열에 구멍. **daily_ohlcv는 run과 무관하게 "매 거래일" 받아야**
  모멘텀(12개월·이동평균) 계산이 안 끊김. 수집 스케줄을 run과 분리(매 영업일 1회 증분).
- **③ 거래정지·가격멈춤**: 같은 가격 지속은 거래정지일 수도, run 간격 탓일 수도. is_suspended 플래그 또는
  volume=0 체크로 구분. 정지 종목은 모멘텀에서 제외(가짜 "안정" 신호 방지).
- **④ 거래비용·회전율**: 모멘텀은 회전율 높음(추세 추종은 자주 교체). 한국 비용(0.18%+세금) 반영 후 평가.
  특히 초소형(sm_a) 결합 시 슬리피지 큼.

**계층 B — 팩터/점수 (mom 트랙)**
- `mom_score.py`(lowvol_score.py 구조 차용): daily_ohlcv 위에서 모멘텀 팩터 계산.
- **후보 팩터(전부 관측 적재 가중0 시작)**: 12-1 모멘텀(12개월수익−1개월), 52주신고가 근접도,
  거래량 동반 추세, 이동평균 정배열, 변동성조정 모멘텀(샤프). → forward IC로 거름.
- 별도 테이블 `mom_scores`(모델 id·spec_hash 동결, lowvol_scores와 동일 패턴).

**계층 C — 표시/판정 (기존 인프라 재사용)**
- `build_mom_filter.py` → docs/mom.html(별도 탭). checkup.py에 mom 트랙 축 추가.
- 판정 §11 그대로: OOS 40거래일 + 상승장 표본. mom은 **상승장에서 빛나므로 레짐 분리 평가 필수**.

### 21-4. 단계별 로드맵 (효율 — 작게 시작, 검증하며 확장)
- **0단계(지금)**: 이 설계 동결. sm_a/lv_short/hv_a forward 누적 대기.
- **1단계**: `daily_ohlcv` 수집 구축(계층 A-1). 전체 종목 일별 OHLCV 적재 시작. **데이터만 쌓기, 분석 X.**
  (서버 구상과 연결 — 일봉은 매집·time-series에도 쓰이므로 최우선 인프라.)
- **2단계**: 1~2개월 쌓이면 `mom_score.py`로 모멘텀 팩터 forward IC 측정(research/ 출력, 게이트 면제).
  진짜 "과매도 밖 모멘텀"이 신호인지 확인. 동시에 매집(OBV)도 같은 daily_ohlcv로 탐색.
- **3단계**: 유의한 팩터만 mom_scores 관측 적재(가중0). docs/mom.html 표시.
- **4단계**: OOS 40거래일+상승장 후 §11 판정 → 가중 부여 or 폐기.

### 21-5. 확장성 설계 (미래 실험의 공통 기반)
- **daily_ohlcv = 단일 raw 토대**: 이 테이블 하나가 모멘텀·매집(OBV)·time-series 수축·장기 백테스트·
  섹터 모멘텀·신고가 돌파 등 **미래 모든 "과매도 밖" 전략의 공통 입력**. 한 번 구축=무한 재사용.
- **트랙 추가 패턴 정착**: lowvol(§13)→large→mom 으로 "별도 유니버스+점수+테이블+탭+checkup축"이
  반복 가능한 템플릿. 6번째 트랙도 같은 틀로. 모델 id·spec_hash·관측0·OOS40 규율 공통 상속.
- **수집 일원화**: KIS/FDR 수집을 `daily_ohlcv` 중심으로 모으면, 종목 유니버스가 트랙마다 달라도
  raw는 공유. 트랙은 raw 위 '뷰'. (현 kis_flows 수급도 장기적으로 이 패턴으로 흡수 가능.)

### 21-6. 리스크·정직성 체크
- **모멘텀은 룩어헤드·생존편향에 취약**: 상폐 종목 제외가 미래정보면 IC 거짓. daily_ohlcv에 상폐도
  포함하거나 시점별 유니버스 동결 필요.
- **거래비용**: 모멘텀은 회전율 높음(추세 추종은 자주 교체). 한국 비용(0.18%+세금) 반영 후 평가.
- **데이터 갭 정직**: 컨센서스·실적 서프라이즈(PEAD)는 별도 데이터 필요 — daily_ohlcv로 안 됨.
  지금 만들 수 있는 건 가격/거래량 기반 모멘텀까지. 그 너머는 명시하고 보류.
- **"상승장 표본" 함정**: 현 데이터 상승 run 4개. mom 트랙은 상승장 없으면 판정 불가 — 시간이 필요.

### 21-7. 구현 현황 — 1단계(daily_ohlcv) 완료 (2026-06-27 야간③)
> §21-4 로드맵의 **1단계 = 데이터 토대 구축, 완료.** 청사진→실물. 2단계(모멘텀 IC)는 forward 누적 대기.
- **소스 결정 = FDR(FinanceDataReader)**: 실물 시험으로 확정. DataReader가 OHLCV+Change 제공,
  **수정주가 기본**(2018 삼성 50:1 분할 실측 — 분할 전 가격이 5.3만원으로 조정됨, 점프 없음).
  StockListing(KRX 마스터)이 **Marcap·Stocks·Amount 직접 제공** → 거래대금·시총 계산 불필요한 수준.
  KIS 대비 백필 쉬움·rate limit 부담 적음. **KIS 시험 불필요(FDR 완비 확인)**.
- **`universe_ohlcv.py` 신설**(`ohlcv.db`는 **레포 바깥** `../dh-q7m3k-data/`에 격리): 전체 KOSPI/KOSDAQ 일봉 적재.
  - DB_DIR=`../dh-q7m3k-data`(코드는 루트 유지, 데이터만 바깥). `_connect()`가 폴더 자동생성.
  - 스키마: ticker·date·OHLC·volume·change_pct·**shares(포인트-인-타임 시총용)**·**is_suspended**·market.
  - `--backfill --years N`(백필) / 인자없음(증분, 최신일 이후만, 최근 7일 재확인 윈도) / `--status`.
  - 실무대책 구현: 생존편향(상폐·오류 종목 `ohlcv_skips`에 사유 기록), 거래정지(volume=0 → is_suspended=1),
    시계열구멍(FDR이 거래일만 주므로 자연히 영업일 기준, run과 분리), 유니버스 필터(6자리 숫자·KOSPI/KOSDAQ만).
- **3년 백필 완료(2026-06-27)**: **2645종목 · 1,847,790행 · 731거래일(2023-06~2026-06) · 270MB · 10분(629초).**
  - 품질 검증(오프라인): 종가/주식수 NULL 0, 음수가격 0, 고가<저가 모순 0. **무결성 완벽.**
  - 거래정지 2.4%(44,474행) — 정상 범위. 731일 전체정지 7종목(초장기정지, is_suspended로 자동 걸러짐).
  - 활발거래(정지율<10%) 2,529/2,645 = **96%** → 모멘텀은 이 풀에서 신호 탐색.
  - 시총 검증: 삼성전자 close×shares = 1,985조(실제와 일치). 포인트-인-타임 계산 정상.
- **데이터 위치 = 레포 바깥** `../dh-q7m3k-data/ohlcv.db`: git·핸드오프 **범위 밖**(막는 게 아니라 물리적 분리).
  - 이유: 270MB라 핸드오프(perf zip)에 넣으면 너↔Claude 전송 불가. + 재생성 가능(FDR 10분)이라 백업 불요.
  - **history.db 는 그대로**(레포 안, perf 핸드오프 대상 — 점수·관측은 재생성 불가라 보존 필수). ohlcv 만 바깥.
  - `.gitignore` 안전장치(`/ohlcv.db`·`/dh-q7m3k-data/`): 실수로 레포 안에 생겨도 추적 차단. make_handoff 무수정(범위 밖).
- **자동화**: `run_all_and_diversify.bat`의 `kis_flows.py` 다음에 `python universe_ohlcv.py`(증분) 추가 → 매 영업일 자동 누적.
- **다음(2단계, 1~2개월 후)**: 증분으로 forward 데이터 익으면 `mom_score.py`로 모멘텀 IC 측정(research/, 게이트 면제).
  **과거 3년 백필은 토대(룩백)용 — 정직한 forward 검증은 "지금부터" 쌓이는 데이터로**(과거 측정=in-sample 함정).
  매집(OBV)도 같은 ohlcv.db로 재개 가능(§20-6 보류분). ATTACH로 history.db와 조인.
- **오프라인 탐색 메모(2026-06-28, in-sample 2년 — 아직 mom_score.py 미구축, 가설일 뿐)**: ohlcv.db로 신호
  몇 개를 즉석 검증(forward 20d IC, 시장초과). **구축한 트랙 없음 — 아래는 다음 탐색이 헛수고 않게 남기는 메모.**
  - **신고가 근접 = "상승 포착"이 아니라 "하락장 상대강도" 신호**: 전체 IC +0.066이지만 장세별로 갈림 —
    **하락장 +0.148 / 횡보 +0.051 / 상승장 +0.015(≈0)**. 하락장에 시장이 다 빠질 때 신고가 유지 종목=진짜 강자라
    변별력 큼; 상승장엔 다 신고가라 변별력 없음. lv_a(저변동 방어)와 다른 각도의 방어 신호(상대강도). 시총 무관(소형·중형·대형 다 ★).
  - **단순이 최적**: 신고가 단독(+0.058) > +거래량(+0.044) > +모멘텀(+0.024, ★소멸). 보조신호 더하면 악화(lv_a 교훈 동일). 120일>60일>20일.
  - **상승장 신호는 못 찾음(갈래B 실패)**: 상승 시점 38개만 떼서 10개 신호 전부 검증 → 다 ≈0 또는 음수
    (20일모멘텀 −0.038★=평균회귀, 신고가 +0.012≈0). **상승장=베타 지배, 종목선택 무력**(§17의 또다른 모습).
    "상승량 키우기"는 종목선택으론 불가(타이밍/레버리지는 발굴 범위 밖). **데이터 한계**: 약상승 위주, 강세장 표본 적음.
  - **결론**: 종목선택 알파는 하락장·횡보장에 있음(상승장 아님). 신고가는 하락장 강자 신호로 가치 — mom_score.py 구축 시
    후보. 진짜 상승장 신호 재탐색은 **forward + 강세장 표본 쌓인 뒤**(지금은 어느 각도든 하락장 신호만 나옴).
- **저변동·섞기·수급 추가 탐색(2026-06-28, in-sample — 전부 잠정, 재측정 필요)**:
  - **저변동이 가장 견고한 단일 신호**: 전체 종목 IC **+0.107**(신고가 +0.065보다 셈). 측정 환경 바꿔도 항상 강함
    (전체/과매도, 5시점~1년, 700/2645종목 다 +0.1~0.24). 단 하락장 신호(상승 +0.04 / 하락 +0.26).
  - **lv_a는 전체 종목서도 상위권**(IC +0.073 ★, 신고가보다 높음): lv_a IC(과매도 한정)와 저변동 IC(전체)는 **모집단이
    달라 직접 비교 불가**(다른 문제 — 과매도 반등 vs 전체 안정주). lv_a는 "약한 모델"이 아님. 단지 반전 팩터가 과매도 전용
    (전체선 +0.011 소멸), 저변동은 보편적. → 저변동 전체 트랙은 별도 가능(단 forward 검증·하락장 신호).
  - **섞기는 저변동 단독을 못 넘음**: 저변동+신고가 등 모든 조합 ≤ 저변동 단독. 신고가↔저변동 **순위상관 +0.46(중복)**
    이라 더해도 새 정보 없음. "단순이 최적"(lv_a·sm_a 교훈 동일).
  - **수급(외인/기관/투신/연기금)·공매도·신용·대차 = 현재 판단 불가**: 저변동과 무상관(독립 정보 ✓)이나, **40거래일치(5~10시점)뿐이라
    부호조차 안 정해짐** — 투신·연기금이 5시점 측정 +0.221/+0.205 → 10시점 측정 −0.048/−0.085로 **부호 뒤집힘**.
    이는 "신호 없음"이 아니라 **"소표본이라 미결"**(§11: 40거래일 미만 노이즈의 실증 사례).
  - **★재측정 트리거(접는 게 아니라 보류)**: 위 수급/공매도 결론은 **전부 잠정**. daily_flows·short_flows가 **forward로
    더 쌓이면**(현 40일 → 목표 100일+, 시점 5개 → 20개+) **반드시 재측정**. 그때 투신·연기금이 양(+)으로 안정되면 저변동에
    더할 후보. **병목은 종목 수가 아니라 기간** — 전체 종목 수급 수집(+24분)으로도 시점은 안 늘어남(40일×2645=여전히 5시점).
    종목 확장보다 **시간 누적**이 우선. KIS 수급은 이미 매일 적재 중이니 자연 해결 — 1~2개월 뒤 재측정.
  - **메타 교훈**: 이번 세션 2회 — 신고가가 상승신호인 줄(→하락장 신호), 투신/연기금이 유망한 줄(→부호 뒤집힘) — **소표본의
    그럴듯한 IC에 속을 뻔.** 작은 표본 결과는 "발견"이 아니라 "재측정 대상". §11(40일·부트스트랩·다중검정)이 존재하는 이유.

### 21-8. 데이터 아키텍처 방향 — ohlcv.db = raw 허브, history.db = 점수 본진 (2026-06-28)
> 오늘 탐색하며 도달한 구조 통찰. ohlcv.db 가 "B 트랙용"에서 "전체 raw 분석 허브"로 진화. §21-5(공통 기반)의 구체화.
- **핵심 분업** (데이터 성격이 갈림):
  - **ohlcv.db 계열 = raw**(관찰된 사실, 가공 전): 전체 종목·긴 시계열·받으면 안 바뀜·분석/백테스트용.
    → 가격(OHLCV)·수급·공매도 등. **전체 데이터 확인·탐색은 ohlcv.db 가 본진.**
  - **history.db = 가공물**(계산된 결과): 과매도 걸러진 종목·매일 갱신·모델 바뀌면 재계산·실전 배포용.
    → stage1~3, v3_scores, lowvol_scores, large_final. 텔레그램·웹 배포. **점수 본진은 history 유지.**
- **왜 이 분업인가 (실증)**: 오늘 저변동(60일)·신고가(120일)·로테이션(2년)·OBV(고저거래량) 분석이 **전부 ohlcv.db라
  가능**했음. history.db stage1 은 전종목이지만 **price(종가)만·28일**(run 누적, 백필 없음)이라 이 분석 자체가 불가.
  → raw 분석엔 ohlcv.db(3년 OHLCV) 필수. 이미 증명됨.
- **history.db 도 전종목 종가는 있음**(stage1_oversold 2200종목, 과매도점수 0부터=게이트 전 전체). 단 **종가만·28일**이라
  분석용으론 부족. 과매도 점수 매기는 용도. (※ "stage1부터 과매도 게이트"는 부정확 — stage1=전체+점수, 거르는 건 stage2/3.)
- **수급·공매도 전체 종목 확장 방향(장기 관점)**: 
  - **논리**: KIS 수급/공매도는 **과거를 못 받음**(FDR 가격과 달리 백필 불가) → 지금부터만 쌓임 → **넓게 일찍 시작할수록
    미래 자산.** 늦게 전체로 늘리면 그때부터 또 1년 기다림(과거 영영 못 채움). ohlcv 3년 백필과 같은 "raw 일찍 쌓기" 논리.
  - **비용**: 일반모드(수급1+공매도1=종목당 2호출, 신용·대차 옵션 OFF). 700종목 13분 → **전체 2645종목 48분(+35분)**.
    파이프라인이 어차피 60~120분이라 +35분 감당 가능. (※ 신용·대차는 §20-6서 공매도와 중복 확인 → 제외 유지.)
  - **저장 위치**: raw 성격이라 **ohlcv.db 계열**이 일치(가격·수급·공매도 한 곳, 조인 편함). 단 기존 checkup 등이 history의
    daily_flows 참조 → 옮기면 그 코드도 수정 필요. **위치는 미결**(history 유지 vs ohlcv 이전) — 다음 세션 설계.
  - **주의**: ohlcv.db 에 수급 칸 만들어도 **과거는 빈칸**(오늘부터 채워짐). 통합이 기간을 만들진 않음 — 여전히 40→100일 대기.
- **장기 비전(미착수)**: 스크리너가 ohlcv.db 가격을 **재활용**해 과매도 점수 계산 → 가격 단일 원천(현재 stage1+ohlcv 중복 제거).
  단 스크리너 재배선 + 0-diff 검증 필요한 큰 작업. 지금은 ATTACH 조인으로 충분, 급하지 않음.

### 21-9. Phase 0~1 실행 완료 (2026-06-30)
> §21-8 구조개편을 단계 실행. Phase0=수집시간 단축(sleep), Phase1=수급/공매도를 ohlcv.db로 이전.
- **Phase 0 — KIS 호출간격 단축(sleep 0.1) ✅**:
  - **KIS 실전 한도 = 초당 20건**(공식 확인, apiportal). 신규고객 3일 제한은 가입 3일 지나 무관(기본유량 상향됨).
  - 기존 0.55초(초당1.8건)는 한도의 9%로 과보수. **0.1초(초당10건=한도 절반)로 단축** → 실측 실패 0(EGW00201 없음).
  - 전체 2536종목 수급+공매도: 65분 → **~20분**(수급11+공매도9.5). 타임아웃 1종목은 네트워크(rate 아님), 내일 윈도 자동보충.
  - `.bat` 반영: `kis_flows.py --universe all --sleep 0.1`. 파이프라인 ~1시간(스크리너30+large8+flows20) → **새벽 분리 불필요, 낮에 한 번**.
  - 0.05초(한도 100%)는 위험(순간초과)·타임아웃↑. **0.1초가 sweet spot**(마진 50%).
- **Phase 1 — 수급/공매도를 ohlcv.db로 이전 ✅ (0-diff 검증 완료)**:
  - **동기**: raw(가격·수급·공매도)는 ohlcv.db(전체·긴시계열·분석), 점수는 history.db. history 가벼움 유지(핸드오프).
    전체종목 flows를 history에 두면 1년 +165MB → 핸드오프 부담. ohlcv(핸드오프 밖)가 맞음.
  - **읽는 코드 3곳 파악**: ① `lowvol_score.py`(short_flows→lv_short 점수) ② `build_large_report.py`(daily_flows→표시,점수아님)
    ③ `large_score.py`(daily_flows 언급뿐 미사용). 셋 다 `con=connect(history)` 단일연결로 읽던 구조.
  - **수정(전부 폴백 유지 — ohlcv 없으면 history)**:
    · `lowvol_score.py`(+22줄): short_flows 를 ohlcv.db 에서 우선 읽고 없으면 history 폴백.
    · `build_large_report.py`(+16줄): ohlcv.db ATTACH → daily_flows 임시뷰로 노출(기존 쿼리 무수정).
      **+ NA버그 수정**: fnet/fmtc 의 `isinstance(v,float) and pd.isna(v)` 가 pd.NA(NAType, float 아님) 못 걸러 `float(NA)` 터짐
      → `pd.isna(v)` 단독으로(None·nan·NA 모두). ATTACH 데이터가 nullable 로 와서 드러난 **잠복버그**(원본도 터짐, Phase1 무관). 0-diff(정상값 동일, pd.NA만 "·").
    · `kis_flows.py`(+6줄): `--flows-db` 옵션 — 저장만 ohlcv 로 분리(종목목록은 여전히 history/ohlcv 읽기). 수집/저장 로직 md5 동일.
  - **이전 스크립트**: `migrate_flows_to_ohlcv.py`(1회) — history flows→ohlcv 복사(INSERT OR REPLACE), 검증 후 `--drop-source` 로 history 삭제.
  - **0-diff 검증 ✅✅**: lowvol_score 는 stage3+short_flows만 읽고 네트워크 0 → Claude 완전 검증 가능.
    history flows 삭제 + ohlcv 읽기 재계산 → lv_short 해시 `153b5a32...` = history 읽기 재계산과 **byte-identical**.
    (기준선 `64417632` 과의 차이는 공매도가 그새 갱신된 것 — ohlcv 이전과 무관, 같은 입력이면 같은 출력 증명됨.)
  - **적용 절차(사용자)**: ① `migrate_flows_to_ohlcv.py` 실행(복사+검증) ② 검증 OK 시 `--drop-source` 로 history flows 삭제
    ③ `.bat` 의 kis_flows 에 `--flows-db ../dh-q7m3k-data/ohlcv.db` 추가 ④ lowvol/large 는 코드상 자동으로 ohlcv 읽음.
  - **다음(Phase 2, 미착수)**: 스크리너가 ohlcv 가격 재활용(가격 중복 제거 → 추가 단축). 단 v2.6 엔진 수정+0-diff라 신중.

### 21-10. Phase 2 완료 — 스크리너가 ohlcv 가격 재활용 (2026-06-30)
> §21-8 구조개편의 마지막. 가격 단일 원천화: 스크리너가 fdr 대신 ohlcv.db 가격을 읽어 과매도 점수 계산.
- **동기**: 전엔 가격을 2번 받음 — ① 스크리너(fdr.DataReader 종목별) ② universe_ohlcv(fdr). 중복 제거.
- **수정(`screener_fdr_v2_6.py`, +51줄/-3줄)**:
  · 어댑터 `_get_prices(ticker, from, to)` 추가: ohlcv.db 우선 읽기 → fdr 형식(대문자 OHLCV, 날짜 인덱스) 변환. 없으면 fdr 폴백.
  · `analyze_ticker` 의 `fdr.DataReader` 한 줄만 `_get_prices` 로 교체. **점수 계산 로직(RSI~oversold)은 byte 동일(0-diff)**.
  · `time.sleep(REQUEST_DELAY)` 를 폴백(fdr) 시에만 — ohlcv 읽기는 sleep 0(네트워크 0) → 스크리너 가속.
  · 안전장치: `USE_OHLCV_PRICES=0`(env) 으로 즉시 fdr 롤백. `OHLCV_DB` 로 경로 지정.
- **`.bat` 순서 변경(필수)**: universe_ohlcv 를 스크리너 **앞으로**. ohlcv 최신일==오늘 보장(최신일 정합).
  순서: `universe_ohlcv → run_and_diversify(스크리너) → large → kis_flows`.
- **0-diff 검증(`verify_ohlcv_screener.py`)**: analyze_ticker 를 fdr경로/ohlcv경로 둘로 돌려 과매도점수+11지표 비교.
  · **결정적 발견**: 1차 검증서 30/30 불일치 → 원인=**ohlcv 최신일 1일 뒤처짐**(스크리너 to_date=오늘, ohlcv=어제). 가격오류 아님.
    return_1w 부호가 fdr+/ohlcv− 로 갈린 게 증거(그날 시장 상승 미반영). **universe_ohlcv 먼저 돌리니 30/30·200/200 0-diff 통과.**
  · 교훈: Phase2 는 fdr(네트워크) vs ohlcv 비교라 Claude 검증 불가 → **Jin 실측이 0-diff 보증**(Phase1 과 다른 점).
- **시간 효과**: 가격을 1번만 받음(전엔 2번). 스크리너 가격수집(수분) 사라지고 universe_ohlcv(5분) 앞에 추가. 순이득=중복분. **정확한 수치는 실측 필요(미측정)**.
- **남은 정리(Phase 3 후보, 급하지 않음 / 미착수)**:
  · `large_universe` 시총: fdr StockListing → ohlcv 의 `close×shares` 로 계산 가능(중복 제거). 단 0-diff 검증 필요.
  · StockListing(종목목록) 파이프라인서 3회 호출(universe_ohlcv/large_universe/diversify_picks) — 이득 작아 후순위.
  · `history_backup.db` 며칠 후 삭제. 스크리너 시간 실측 1회 권장.
- **핸드오프 갱신(`make_handoff.py`, +44줄) — Phase1 부수효과 대응**:
  · 문제: Phase1 으로 수급/공매도가 ohlcv 로 이전 → 전엔 history(perf zip)에 있어 IC 분석 가능했으나 **이제 perf zip 에서 빠짐**.
  · 해결: perf zip 생성 시 ohlcv 에서 `daily_flows·short_flows` 만 발췌한 `_flows_from_ohlcv.db`(~22MB) 동봉.
    가격(daily_ohlcv 185만행)은 제외 — 전체 295MB 는 핸드오프 부담. flows 만이면 OK. zip 후 임시발췌 자동 삭제.
  · 효과: 앞으로 `python make_handoff.py` 의 perf zip 만으로 lv_short IC·수급 분석 가능(ohlcv 통째 전송 불필요).
    가격 raw 가 필요한 특수분석(전체장 팩터 IC 등)만 ohlcv.db 별도 첨부.
  · (구 메모 "make_handoff 무수정" 은 폐기 — Phase1 후 flows 발췌 위해 수정됨.)
- **구조 점검 결과(2026-06-30)**: 인덱스(ohlcv daily_ohlcv 의 ticker/date — Phase2 쿼리가 탐) ✅ / DB 분리·크기(ohlcv 295MB·hist 가벼움) ✅ /
  OneDrive 동기화 ❌없음(경로 `C:\Users\SAMSUNG\Documents\...` 깨끗) → 구조적 추가 개선 불필요. 디렉토리 대청소는 위험>이득이라 하지 않음.

> **§21-8~10 최종 아키텍처(달성)**: ohlcv.db=raw 단일허브(가격+수급+공매도, 전체종목, 분석, 핸드오프밖) /
> history.db=점수 본진(stage·v3·lowvol·large, 매일갱신, 가벼움, 핸드오프). 가격·수급·공매도 중복 0, 역할 분리 완성.
> 읽기 방향: 스크리너·large·lowvol 이 ohlcv 를 읽어 점수 생성. 쓰기: universe_ohlcv(가격)·kis_flows(수급/공매도) 가 ohlcv 에 적재.
> 핸드오프: code zip(코드) / perf zip(history.db + ohlcv flows 발췌 + docs json). 가격 raw 필요시에만 ohlcv.db 별도.

---

## 22. 전체 종목(whole-universe) lv 계열 탐색 + 가격팩터 IC (2026-06-30 저녁)

**동기**: lv_a 가 "안 좋아 보인다"(상위가 다 많이 빠진 종목) → 원인 규명 + "전체 종목에서 점수 보기".
lowvol_score 의 lv 계열은 **과매도 선별(stage3) 340종목** 안에서만 점수. 이를 **ohlcv 전체 2600+종목**
모집단에 같은 점수식으로 적용해 보고, 가격팩터의 forward IC 를 과거 733거래일 전체에서 측정.

### 22-1. 새 탐색 스크립트 3종 (research/, 게이트 면제, 기존 트랙 불간섭)
- **`whole_score.py`** — lv 계열(lv_a~d, hv_a, lv_short, mom_a)을 전체 종목에 적용. 점수식·NaN 규칙·
  팩터 방향 전부 lowvol_score 와 일치(rank-sum, 핵심팩터 실측필수, 보조 0.5 중립). 모집단만 '전체'.
  가격팩터(저변동·낙폭·반전·sma20·mom·vol_exp)는 ohlcv.daily_ohlcv 에서 **직접 계산**(스크리너 식 그대로:
  drawdown=(price/52주최고−1), sma20=(price/SMA20−1), vol_exp=std5/std21). ROE=valuation EPS/BPS×100.
  출력 research/whole_score_{date}.csv (롱포맷 model×ticker). ohlcv READONLY.
- **`whole_ic.py`** — '단일 가격팩터'의 forward IC 백테스트. (시점,시장)별 cross-sectional Spearman,
  forward=시장초과수익(=종목수익−유니버스 중앙값). v3_backtest 와 동일 방식. signed_IC=IC×가설부호.
- **`whole_ic_combo.py`** — lv 계열 '조합(rank-sum) 뼈대'의 IC(ROE 제외 가격팩터만). 단일 저변동과 나란히 비교.

### 22-2. 데이터 가드 (전체 모집단 전용 — stage3 엔 없던 raw 쓰레기)
전체 ohlcv 는 raw 라 stage3(과매도 선별)엔 없던 것이 섞임. 3종 가드 필수(검증: 모의로 거래정지·점프 제외 확인):
- **거래정지 컷**: 윈도우 내 무변화일(수익률=0) 비율 > 50% → 제외. (가격 고정 → std≈0 → 가짜 저변동 상위 오염)
- **가격점프 컷**: 일중 |수익률| > 30%(상한가 초과) → 종목 제외. (액면병합·유증·신규상장 → 변동성 폭등)
- **변동성 하한**: realized_vol < 0.003 → 제외.
실측(6/30, 전체 2645): 거래정지 120 · 점프 63 · 변동성하한 98 → 177종목(6.7%) 제외. 첫 실행 때 거래정지
종목(012510)이 변동성 0.0018 로 lv_a rank 20 까지 올라온 걸 가드가 제거. **IC 정직성의 핵심**(거래정지는
forward 수익도 0 근처 → 안 거르면 가짜 저변동 신호로 IC 부풀려짐).

### 22-3. ★ 핵심 발견 — 가격팩터 IC (85시점, 전체 2647종목, in-sample)
| 팩터 | h5 | h20 | h60 | 판정 |
|---|---|---|---|---|
| **저변동(realized_vol)** | +0.090 | **+0.115** | **+0.126** | ◎ 압도적 최강 |
| **낙폭(drawdown)** | −0.042 | −0.066 | **−0.116** | × **역방향(전체장선 독)** |
| 반전(return_1w) | +0.014 | +0.011 | +0.006 | ~0 무의미 |
| 모멘텀(mom_1m)·sma20 | ~0 | ~0 | ~0 | 무의미 |

- **저변동 = 전체장 유일한 강신호**. h20 +0.115, h60 +0.126(IC>0 비율 0.80~0.85). 오늘 탐색의 +0.130 과
  일치 — 85시점으로 견고 재확인. 길수록 강함.
- **낙폭이 전체장선 반대**(h60 signed −0.116). 큰 낙폭 → 더 빠짐(고수익 아님). 단 **과매도 모집단(stage3)
  에선 낙폭 IC +0.129**(정반대). → **낙폭은 "이미 과매도된 종목 안에서만" 작동**하는 신호. 전체로 풀면 독.
  ("떨어지는 칼날"이 전체에선 진짜 칼날.) **모집단이 신호를 뒤집는다**는 핵심 교훈.

### 22-4. ★ lv 계열 조합 IC — 반전이 IC를 깎는다 (h20, ROE 제외 뼈대)
| 모델(뼈대) | h20 IC | 판정 |
|---|---|---|
| **lv_b (저변동)** | **+0.115** | ◎ 최강 |
| lv_a (저변동+반전) | +0.077 | ◎ 강함(lv_b보다 낮음) |
| lv_d (낙폭) | +0.066 | ◎ |
| lv_c (낙폭+반전) | +0.060 | ◎ |
| hv_a (고변동) | −0.066 | × 역방향(최악) |

- **반전(reversal) 추가 = 항상 IC 하락**: lv_b(+0.115) > lv_a(+0.077), lv_d(+0.066) > lv_c(+0.060). 예외 없음.
  → lv_a 가 "안 좋아 보이는" 진짜 이유: 반전이 빠진 종목을 끌어올리는데 **그게 신호를 깎는다**(보기도, IC도 나쁨).
  사용자 첫 직관("lv_a 안좋아 보임")이 수치로 증명됨.
- **저변동 단독(lv_b)이 모든 조합을 이김**. 가장 단순한데 최강. "단순이 최적" 재확인(신고가·수급·로테이션·
  반전·낙폭 다 더해도 순수 저변동 못 넘음 — §0/19/20 와 일관).
- **전체 종목 최적 = lv_b 뼈대(순수 저변동+ROE, 반전 제외).**

### 22-5. 한계 (정직성)
- **전부 in-sample**(과거 733거래일 전체, post-hoc). §11 기준 '강한 가설'이지 채택 아님. 진짜 확정은
  forward OOS 40거래일(오늘부터 whole_score 매일 쌓아야).
- **ROE 효과 미측정**: 전체종목 ROE(valuation)가 7시점뿐(6/22~6/30) → 룩어헤드 없이 ROE 포함 IC 불가.
  whole_ic_combo 는 ROE 제외 '가격 뼈대'만. 저변동+ROE vs 저변동만은 forward 로 별도 확인 필요.
- whole_score lv 의 **과매도판(lowvol_score) vs 전체판** 직접 IC 비교도 forward 누적 후.

### 22-6. lowvol.html 에 lv_b 비교 컬럼 추가 (관측 페이지)
- `build_lowvol_filter.py`: CSV 에 **lv_b_score·lv_b_rank** 컬럼 추가(lv_short 패턴 그대로, lv_a 점수·순위 0-diff).
  lv_b 는 이미 lowvol_scores 에 적재됨(17 run, 매일 자동) → 새 점수 불필요, build 가 읽기만.
- `docs/lowvol.html`: COLS 에 lv_b 열 2개 + 설명 추가(IC 결과 명시). 헤더 클릭 정렬 가능.
- **lv_a vs lv_b 는 같은 유니버스(과매도30~70+유동성5억) = 같은 340종목**. 점수식만 달라(반전 유무) **순위만 다름**.
  표에서 lv_a #2→lv_b #14(GKL) 처럼 순위 차 = 반전이 끌어올린(많이 빠진) 종목. 반전 효과가 눈으로 보임.
- ⚠️ 이 HTML 의 lv_b 는 **과매도 모집단(340)**. IC +0.115 짜리 lv_b 는 **전체 모집단(2600+)** — 모집단 다름.
  전체 종목 lv_b 를 HTML 로 보려면 whole_score 결과를 별도 페이지로(미착수).

> **§22 요약**: 전체 종목에선 **저변동이 유일 강신호(+0.115~0.126)**, **반전은 IC 를 깎고**(lv_b>lv_a),
> **낙폭은 과매도 안에서만 작동**(전체선 독). 최적=lv_b(순수 저변동). 단 in-sample·ROE제외 가설 →
> forward OOS 로 확정 필요. 탐색 스크립트(whole_score/whole_ic/whole_ic_combo)는 research/ 게이트면제,
> ohlcv READONLY, 기존 트랙 불간섭. lowvol.html 에 lv_b 비교 컬럼 추가(과매도 모집단).

---

## 23. wu 트랙 — 전체종목 관측 (2026-07-03 신설, 등록 20260702)

**목적**: 전체 상장(과매도 게이트 없음)에서 "시장초과+절대수익" 가설 검증. v3(중소형 과매도)·large(대형 가치)·
lowvol(과매도 저변동)과 **모집단부터 별개** — 점수·테이블·표시 완전 분리(신규 테이블 `wu_scores`).

- **모델(spec 동결, PREREGISTER_wu.md)**: `wu_a`=lv63+nh252+mom12+big(순위합, 핵심=lv63) — 균형·방어형 /
  `wu_b`=nh252+mom12 — 순수선택 대조(시총 무베팅). spec_hash: wu_a `6c146134b5a1`, wu_b `f09894fd2fcf`.
- **유니버스 가드**: rv21≥0.003, flat63≤0.5, 직전21일 |일수익|≤0.30, amt20≥5억, 정지 제외, 룩백 273거래일.
  전체 단일 순위(시장 통합). 원천 ohlcv.db(읽기)만 — 신규 API 0.
- **판정(§11 상속)**: h=20d 주지표, 등록(20260702) 후 **OOS 40거래일**, 날짜 블록부트 CI>0, 방향일치≥60%,
  **Bonferroni 분모 11**(lv 5종+mom_a+lv_short+hv_a+sm_a+wu 2종 — 등록 시 sm_a 누락 오기를 보수 방향으로 정정).
  부지표: top50 20d 절대·EW초과·적중(병행 기록, 단독 근거 아님).
- **발견 요약(in-sample 2024-07~2026-07, 전부 가설)**: 저변동=IC 최강이나 하위회피형(Q5초과≈0) /
  수익은 고점근접+12-1모멘텀 / 시총(big)이 구간 최강이나 **대형 독주 레짐 베팅 성격**(같은 기간 EW −15% vs
  CW +174%) — wu_b 대조와 OOS가 이걸 판별하는 목적. 상세: `research/RESEARCH_wholeuniverse_20260703.md`.
- **인프라**: `wu_score.py`(증분·idempotent·최초 실행=최신 1일=등록일·등록일 이전 백필은 --backfill-from만) /
  파이프라인 2.88(적재)·2.89(`build_wu_filter.py`→docs/latest_wu.csv, 이름=stage1+large+우선주 유도,
  업종=sector_cache+large_final 폴백, 수급=KIS daily_flows 5·20일) / `docs/wu.html`(경고 배너·wu_b 대조 컬럼) /
  텔레그램은 v3 푸터 링크(테스트 라벨) + `notify_wu_test.py` 수동. 표시는 판정 기준이 아님(골대 불변).
- **데이터 갭(명시)**: 이름 ~30종(초소형, 원천 부재)·업종 ~47종(시총상위500 밖)은 빈칸이 정상 — 상상으로 채우지 않음.

## 24. run_id 거래일 체계 + 이중실행 방어 (2026-07-03 사건, 2026-07-04 조치)

**사건**: 7/2 몫 배치가 7/3 새벽(00:21)으로 밀려 run_id=20260703 적재 → 같은 날 저녁(21:12) 정상 재실행.
stage 테이블은 같은 run_id **덮어쓰기**, lowvol_scores·v3_scores(동결)는 **append-only 스킵** → 새벽/저녁
유니버스 혼재: lv_a 60종이 stage3 밖(표시지표 전부 결측으로 가시화), v30@0703 frozen_at 2종(00:39 1230행 +
21:28 96행). 크기 게이트(유니버스<중앙값 30%)로는 미탐지(저녁분이 정상 크기).

**조치 4종(전부 오프라인 검증)**:
1. **run_id = 거래일 기준**(`accumulate_history.py`): 실행시각 <09:00 → 전일(주말은 금요일로 스킵).
   자정 넘김이 자동 보정됨. 수동 교정은 `RUN_ID_OVERRIDE=YYYYMMDD`. **한계**: 공휴일 캘린더 없음(순수
   날짜 계산) — 공휴일 다음날 새벽 실행은 라벨이 하루 어긋날 수 있음 → override로 교정.
2. **중복 실행 가드**(동일 파일): 이미 적재된 (market, run_id)는 정상 no-op 스킵(exit 0). 주말 새벽
   반복 실행(토·일·월 → 전부 금요일 id)의 충돌을 원천 차단. 의도적 재적재는 `FORCE_REINGEST=1` —
   이후 `python lowvol_score.py --run <id>` 재정합 **필수**.
3. **lowvol idempotent 재적재**(`lowvol_score.py --run <id>`): 그날 기존 행 자동 삭제 후 현재 stage3
   기준 재계산. 7/3 복구에 사용(lv 9모델 2913→3195행, stage3 밖 0, 타 테이블 0-diff).
4. **이중실행일 IC 게이트**(`compute_ic.py`): v3_scores에서 한 run_id에 frozen_at 2종 이상 → 판정서
   자동 제외(하드코딩 아님). 동결점수는 **사후 수정 금지** 원칙 유지 — 오염된 '하루'만 뺀다(§11 정신).
   현재 제외 대상: 20260703 (27→26거래일, 판정 결론 불변).

**전환 확인 포인트**: 다음 거래일 배치에서 run_id=20260706(월 종가) 신규 적재 + 주말 새벽 로그의
"중복 실행 가드 스킵" 메시지. **주의**: FORCE_REINGEST 재적재 후 lowvol --run 누락 시 7/3형 불일치 재발.
