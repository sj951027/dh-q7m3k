# PREREGISTER — wu 트랙 (전체종목 관측 모델 2종) — 골대 고정

- **등록일**: **20260702** — Jin 첫 실제 실행(2026-07-03 수동 최초 적재)의 run_id. 이후 파이프라인
  갭필로 20260703부터 매일 누적. (같은 날짜의 Claude 샌드박스 검증분은 사본 실행이라 등록과 무관.)
- **발견기간(in-sample, 판정서 제외)**: 2024-07 ~ 2026-07-02. 근거: RESEARCH_wholeuniverse_20260703.md.

## 모델 (spec 동결 — 변경 시 새 model_id)
| model_id | spec_hash | 정의(순위합, 첫 팩터=핵심·실측필수, 보조 NaN=0.5) | 성격 |
|---|---|---|---|
| `wu_a` | `6c146134b5a1` | lv63 + nh252 + mom12 + big | 균형·방어형 |
| `wu_b` | `f09894fd2fcf` | nh252 + mom12 | 순수선택 대조(size 무베팅) |

- 팩터 동결: lv63=std(일수익,63,min30) / nh252=close÷252d최고−1(min120) / mom12=close[t−21]÷close[t−252]−1 / big=log10(close×shares). 순위=가드 유니버스 **전체 단일 순위**(시장 통합), ties=average.
- 유니버스 가드: rv21≥0.003, flat63≤0.5, 직전21일 |일수익|≤0.30, amt20≥5억, is_suspended=0, 당일 종가 존재. 룩백 273거래일 미달 날짜 스킵. 과매도 게이트 없음.
- 적재: history.db 신규 테이블 `wu_scores`(PK run_id·market·ticker·model_id, append-only, wu_rank 포함).
  최초 실행=최신 1일(그날=등록일). 이후 등록일 이후 갭 자동 보충. 등록일 이전은 `--backfill-from`(연구용)만.

## 판정 (§11 상속)
- **주지표**: h=20d cross-sectional Spearman IC(시장초과와 동치). 등록 후 **40거래일 OOS 미만 = 노이즈**.
- 통과 조건: 날짜(블록)부트스트랩 95% CI > 0 **그리고** 주간 방향일치 ≥ 60% **그리고**
  Bonferroni 통과 — **분모 11** (lv_a~d·lv_a3 5 + mom_a + lv_short + hv_a + **sm_a** + wu_a + wu_b). 경계값='기움'.
  *[정정 2026-07-04]* 등록 시 sm_a(초소형 관측, 2026-06-27 등록 §20-6) 누락으로 분모 10 오기 → 11 교정.
  검정이 **더 엄격해지는 방향**의 오류 교정이므로 골대 이동 아님.
- **부지표(병행 기록, 단독 채택근거 아님)**: top50 20d 절대수익·EW초과(가드 유니버스 동일가중)·적중률.
  CW(시총가중) 수익 참고 병기. 판정 시 전방 |일수익|>0.32 종목-시점은 데이터오류로 제외.
- wu_a vs wu_b 우열 판정도 동일 OOS로만(in-sample 우위는 wu_a — '기움'일 뿐).

## 불변·금지
- 가중치 0 관측. v3·large·lowvol 점수/테이블/표시 **0-diff**(검증 완료: 기존 8테이블 해시 전후 동일,
  신규 wu_scores만 추가, 재실행 idempotent, 최신일 wu_a top5 점수가 연구 스냅샷과 일치).
- **[표시 조항 개정 2026-07-03, 소유자 결정]** 판정 전 표시는 lv_a 패턴의 **테스트 노출**로 한다:
  전용 `docs/wu.html`(경고 배너 필수) + v3 텔레그램 **푸터 링크**('테스트·관측데이터, 검증 전' 라벨)
  + `notify_wu_test.py`(수동 미리보기). 표시 여부는 판정 기준이 아니며 **spec·판정 조항은 불변**
  (골대 이동 아님 — 노출 형식만 결정). 오버레이(추세/변동성 필터)는 본 등록 불포함 — checkup 관측만.
- 판정 전 기준 변경 금지, 사후 파라미터 탐색으로 본 spec 대체 금지, 자동매매 연결 금지.

## 운영(1줄 통합)
`.bat`에서 **universe_ohlcv 이후** 아무 지점에 `python wu_score.py` 1줄(비치명). ohlcv 최신일==당일
정합 필요(§21-10 교훈). run_and_diversify 2.88 단계 통합은 선택(원하면 다음 세션에 패치+0-diff).
