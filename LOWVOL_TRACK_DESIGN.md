# LOWVOL_TRACK 설계노트 — 저변동·우량·반전 트랙 (dh-q7m3k)

> **용도**: dh-q7m3k의 **세 번째 트랙** 설계노트. v3(과매도 중소형)·large(대형 가치)에 이은
> 신규 트랙. PROJECT_KNOWLEDGE.md / LARGE_SCORE_DESIGN.md 를 먼저 읽고 이 문서를 본다.
>
> **상태**: 1단계 코드 완성·오프라인 검증 완료(§10). 다음 = 사용자 실행 → forward 누적.
>
> **출발점**: 18거래일(0602~0624) history.db 오프라인 분석에서 v3의 한계를 추적하다 발견한
> 신호 3종(저변동·단기반전·ROE). **모두 in-sample 관측 = 가설이지 검증된 엣지 아님.**
> §11(40거래일) / 이 트랙 §9(호라이즌 분리) 전엔 점수에 가중 금지.

---

## 0. 한 줄 정의
v3가 "과매도 반등 사냥"이라면, lowvol_track은 **"적당히 빠진 우량주 중 변동성 낮은"** 종목을
**호라이즌을 신호별로 분리**해 점수화하는 트랙. v3·large와 데이터/인프라만 공유, 점수·유니버스·표시 분리.

---

## 1. 왜 만드는가 — v3 진단에서 나온 근거 (18거래일, 노이즈 구간 단서)

오프라인 history.db 분석에서 확인된 사실(전부 n·IC 동반, but 18일=노이즈):

| 사실 | 수치 | 함의 |
|---|---|---|
| v3 추천(BUY+WAIT) 5일 절대수익 | −4.47% (상승비율 28%) | 절대수익 칼날 — 단 시장 자체 −2.3% |
| v3 추천 5일 **시장초과** | BUY +2.41%p (CI 1.6~3.3, 0 위) | **상대로는 옳은 방향** — 나쁜 것 걸러내기 작동 |
| realized_vol 단변량 IC(h5) | **−0.227** (n=11365) | 저변동일수록 5일수익 높음 — 가장 강한 신호 |
| return_1w 단변량 IC(h5) | **−0.215** | 단기반전(학술 일치: Jegadeesh 1990) |
| roe_value 단변량 IC(h5) | **+0.115** | 변동성과 독립인 유일한 질적 신호 |
| **return_1w IC(h20)** | **+0.090** (부호 반전!) | 반전은 단기현상, 20일이면 모멘텀으로 뒤집힘 |

→ **핵심 교훈 2가지:**
1. **신호별 적정 호라이즌이 다르다.** 반전=5d(짧게), 저변동·ROE=20d+(길게). 한 점수에 같은
   호라이즌으로 묶으면 깨진다(지난 "AND 결합 실패"의 근본 원인).
2. **현재 v3 점수와 음의 상관인 정보(저변동)에 엣지가 있다.** v3가 안 쓰거나 거꾸로 쓰는 축.

학술 근거(웹검증): 단기반전은 30년 문헌의 확립된 이상현상(Jegadeesh 1990 월 ~2%, Lehmann 1990
주간). 단 **반전 수익 상당부분은 유동성 공급 프리미엄(Nagel)** → 저유동 소형주일수록 크지만
거래비용에 먹힘 → **유동성 하한 필수**. FSCORE(펀더멘털) 일치 반전이 더 우수 → ROE 결합 정당.

---

## 2. 유니버스 — v3와 무엇이 다른가 (데이터로 확정)

| | v3 | lowvol_track |
|---|---|---|
| 과매도 게이트 | **≥40 필수** | **중간대(30~70) 타깃, 극단 제외** |
| 이유 | 낙폭과대 진입 | 극단 과매도(70+)는 반전 IC 반토막(−0.287→−0.124) = 칼날 |
| 핵심축 | 낙폭+턴어라운드+단기수급 | **저변동 × 우량(ROE) × (단기반전 트리거)** |
| 금융주 | 제외 | **일단 포함**(저변동·배당 친화), 검증 후 판단 |
| 변동성 | 무관(과매도=고변동 경향) | **저변동을 메인축** — v3와 정반대 |

- **유니버스 크기**: 중간 과매도(30≤oversold_score<70) = 일평균 691종목(min 512 / median 668
  / max 900, 18일 실측). 충분히 넓음.
- **realized_vol vs oversold_score 상관 +0.028**(≈0) → 게이트가 저변동을 역선택하진 않음.
  게이트 유무는 신호 죽이기보다 **모집단 크기** 문제. 그래서 "완전 제거" 아닌 "중간대 타깃".
- **포인트-인-타임**: 유니버스 편입도 그날 oversold_score 기준(룩어헤드 차단).

---

## 3. 데이터 인벤토리 (전부 stage3_final/stage1에 이미 있음 — 신규 호출 0)

| 재료 | 컬럼 | 출처 | 상태 |
|---|---|---|---|
| 실현변동성 | `realized_vol` | stage3_final | ✅ (observe_vol 적재, 커버리지 ~43%) |
| 단기반전 | `return_1w_%` | stage3_final | ✅ 100% |
| 우량 | `roe_value` | stage3_final | ✅ (자본잠식 NaN) |
| 유동성 하한 | `amt_avg_1m_억` | stage3_final | ✅ — 반전 유동성 함정 방어 필수 |
| 과매도 강도 | `oversold_score` | stage3_final | ✅ — 유니버스 게이트용 |
| 가격(PIT 수익) | `price` | stage1_oversold | ✅ run별 종가 |

**갭 없음.** large 트랙과 달리 신규 데이터 소싱 불필요 → 1단계 착수 즉시 가능.
단 `realized_vol` 커버리지 43%(과매도 패널 한정·신규편입 pending) — 적재 시 NaN 처리 명시.

---

## 4. 점수 설계 v1 — **순위합 4모델 (2×2 대조)** [검증 완료, 코드 lowvol_score.py]

### 핵심 교훈 1: AND 아님, 순위합
지난 실패(저변동·반전 AND 결합 → 절대수익 악화)의 교훈:
- 저변동(안정)과 반전(폭락 후)은 **AND 교집합 시 모순** → "지난주 폭락한 안정주" = 추가하락
- **순위합(점수 가산)이면 모순 없음** — 한쪽 약해도 다른 쪽 보완.
- **데이터 교정**: 설계 초안의 "반전을 점수에서 빼고 트리거로만"은 **과잉반응이었음.**
  순위합으로 반전을 더하면 IC 0.173→0.199(전체), 0.191→0.226(좁은 유니버스) 상승.
  → **반전을 점수에 포함**(단 h=5d 단기 보유 전제).

### 핵심 교훈 2: 핵심 팩터 NaN은 채우지 마라 (코드 버그로 발견)
초안은 NaN 팩터를 0.5(중립) 채움 → realized_vol 없는 종목까지 "저변동 트랙"에 섞여
IC 0.199→0.119로 망가짐. **수정**: 모델의 핵심 팩터(첫 팩터)는 실측 필수(NaN=제외),
보조 팩터만 0.5 채움. 결과 IC 정상화(lv_a 0.226).

### 4모델 + 변형 (2×2 핵심 + 미세조정 관찰)
| model_id | 핵심팩터 | 구성(순위합) | 유니버스 | 커버리지 |
|---|---|---|---|---|
| `lv_a` | realized_vol | 저변동 + ROE + 반전 | 과매도30~70+유동5억 | ~43% |
| `lv_b` | realized_vol | 저변동 + ROE | 과매도30~70+유동5억 | ~43% |
| `lv_c` | drawdown | 낙폭 + ROE + 반전 | 과매도30~70+유동5억 | ~100% |
| `lv_d` | drawdown | 낙폭 + ROE | 과매도30~70+유동5억 | ~100% |
| `lv_a3` | realized_vol | 저변동 + ROE + 반전 | **과매도30~60**+유동5억 | ~43% |

**lv_a3 = 미세조정 관찰 모델** (2차 견고성 검증서 추가): lv_a와 점수식 **완전 동일**, 유니버스만
과매도 상한 70→60으로 좁힘. 민감도 분석 근거(상한 좁힐수록 IC↑: 70→0.226, 60→0.234, 50→0.246,
단 n 트레이드오프). **원본 lv_a 점수는 0-diff 불변**(코드 검증: 점수 최대차 4e-16). 좁힌 게
forward(OOS)서도 나은지 lv_a와 나란히 관찰. (lv_a2=과매도 하한 제거안은 유동성 5억이 이미
저과매도를 걸러 **원본과 0-diff** → 무의미해 제외. "하한 30 불필요" 검증으로 갈음.)

- z_rank = run 내 cross-sectional 백분위(PIT 안전, 그날 정보만). 부호: 저변동/지난주패자=
  작은값이 좋음(asc=False), ROE/낙폭=큰값이 좋음(asc=True). **코드에서 부호 검증 완료.**
- **유니버스**: 중간 과매도(30≤oversold<70) + 유동성 하한 **amt_avg_1m≥5억**(분포 근거:
  중간대 중앙값 10.4억의 절반, 하위25%=2.8억 위. 일평균 439종목. 매직넘버 아님).

### 검증된 IC (좁은 유니버스 내, h=5d 시장초과, 날짜부트 CI) — 전부 가설(18일)
| 모델 | IC5 (95%CI) | IC20 | 관전 |
|---|---|---|---|
| lv_a 저변동+ROE+반전 | **0.226** (0.14~0.29) | 측정불가(n0) | 단기 최강 |
| lv_b 저변동+ROE | 0.191 (0.09~0.27) | 측정불가(n0) | |
| lv_c 낙폭+ROE+반전 | 0.142 (0.11~0.17) | 0.169 | 반전이 오히려 해 |
| lv_d 낙폭+ROE | 0.158 (0.11~0.21) | **0.20** | 장기 유일 검증가능 |

**2×2가 드러낸 사실:**
1. **저변동 > 낙폭**(h5): 단기엔 저변동이 강함.
2. **반전 효과는 저변동에서만 +**(lv_a>lv_b), 낙폭에선 − (lv_d>lv_c). 상호작용 존재.
3. **h=20d는 낙폭(lv_d)만 측정 가능**, IC20 0.20. **realized_vol 커버리지 한계로 저변동의
   장기 검증 불가** → 장기 보유면 현재 lv_d가 유일 후보. forward로 realized_vol 쌓여야 lv_a/b 장기 판정 가능.

### 컴포넌트별 적정 호라이즌 (검증 시 분리 평가)
| 컴포넌트 | 평가 호라이즌 | 근거 |
|---|---|---|
| realized_vol | h=20d 목표(현재 데이터 부족) / h=5d 가능 | 저변동 길게 발현 |
| roe_value | h=20d | 질적 신호 |
| return_1w(반전) | **h=5d만** | h20 부호 반전(−0.21→+0.09) |
| drawdown | h=5d·h=20d 둘 다 | 커버리지 100%, 양 호라이즌 견고 |

### 제외한 후보 (가산가치 없음 — 검증)
낙폭·OCF·1개월모멘텀은 단변량 신호이나 저변동+ROE에 **더해도 IC 안 오름**(중복정보):
4종합 0.164, +OCF 0.162, +모멘텀 0.157 < 저변동+ROE+반전 0.199. → **3팩터가 스위트스폿.**

---

## 5. 불변 규칙 (v3·large 상속 + 트랙 특화)
1. **관측 팩터는 점수식에 안 들어감**(검증 전 가중치 0). — 상속
2. **v3·large 엔진/테이블/표시 절대 안 건드림.** lowvol은 별도 테이블 `lowvol_final`(또는 v3 내
   새 model_id)·별도 점수·별도 탭으로 완전 분리.
3. **매직넘버 금지**: 유동성 하한·과매도 컷 = 분포→데이터로. 임의 하드코딩 금지.
4. **신호별 호라이즌 분리 엄수**: 반전을 20d로 평가하지 않는다(부호 반전).
5. **백테스트 PIT**(large §8 상속): 가격=당시 price, 신호=그날 stage3 값만.
6. **post-hoc 정직성**: 이 신호 3종은 v3 진단 중 **사후 발견** → forward-only, 발견기간(≤0624)
   in-sample 수치는 가설. 등록일 이후 OOS로만 판정(PROJECT §11-A 상속).
7. **출력 동일성/오프라인 검증 우선**: 점수·동작 변경은 history.db "결과 0 diff" 증명 후.

---

## 6. 검증 기준 (사전등록 — 골대 고정)
- **관측 → IC 검증 → 그다음 가중.** 검증 전 어떤 컴포넌트도 추천에 노출 안 함.
- **호라이즌**: 컴포넌트별 분리(§4 표). 종합 점수는 h=20d 주력(저변동·ROE 베이스라서).
- **판정 시점**: 등록일 이후 **OOS 40거래일**(v3 §11 준용). 그 전 노이즈.
- **채택 조건**: 부트스트랩 95% CI(날짜 리샘플) 0 초과 + 주별 방향 60% + 유동성 하한 후에도 유지.
- **표본/거래비용**: 유동성 하한 통과분만 평가(못 먹는 소형주 반전 제외). 한국 거래비용·세금 반영.
- **무판정도 결론**: 40일 차도 기준 미달이면 **노출 안 함**(정상). v3·large와 동일 철학.

---

## 7. 아키텍처 — 인프라 공유, 머리 분리
- **공유**: stage1_oversold·stage3_final·price·history.db·대시보드·텔레그램·git push.
- **분리(신규)**:
  - `lowvol_universe.py` — 중간 과매도(30~70) + 유동성 하한 필터로 유니버스 생성
  - `lowvol_score.py` — 저변동·ROE 순위합 + 반전 트리거 플래그, **관측 적재(가중치 0)**
  - 새 테이블 `lowvol_final`(별도. stage3_final 재활용 X — 유니버스·컬럼 다름)
  - 표시: 검증 후 "저변동" 탭 또는 docs/lowvol.html
- 검증: 챔피언/챌린저 틀 재사용(컴포넌트별 호라이즌 분리 평가).

---

## 8. 단계별 로드맵
| 단계 | 내용 | 상태 |
|---|---|---|
| **0** | 설계노트 확정 + 데이터 갭 확인 | ✅ 완료(갭 없음) |
| **1** | `lowvol_score.py` 4모델 관측 적재(가중치 0) + 오프라인 검증 | ✅ 코드 완성·검증(§10) |
| **2** | 사용자 실행 → forward 누적. 40거래일 후 컴포넌트별 IC 검증 | ⏳ 사용자 실행 대기 |
| **3** | 검증된 모델만 가중 → 순위 확정 → "저변동" 탭 노출 | 추후 |

**1단계 운영**: `.bat`에 `python lowvol_score.py`(증분) 한 줄 추가. 또는 단독 실행.
최초 백필은 `python lowvol_score.py --full`(이미 오프라인서 1회 실행됨, 32856행).

---

## 9. 검증 내역 (오프라인, 이 repo history.db)
- **적재 동작**: `--full` 36178행(lv_a/b 각 5646·13run, lv_c/d 각 10782·25run, lv_a3 3322·13run).
  핵심팩터 커버리지 정직 반영(저변동 43% → lv_a/b/a3 행수 적음).
- **부호 검증**: 점수↑=초과수익↑(전 모델 IC 양수). rank ascending 부호 버그 1건 발견·수정.
- **NaN 결함 수정**: 핵심팩터 NaN 채움이 IC 0.199→0.119로 망침 → 핵심팩터 실측 필수로 수정.
- **PIT 안전**: score_run이 단일 (run,market) 입력만 받고 미래 미참조 → 구조적 룩어헤드 불가.
- **증분 모드**: 재실행 시 신규 0행. 모델별 spec_hash(원본 4종 + lv_a3 1종 = 5종).
- **타 트랙 불변**: v3_scores·large_final·stage3_final 전부 불변(불변규칙 §5-2).
- **변형 0-diff(2차)**: lv_a3 추가 시 원본 lv_a~d 점수 **최대차 4e-16**(=0), 키 완전일치. 변형이
  원본 미오염 확인. (spec_hash는 전역→모델별 계산으로 정밀화돼 값 자체는 바뀜 — 점수 불변이라 실질 0-diff.
  도입 시 `--full` 1회 권장.)
- **2차 견고성 검증**: lv_a가 시장분리(코스피 0.251/코스닥 0.211)·시간분할(전반 0.242/후반 0.226)·
  단조성(Q1~Q5 −1.02→+2.13 무결점 계단)·유니버스민감도(하한·컷 흔들어도 0.2~0.25) **전부 최상위 통과**.
- **미검증(사용자 1회 확인)**: Windows 경로 실행, `.bat` 통합. 적재는 stage3만 읽어 신규 API 0.

---

## 10. Claude 협업 메모 (이 트랙 한정)
- 네트워크 없음 → 적재는 사용자 실행, Claude는 history.db·zip 오프라인 검증.
- **관측 적재 단계.** "돈 버는 모델"로 확정 금지. 18일 in-sample은 가설.
- 데이터 갭 없으니 "상상 속 팩터" 위험 낮음 — 그래도 임의 가중·매직넘버 금지.
- PIT를 코드에 구조적으로 박음(단일 run 입력). cross-sectional 순위는 run 내에서만.
- **반전 순위합 포함(§4 교훈1)·핵심팩터 실측필수(§4 교훈2)가 이 트랙 핵심 설계** — 흔들지 말 것.
- **realized_vol 커버리지(43%)가 저변동 장기검증의 병목** — forward로 쌓일수록 lv_a/b 평가력↑.
