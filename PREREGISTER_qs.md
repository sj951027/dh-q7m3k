# PREREGISTER — qs_a(조용한 강자) 관측 모델 (2026-07-22 동결)

> wu 트랙(전체종목, wu_scores 테이블)에 추가되는 **가중치 0 관측 모델 1종**.
> 발견은 전부 post-hoc(in-sample, 2023-06~2026-07 백테스트) → **첫 적재일(REG_DATE, 예상
> 2026-07-23)부터의 forward 데이터만 §11 판정에 사용.**
> 표시·추천·텔레그램 사용 금지(판정 전). 이 문서 이후 spec 변경 금지 — 바꾸려면 새 model_id.

## 1. 출처 (발견 경위 — 판정에는 사용 불가한 in-sample 근거)

- `factor_scan.py` 전 구간 스캔(2026-07-22): ohlcv.db 748거래일, 보통주 2,559종목,
  PIT(ENTRY_LAG=1, JUMP_CAP=0.32), 날짜×시장 Spearman IC, 블록 부트스트랩 CI, 3분할 폴드.
  **다중검정 원장: 총 44회 시도**(가격 26 + 공매도 10 + 수급 6 + 결합 2).
- 단일 팩터(n=475~490일, 폴드 3/3 일관·CI 0 제외):
  저변동 vol_60 h20 IC **−0.121** CI[−0.159,−0.059] · 52주고가근접 dist_52wH **+0.051**
  CI[+0.010,+0.079] · 저거래대금 logval_20 h5 **−0.052** CI[−0.078,−0.025].
- 결합(조합 탐색이 아니라 위 3개의 사전 선언 1건): rank 평균 `quiet_strength`
  h20 IC **+0.107** CI[+0.040,+0.147] 폴드 [0.115, 0.080, 0.124] · h5 +0.083 CI[+0.052,+0.113]
  · 상위−하위 20% h20 스프레드 +1.82% CI[−0.005,+0.033](**0에 걸침 — 관전 포인트 ③**)
  · 상위분위 승률(시장중앙값 대비) 58%.
- 참고: 순열 널분포(1000회) 기준 운 상한(IC 최대 +0.03~0.06) 대비 큰 여유. 기각된 팩터(고전
  모멘텀·단기반전·거래량급증)와 데이터 갭(credit_bal_rate 6종목뿐)은 factor_scan JSON에 기록.

## 2. 동결 스펙 (wu_score.py MODELS/FACTORS와 1:1)

공통: wu 트랙 유니버스·가드 그대로(전체 상장, rv21≥0.003 · flat63≤0.5 · jump21≤0.30 ·
amt20≥5억 · 미정지 · 종가 존재). 순위합 = cross-sectional pct rank, 핵심(첫 팩터) 실측필수
(NaN=제외), 보조 NaN=0.5. 저장: history.db `wu_scores` (model_id='qs_a').

| model_id | 팩터(순서=핵심 먼저) | 방향 | spec_hash |
|---|---|---|---|
| **qs_a** | lv63 = std(pct_change(close),63) | ↓ (저변동 우대) — 기존 팩터 재사용 | `77ece1338941` |
| | nh252 = close/rolling_max(close,252)−1 | ↑ (고가 근접 우대) — 기존 팩터 재사용 | |
| | amt20l = mean(close·vol,20)/1e8 | ↓ (저거래대금 우대) — amt20f 동일 프레임의 방향 반전 | |

- 스캔에서는 vol_60/dist_52wH/log(amt20)을 썼으나 동결 스펙은 wu 트랙 기존 팩터 정의
  (lv63·nh252·amt20 원값)를 재사용한다 — 창 길이 60→63, log 유무는 랭크에 단조 동치(스펙의 일부).
- 스캔은 유동성 가드 없이 발견 → 실적재는 amt20≥5억 가드 하에서 이뤄짐(관전 포인트 ②).
- 신규 모델은 기존 run_id(≤20260722)에 소급 적재하지 않음 — 다음 신규 run부터 자연 시작.

## 3. 판정 규칙 (§11 그대로)

- **h=20d IC 주지표, h=5d 보조.** OOS ≥ 40거래일 전까지 무조건 '노이즈'.
- 부트스트랩 CI(2,000) > 0 · 방향일관성 ≥ 60% · Bonferroni(wu 트랙 동시검정 모델 수 = 5).
- 관전 포인트(사전 서약): ① 3년 in-sample 우위가 forward에서 재현되는가
  ② amt20≥5억 가드 하에서도 저거래대금 성분이 기여하는가
  ③ 스프레드 CI가 0에 걸쳤던 점 — IC(랭킹력)만이 아니라 실전 수익 크기가 나오는가.
- 판정 예상 시점: 등록(예상 20260723) + 40거래일 ≈ **2026-09 중순**.

## 4. 불변 확인 (2026-07-22 오프라인 검증 완료)

- **구코드 vs 신코드, 같은 입력 직접 비교**: history.db 사본 2개에서 20260722 삭제 후 각각
  구/신 코드로 재적재 → 기존 4모델 4,476행 **완전 동일**(equals=True).
- 기존 spec_hash 불변: wu_a `6c146134b5a1` / wu_b `f09894fd2fcf` / le_a `29cb040142b0` /
  sv_a `e38efdfc260d`.
- 라이브 DB 대비 재계산 차이는 sv_a에만 존재(max 0.17) — 원인은 코드가 아니라 **적재 시각**
  (wu_score 20:49 → short_flows 21:02 적재). PREREGISTER_le_sv에 동결된 '직전 적재분 사용'
  스펙 그대로이며, 구코드도 동일하게 재현됨. 가격 전용 모델(wu_a/wu_b/le_a)은 max diff 4e-16.
- idempotence: 신코드 재실행 → "[증분] 신규 적재 대상 없음".
- qs_a 스모크(사본, run 20260722): 1,121행 적재, 점수범위 0.231~2.867, wu_rank 정상.
- build_wu_filter.py 는 MODEL="wu_a" 하드코딩 — qs_a 는 docs/텔레그램 어디에도 노출 안 됨.

## 5. 배포 절차 (사용자 실행)

1. repo의 `wu_score.py`를 이 수정본으로 교체(다른 파일 변경 없음).
2. 다음 일일 파이프라인이 돌면 qs_a 첫 적재 → 그 run_id가 REG_DATE.
3. 첫 적재 확인 후 `checkup.py` REG_DATE에 한 줄 추가: `"qs_a": "<첫 run_id>",` (예상 20260723).

## 6. 개정 기록

- **2026-07-29 (사용자 결정): '판정 전 표시·텔레그램 사용 금지' 조항을 '관측 페이지 노출 허용'으로
  개정.** 다른 관측 모델(wu_a·lv_b·mom_a·v31g)과 동일한 "테스트·관측·검증 전" 라벨로
  docs/qs.html + 텔레그램 링크 배선. **spec·점수·§11 판정 규칙·REG_DATE(20260723)는 불변** —
  노출은 표시 전용이며 IC 계산에 영향 없음. 개정 시점 OOS 4거래일(h1 IC +0.306, n=2)은
  §11 기준 노이즈 구간임을 인지한 상태의 결정임(추이 관측 목적).
  변경 파일: build_wu_filter.py(--model/--out 파라미터화, 기본값 wu_a 0-diff),
  docs/qs.html(신규), run_and_diversify.py(2.89b단계), notify_telegram.py(QS_URL 링크).
