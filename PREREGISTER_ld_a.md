# PREREGISTER — ld_a (lead 트랙 · 고베타×최근 신고가 월간 관측) **[등록 — 2026-09-24 · 첫 앵커 2026-10-01]**

> 상태: **등록**(2026-09-24 사용자 결정). 새 트랙 `lead`(주도주) — 기존 v3·lowvol·wu·large 트랙과 **테이블·잣대·분모 전부 분리**
> (§11 h20 IC·40거래일 판정을 쓰지 않는다 → 10월 판정(px_a·lv_e·v30 W2b)의 Bonferroni 분모 불변).
> 가중 0 · **표시 전용 페이지 `docs/lead.html`**(build_lead_page.py, 리더보드 성적표 표에는 미포함 — 잣대 상이) · 기존 점수/판정/게이트 0-diff. 적재: `lead_observe.py` → `history.db.lead_picks`. 평가: `lead_eval.py`.
> 근거 연구: `research/RESEARCH_bigwinners_20260924.md`(정오표 포함) · `research/RESEARCH_angles_20260924.md` ·
> Codex 독립 검토 `research/handoff/REPLY_20260924_bigwinners_review.md` · `REPLY_20260924_r1_controls_and_accounts.md`.

## 1. 스펙 (동결 — 변경은 새 id `ld_b…`로만)

| 항목 | 값 |
|---|---|
| model_id | `ld_a` · spec_hash **`4f01c8fcfb95`** (tests/test_lead_observe_rules.py 골든) |
| 대조군 | `ld_ctl_amt` = 거래대금20 상위 20 · spec_hash `ea086b02aa03` (모델 아님 — 같은 앵커에 같이 동결, 짝비교 전용) |
| 유니버스 | 전종목(코스피+코스닥 통합), 가드 = fslib §22-2: 63일 무변화 비율 ≤0.5 · 21일 내 \|일수익\|>32% 없음 · rv21 ≥0.003 · 거래대금20 ≥5억 · 정지 아님 |
| 핵심(실측 필수) | `beta60` = cov(종목 일수익, 같은 시장 지수 일수익; 60일, min40) / var(지수; 60, min40) — **높을수록 상위** |
| 보조(NaN=0.5) | `days_since_high` = 종가 ≥ 0.999×rollmax(종가,252,min120) 이후 경과 거래일(신고가일=0; 룩백 330일 내 신고가 없음=330) — **작을수록 상위** |
| 점수 | 통합 백분위 순위합: rank(beta60) + (1 − rank(days_since_high)). 가중 동일(탐색된 가중 없음) |
| 그룹 상한 | 120일 수익률 상관 → 상위 10 고유벡터 → k-means 25(seed 0) 동행 그룹, **그룹당 최대 4종목**. 클러스터 실패 시 상한 없이 선정하고 행에 cap_applied=0 기록 |
| 선정 | 상위 **20** (통합, 시장 비중 자유) |
| 앵커 | **매월 첫 거래일**(daily_ohlcv 기준) 종가, 월 1회. 같은 달 재적재 금지(동결) |
| 진입·보유 | t+1 종가 진입 · **120거래일 고정 보유** · 손절·익절 없음 · 동일가중 · 진입 불가(거래량 0)는 현금 |
| 비용 | 왕복 0.5% |
| 데이터 원천 | daily_ohlcv(close·volume·shares·is_suspended·market) + market_daily — 신규 수집 0 |

연구 코드(`research/bigwinners_20260924/bw_build.py` 전체 패널)와 프로덕션(330일 룩백)의 선정 대조: 2026-04/06/07/08/09 앵커 top20 **19~20/20 일치**(차이 = float32/64 동순위 처리). [실측 2026-09-24]

## 2. 근거 (전부 in-sample — 가설, 정오표 반영 후 수치)

- 3년 패널(2023-06~2026-09) 월간 앵커 27개, h120, 비용 후: 픽비중 지수 초과 **+10.4 [+2.6, +19.5]**, 동일가중 초과 CI>0.
- 성분: 베타만 +0.8 · 신고가만 −15.9 · **조합 +13.5** — 상호작용이 동인. 1년 모멘텀 추가 이득 미확인(짝차이 +0.7 [−2.7, +4.0]) → 제외.
- 대조군 '거래대금만'과 비용 후 수익 차이 +2.7 [−8.5, +15.5] — **구분 못 함**(지수초과로는 +13.5 vs −4.0, 벤치마크 차이). 이것이 판정의 핵심 질문.
- 성질: 국면 무관(약세 진입 +6.7 [+3.1, +10.3]) · 손절 유해(추적손절 20% → +2.8) · 그룹 상한 4 무비용(+10.7) · 조건 추가 시 전부 악화.
- **정직성 고지**: 스펙(순위합·N=20·120일·손절 없음·상한 4)은 같은 3년 데이터에서 다듬음. 2025 초대형 랠리 1국면 비중 큼. 룩어헤드 버그 1건(fw ±500% 절단이 후보 마스크에 들어감)은 Codex 검토로 발견·수정 후 수치 갱신. 생존편향(상폐 종목 부재). 유효 창 4~5개. 다중검정 미보정. 현재 픽 반도체 일색.

## 3. 판정 (사전 고정 — 이 트랙 전용 잣대, §11 대체 아님)

| 항목 | 규칙 |
|---|---|
| 창 | 앵커별 120거래일 실현 수익(t+1 → t+121 종가). **비겹침 창 ≥3개**(첫 앵커 2026-10-01 → 3창 마감 ≈ 2028-03) 후 1회 판정. 그 전엔 '관측 중'(중간 h60 수치는 참고만) |
| 비교 3중 | ① 픽 시장비중 지수(코스피/코스닥 비중 = 픽 구성비) ② 같은 앵커 가드 유니버스 동일가중(같은 비용) ③ 대조군 ld_ctl_amt |
| 통계 | 월간 앵커 전부에 6개월 원형 블록 부트스트랩 95%(3,000회) + 비겹침 창 개별 부호 병기. 분모 = lead 트랙 distinct model_id(=1, 대조군 제외) |
| 라벨 | **유의** = ①②③ 초과 CI 모두 0 위 · **기움** = ① CI 0 위이나 ③ 짝차이 CI 0 걸침(=대형 주도주 사기와 구분 안 됨) · **노이즈** = ① CI 0 걸침 · **역작동** = ① CI 0 아래. 비겹침 창 중 음수가 과반이면 한 단계 강등 |
| 실행 가정 명시 | 종가 체결·상한가 미체결 미반영·거래정지 시 마지막 관측가. 결측 출구 전손 스트레스 병기 |
| 무판정도 결론 | 못 넘으면 폐기 — 정상. lv_b·v30 과의 IC 절대값 비교 금지(잣대 다름). 승격(표시·운용)은 별도 사전등록 |

## 4. 배선 (2026-09-24, patch_note/20260924_lead_track_ld_a.md)

- `lead_observe.py`(신규): 월초 게이트·가드·팩터·순위합·클러스터 상한·동결 적재(INSERT OR IGNORE, 같은 달 재적재 금지). `--dry-run`·`--anchor`·`--force(dry-run 전용)`.
- `run_all_and_diversify.bat`: build_scoreboard 다음 1단계 추가(비치명, FAILED 집계). 배치가 도는 날마다 호출되지만 첫 거래일이 아니면 즉시 종료 0.
- `lead_eval.py`(신규, 읽기 전용): §3 잣대 계산·출력(자동 라벨 없음).
- `build_lead_page.py`(신규, 표시 전용): 같은 계산으로 `docs/lead.html` 생성(앵커별 픽·①②③ 비교·누적, 라벨 없음). 배치에서 lead_observe 직후 실행. `lead_universe` 표(앵커일 가드 유니버스)를 lead_observe 가 함께 저장해 ② 동일가중을 재현. `docs/leaderboard.html` 하단에 링크 1줄(성적표 표에는 미포함).
- `tests/test_lead_observe_rules.py`(신규, run_tests 등록): spec_hash 골든·순위합·게이트·상한·클러스터 결정성.
- `checkup.py REG_DATE["ld_a"]="20261001"`(기록용 — checkup 점수 테이블엔 없어 판정 대상엔 안 들어감) · MODELS_LEDGER 현역 표 · models_registry.json `observe` 키(표시 소비자 무영향).
- 검증(실측): 사본 history.db 에 20260303 앵커 적재 → 재실행 시 "이미 적재 → 건너뜀" → 같은 달 다른 날 게이트 → lead_eval h120 출력. 실 DB 무접촉. 기존 테스트 전체 통과.

## 5. 첫 앵커 이후 일정
- 2026-10-01(목) 배치에서 첫 적재(9/28 월 배치는 첫 거래일 아님 → 건너뜀). 이후 매월 첫 거래일.
- 2027-03 말: 첫 창 마감 → `lead_eval.py` 참고 수치(관측 중). 2028-03 말: 3창 → 판정문 VERDICT_ld_a.
