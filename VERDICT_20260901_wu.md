# VERDICT — wu 트랙 1차 §11 판정: wu_a·wu_b (2026-09-01) **[정본]**

> 상태: **정본** — 2026-09-01 사용자 승인으로 확정(처분 포함: 기각·적재 유지).

> 재현: `python research/verdict_wu_20260901.py` (읽기 전용, 2026-08-31 데이터 기준).
> 골대 정본: PREREGISTER_wu.md(등록 20260702, spec 동결). leaderboard.json(08-31)의
> 자동 라벨(wu_a·wu_b '역작동')과 방향 일치 확인.

## 0. 대상과 게이트
- 대상: **wu_a**(lv63+nh252+mom12+big 균형형)·**wu_b**(nh252+mom12 순수선택 대조) —
  8/31 기준 OOS 40거래일 도달. sv_a(32)·le_a(32)·qs_a(27)·px_a(15)는 미도달 → 판정 보류.
- 게이트·규약: §11 상속(ENTRY_LAG=1, h=20d, 시장별 IC→일평균, iid 부트스트랩 4,000회,
  부분실행 20260608·이중실행 20260703 제외).
- Bonferroni 분모: **사전등록 11**(PREREGISTER_wu.md 정본) — wu_scores 실측 6으로도 병행 계산,
  아래 결과는 두 분모 모두에서 결론 동일.

## 1. 판정 결과 (h20 주지표)
| 모델 | OOS | n(h20) | IC h20 | 95% CI | Bonferroni(/11) CI | 주별 양(+) | **판정** |
|---|---|---|---|---|---|---|---|
| wu_a | 40 | 19 | **−0.1079** | [−0.1379, −0.0802] | [−0.1501, −0.0671] | 20%(5주) | **역작동(유의) → 기각** |
| wu_b | 40 | 19 | **−0.1309** | [−0.1592, −0.1036] | [−0.1705, −0.0929] | 0%(5주) | **역작동(유의) → 기각** |

**요지**
1. 두 모델 모두 95% CI와 Bonferroni 보정 CI(분모 11·6 모두)가 **전부 음수** — 등록 가설
   (52주고 근접+모멘텀이 전체 유니버스에서 상위 수익)은 이 OOS 창에서 **거꾸로 작동**했다.
2. 표본 창(앵커 20260702~20260730)이 급락→반등 국면과 겹침: 고점 근접·모멘텀 상위 종목이
   급락에서 더 맞고 반등은 낙폭 큰 쪽이 주도한 장세 — 방향은 국면 해석과 정합.
3. 주블록 감도(각주③ 관례): wu_a [−0.1503, −0.0708] · wu_b [−0.1767, −0.0949] — 5블록,
   iid와 동일하게 전부 음수. post-hoc h10도 같은 방향(wu_a −0.038 상단 +0.003 / wu_b −0.081 전부 음수).
4. 짝비교(사전등록 조항): wu_a−wu_b diff +0.0230, CI [−0.0192, +0.0634] — in-sample의
   wu_a 우위는 '기움'조차 못 됨(유의차 없음).

## 2. 참고 관측 (판정 아님 — OOS<40)
- **sv_a**: n=11, IC +0.0601, CI [+0.0458, +0.0746], 주별 양 100%(3주) — 유망하나 §11상
  40일 미도달=노이즈 기본값. **판정 ~9/10, 조기 등록·배선 없음**(sv_b는 예약 작업대로).
- le_a: n=11, IC −0.0050 (노이즈 방향) · qs_a: n=6, IC −0.2254 (역작동 방향, 판정 ~9월 말).

## 3. 처분 (제안 — 확정 시 본 문서가 정본)
- wu_a·wu_b **기각**. lowvol 전례(8/29)와 동일하게 **적재는 유지·가중 0**(분모 행 보존,
  국면 전환 감시용) — v3 트랙식 RETIRED 동결과 달리 wu 테이블은 sv_a 등 현역과 공유.
- 부활은 새 model_id + 사전등록으로만(§11 불변). docs/wu.html 테스트 노출은 '기각' 라벨
  병기로 갱신(표시 조항은 판정 기준 아님 — 노출 형식만).

## 각주
① 앵커 19개·5주가 전부 단일 국면 — 이 판정은 "이 창에서 실패"의 확정이지 전 국면 일반화 아님.
   단 채택 실패는 §11상 그대로 기각이다(골대 고정).
② 분모: 사전등록 11(정본)과 실측 6(감도) 병기 — 본 판정은 어느 쪽으로도 동일.
③ 주블록 부트스트랩 감도는 본문 요지 3 참조(관례: VERDICT_20260829_lowvol 각주③).
