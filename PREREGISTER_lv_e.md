# PREREGISTER — lv_e (저변동 + ROE + 저회전) **[등록 — 2026-09-01]**

> 상태: **등록**(2026-09-01). 배선 패치 적용 완료(spec_hash 1774127f89ef 사전등록값과 일치, tests 통과).
> 승인 시: ① 아래 배선 패치 적용 → ② 첫 적재일을 REG_DATE로 확정해 이 문서 §2에 기입 →
> ③ MODELS_LEDGER.md·checkup.REG_DATE 갱신. **등록일 확정 전에는 이 문서가 골대가 아니다.**
> 근거 연구: `research/RESEARCH_lv_ablation_20260812.md` (V6) · 배선 검증: `research/lv_e_wiring_20260829.patch`

## 1. 스펙 (동결 대상)

| 항목 | 값 |
|---|---|
| model_id | `lv_e` |
| 트랙 | lowvol (`lowvol_scores` 테이블, 가중치 0 관측) |
| 유니버스 | `DEFAULT_UNI` = 과매도 30~70 + 거래대금 1개월평균 ≥ 5억 — **lv_b와 완전 동일** |
| 점수 | run×시장 cross-sectional 백분위 **순위합** (기존 규약 그대로) |
| 팩터 1 (핵심, 실측필수) | `realized_vol` — 낮을수록 좋음 |
| 팩터 2 (보조, NaN=0.5) | `roe_value` — 높을수록 좋음 |
| 팩터 3 (보조, NaN=0.5) | **`to20`** — 최근 20거래일 평균 (거래량 ÷ 상장주식수), **낮을수록 좋음** |
| to20 출처 | `ohlcv.db / daily_ohlcv (volume, shares)` LEFT JOIN. 전체 날짜 격자 위 `rolling(20, min_periods=1).mean()`, 기준일 포함 직전 20거래일 → **PIT 안전(미래 미사용)** |
| spec_hash | `1774127f89ef` (오프라인 산출, lv_b는 `b7d6bd0b51d9`) |

**변수 1개 원칙 준수**: lv_b(realized_vol + roe)에 `to20` **하나만** 추가. 유니버스·순위합 방식·
핵심팩터 규칙 전부 lv_b와 동일 → lv_b와의 짝비교가 곧 `to20`의 순수 기여다.

## 2. 등록·판정 골대

| 항목 | 값 |
|---|---|
| REG_DATE | **20260901** (첫 적재 예정일 — sm_a 판정(VERDICT_20260901_sm_a) 후 2026-09-01 사용자 승인·배선 적용. 당일 파이프라인 미실행 시 실제 첫 적재 run_id로 정정) |
| 판정 기준 | §11 — 등록 후 OOS **40거래일**, h=20d 주지표 |
| 판정 예정 | 등록일 + 40거래일 ≈ **등록 8주 후** |
| 채택 조건 | h20 IC 95% CI 하단 > 0 **그리고** 앵커 방향 양(+) ≥ 60% **그리고** Bonferroni(분모 11) 여유 |
| post-hoc 가중 | 이 스펙은 **post-hoc 발견**(2026-08-12 절제 스캔, 30검정 중 선택) → **h10 별도 호라이즌 재현 요구**. h20만 통과하면 '기움'까지만 인정 |
| 짝비교 | lv_b 대비 같은 앵커일 diff(h20·h10)를 판정문에 **반드시 병기** |

### ⚠ 동시검정 분모 증가 (명시)

lv_e를 등록하면 **lowvol 트랙 Bonferroni 동시검정 분모가 10 → 11**이 된다
(leaderboard.py는 `lowvol_scores`의 distinct model_id를 실측해 분모로 쓴다 — 하드코딩 아님).
즉 **기존 lowvol 모델 전부의 판정 문턱이 함께 올라간다.** 그래서 2026-08-12 연구 권고대로
lv_b 판정(2026-08-29 완료·확정, VERDICT_20260829_lowvol.md)을 **끝낸 뒤** 등록하는 순서를 지켰다.

**등록 시점(2026-08-29 사용자 결정)**: sm_a가 다음 거래일 40거래일 도달이므로 **sm_a 판정을
마친 뒤 등록**한다. mom_b(~9월 중)까지는 기다리지 않고 분모 11 판정을 감수한다
(mom_b는 현재 h20 IC 음수 — 분모 증가로 결론이 바뀔 가능성 낮음(추정)).

## 3. 근거 (전부 in-sample — '가설'이지 '증거' 아님)

`research/RESEARCH_lv_ablation_20260812.md` V6 = lv_b + to20, lv_b 실동결 유니버스 44 run
(2026-06-05~08-11) 위 재계산, **같은 앵커 짝비교(diff) + 주블록 부트스트랩**:

| 지평 | diff (V6 − lv_b) | 95% CI | 주별 일관 |
|---|---|---|---|
| h5 | **+0.031** | [+0.011, +0.046] | — |
| h10 | **+0.047** | [+0.026, +0.061] | — |
| h20 | **+0.051** | [+0.026, +0.062] | 6/7주 |

- **3개 지평 전부 CI>0** — 절제 스캔 10변형 중 가장 견고(V9 +lv20이 예비).
- 독립 경로 수렴: px_a(lv60+to20+lv20+nh252)의 성분과 일치 — "조용함을 여러 각도로 재기".
- **한계(그대로 옮김)**: OOS 창이 폭락+반등 **단일 국면**, h20 앵커 창 겹침, 30검정 중 선택.
  연구 결론도 '채택'이 아니라 **'강한 기움'**이었다. forward 40거래일이 유일한 증거다.

## 4. 기각·주의 조건 (미리 적어 둔다)

- h20 IC CI가 0을 걸치면 → **기각 아님, '기움' 이하**. 채택은 CI>0 + h10 재현 + 분모 11 여유까지.
- lv_b 대비 짝비교 diff의 CI가 0을 걸치면 → to20 추가의 기여는 **미확인**으로 기록.
  lv_b 대비 우위가 없으면 lv_e는 승격하지 않는다(IC 절대값만으로 승격 금지).
- to20 커버리지가 lv 유니버스의 90% 미만으로 떨어지면(보조 NaN=0.5가 과반) → 판정 보류하고
  커버리지 먼저 보고.
- **판정 전 스펙 변경 금지**(챌린저 spec 동결 규칙).

## 5. 배선 (제안 — **미적용**)

패치 파일: `research/lv_e_wiring_20260829.patch` (`lowvol_score.py` 대상, 63줄)

변경 3곳, **전부 추가만**:
1. `FACTORS`에 `"to20": ("to20", False)` 추가
2. `MODELS`에 `"lv_e": {"factors": ["realized_vol","roe","to20"], "uni": DEFAULT_UNI}` 추가
3. `run()`에 to20 LEFT JOIN 블록 추가 — `short_flows` LEFT JOIN과 동일 패턴,
   `ohlcv.db` 부재 시 NaN(보조라 0.5 중립) → 다른 모델·유니버스 무영향

### 오프라인 검증 (사본 DB, 실 DB 무접촉)

| 검증 | 결과 |
|---|---|
| 기존 10개 모델 **0 diff** | ✅ 사본에 원본판·패치판을 각각 `--full` 재적재 → 공통 173,656행, 점수 불일치 **0**, spec_hash 불일치 **0** |
| lv_e 신규 적재 | 16,238행 / 55 run — **lv_b와 행 단위 1:1 완전 일치**(유니버스 동일 확인) |
| lv_e·lv_b 점수 상관 | run×시장 순위상관 평균 0.900 (중앙값 0.902) — 같은 계열이되 서열은 다름 |
| PIT | to20은 기준일 포함 직전 20거래일만 사용, forward 미참조 |

### ⚠ 검증 중 발견한 별건 (판정 무영향, 사용자 확인 요망)

실 DB의 동결 lowvol_scores vs 현재 stage3/ohlcv 기준 `--full` 재적재를 비교하면
**173,656행 중 14,501행(8.35%)이 불일치**한다. 내역:
- **13,895행 = `lv_short`** — short_flows가 ohlcv.db로 이전·백필되면서 과거 run의 공매도 값이
  달라졌다(§21-8 Phase1의 예상된 부작용).
- **606행 = `20260824` run 1건** — 그날 stage3_final이 lowvol_scores 동결 이후 덮어써진 것으로 보임.
- 나머지 모델·날짜는 완전 일치 → **재현성 자체는 정상**.
- 판정 영향: lv_b h20 앵커는 20260729까지라 두 건 모두 **이번 판정 표본 밖**. 다만
  "`--full` 재적재는 과거 동결값을 바꿀 수 있다"는 사실은 운영 주의사항으로 남긴다.

## 6. 승인 후 실행 순서

1. `research/lv_e_wiring_20260829.patch` 적용 (`git apply` 또는 수동)
2. `python lowvol_score.py`(증분) 1회 실행 — lv_e 백필 행이 생기지만 REG_DATE 게이트로 판정 제외
3. 첫 적재 run_id를 REG_DATE로 확정 → 이 문서 §2 + `checkup.py REG_DATE` + `MODELS_LEDGER.md` 기입
4. `python leaderboard.py` — 분모 10→11 반영 확인, 기존 모델 IC 값 불변 확인
