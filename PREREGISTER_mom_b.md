# PREREGISTER — mom_b(모멘텀+눌림목) 관측 모델 (2026-07-17 동결)

> lowvol 트랙(lowvol_scores 테이블)에 추가되는 **가중치 0 관측 모델 1종**.
> 발견은 post-hoc(in-sample) → **REG_DATE 2026-07-17 이후의 forward 데이터만 §11 판정에 사용.**
> 이 문서 이후 spec 변경 금지 — 바꾸려면 새 model_id. 추천·텔레그램 사용 금지(판정 전).
> 표시는 mom.html '관측 병기'(mom_b순위 컬럼)만 허용 — mom 페이지 자체가 관측 전용
> 페이지(매수신호 아님 라벨)이며 v3·large·텔레그램 산출물은 불변.

## 1. 출처 (발견 경위 — 판정에는 사용 불가한 in-sample 근거)

- 오프라인 오버레이 스캔(2026-07-17, outputs/research_overlay.py): stage3_final 41 run
  (20260523~20260716, 게이트 후 28거래일)에서 mom_a 상위10 풀 내 `return_1w_%`가
  h5 시장초과수익을 가르는 day-IC **−0.269 CI[−0.356,−0.178]** (n=28일, 음수일 86%).
  후보 22개 중 최강 — 모델당 22개 다중검정을 감안해도 CI가 0에서 가장 멀리 떨어짐.
- 필터 시뮬(같은 스캔): top10 → '지난주 하락 우대 5종' 교체 시 h5 시장초과
  **Δ+2.29%p CI[+0.49,+5.06]**, 일 승률 50%→61%.
- 문헌 부합: 모멘텀 + 단기반전(pullback entry) 오버레이는 알려진 조합(단기반전은
  1~5거래일 되돌림 — 단독으론 약하고 오버레이로 유효하다는 보고).
- ⚠️ 발견 구간은 급락→반등 단일 레짐. 눌림목(반전) 계열이 구조적으로 유리했던
  장일 수 있음 — **상승 추세장에서의 유지 여부가 OOS 판정의 핵심 관전 포인트.**

## 2. 동결 스펙 (lowvol_score.py MODELS/FACTORS와 1:1)

mom_a와 **팩터 1개 차이**(챌린저 규칙 준수). 유니버스·방식은 mom_a와 동일:
과매도 30≤os<70 · 거래대금 1개월평균 ≥5억, cross-sectional pct rank 순위합,
핵심(첫 팩터) 실측필수(NaN=제외), 보조 NaN=0.5.

| model_id | 팩터(순서=핵심 먼저) | 방향 | spec_hash |
|---|---|---|---|
| **mom_b** | sma20 = vs_SMA20_% | ↑ (20일선 위 회복) | `147021a88bb0` |
| | mom_1m = return_1m_% | ↑ (1개월 모멘텀) | |
| | vol_exp = vol_1w_vs_1m_ratio | ↑ (거래량 팽창) | |
| | **reversal = return_1w_%** | **↓ (지난주 눌림 우대)** ← mom_a와의 유일한 차이 | |

- 적재: history.db `lowvol_scores` (model_id='mom_b'), 파이프라인 2.86단계(lowvol_score.py)에서 자동.
- 백필 주의(스펙의 일부): lowvol 트랙 로더는 신규 모델을 stage3 전 구간에 소급 적재함
  (mom_a 등 기존 관례와 동일). **run_id < 20260717 행은 in-sample 백필 — §11 판정에서
  REG_DATE 게이트로 자동 제외**(leaderboard.py·checkup.py가 원장 기준으로 필터).

## 3. 판정 규칙 (§11 그대로)

- **h=20d IC 주지표**, h=5d 보조(발견이 h5 구조였으므로 h5를 '관전 1순위'로 명시하되
  판정 승격은 h20 기준 유지). OOS ≥ 40거래일 전까지 무조건 '노이즈'.
- 부트스트랩 CI(2,000) > 0 · 다중검정(lowvol 트랙 동시검정 모델 수 기준 Bonferroni).
- 관전 포인트(사전 서약):
  ① mom_a 대비 ΔIC가 forward에서도 양(+)인가 (같은 유니버스라 직접 비교 가능)
  ② 반등장이 끝나고 추세장이 오면 눌림목 우위가 소멸하는가
  ③ 스캔에서 나온 '저유동성 쏠림'(amt IC −0.191)이 mom_b 상위픽을 오염시키지 않는가
    (상위5 거래대금 중앙값을 mom_a와 병행 관찰)
- 판정 예상 시점: 2026-09 중순(거래일 기준 40일).

## 4. 불변 확인 (2026-07-17 오프라인 검증 완료)

- 실 history.db 사본(사전 107,432행 체크섬 `a854789ced2e2e27`)에 신코드 실행 →
  기존 모델 행 **0-diff**(행수·체크섬 동일), 기존 9개 모델 spec_hash 전부 불변.
- mom_b 40 runs(20260523~20260716) 적재 확인, spec_hash 단일(`147021a88bb0`).
- build_mom_filter.py: mom_b_rank 컬럼 병기 확인(커버리지 100%), mom_a 순위와 유의미하게
  다름(예: 지난주 +상승 종목이 뒤로 밀림). mom.html COLS에 'mom_b순위' 추가(관측 라벨).
- checkup.py REG_DATE 등재: "mom_b": "20260717" (leaderboard는 checkup 원장을 import).
