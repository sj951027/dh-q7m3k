# ====================================================================
#  절대 커밋하면 안 되는 것 (보안)
# ====================================================================
.env
.env.*
!.env.example
*.key

# ====================================================================
#  캐시 / 재생성 가능 (용량만 차지)
# ====================================================================
__pycache__/
*.pyc
*.pyo
.venv/
venv/
dart_cache/
price_cache/          # validate_scores 시세 캐시 (커질 수 있음)

# ====================================================================
#  날짜별 출력 스냅샷 (매번 쌓임 — 최신만 있으면 됨)
#  ※ latest_*.csv, docs/, history.db, snapshots/, archive/ 는
#    대시보드·누적기록에 필요하므로 '올린다' (여기서 무시하지 않음)
# ====================================================================
diversified_picks_*.csv
validation_picks.csv
validation_summary.csv
v2_kospi_*.csv
v2_kosdaq_*.csv

# ====================================================================
#  OS / 에디터
# ====================================================================
.DS_Store
Thumbs.db
.vscode/
.idea/
