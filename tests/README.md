# tests/ — 회귀 테스트 (2026-08-29 도입)

목적: **"조용히 틀어지는" 사건의 재발 방지.** 2026-08-12 게이트 오탐(정상 run 6개가 부분실행으로
제외), 2026-08-04~10 표시 동결 — 둘 다 숫자가 그럴듯해 보여서 늦게 발견된 유형이다.
판정 프로토콜과 점수 규칙을 골든값으로 못박아, 코드가 바뀌면 사람이 아니라 테스트가 먼저 안다.

- `test_lowvol_score_rules.py` — 합성 데이터(DB 불필요, 즉시): 핵심 NaN 제외·보조 0.5 중립·
  순위합·**spec_hash 골든 10종**(스펙 드리프트 감지. 새 모델은 목록에 추가만).
- `test_leaderboard_frozen.py` — 실 DB(읽기 전용, ~20초): 게이트({20260608}/{20260703})·
  REG_DATE 핵심값·**동결창(앵커≤2026-07-29) h20 IC 골든**(lv_b +0.0687·lv_c −0.0962·
  v30 +0.0584 — 이 앵커들의 h20 창은 전부 마감이라 데이터가 늘어도 영원히 불변)·프로토콜 상수.
- `run_tests.py` — 일괄 실행, 실패 시 종료코드 1.

규칙: **골든이 깨지면 골든을 고치지 말 것.** 로직 변경이 의도된 것인지(전/후 비교 + 사용자
승인, 프로젝트 0-diff 원칙)부터 확인한다. 의도된 변경으로 확정된 경우에만 patch_note 와 함께
골든을 갱신한다. 권장 습관: 점수·판정 코드를 만진 세션 끝에 `python tests/run_tests.py`.
