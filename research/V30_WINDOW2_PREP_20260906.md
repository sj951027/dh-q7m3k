# v30 2차 창 판정 준비 (2026-09-06 — 관측 전용, 판정 아님)

스크립트: `research/verdict_v30_window2_prep_20260906.py` (leaderboard.py 규약 그대로, 읽기 전용). 9/04 짝비교(`RESEARCH_pair_v30_lvb_20260904.md`)의 후속.

## 1. 왜 2차 창이 필요한가 (쉬운 말)

- v30 '유의'(8/09 정본)는 등록 후 40앵커 누적 IC +0.043(iid CI>0)으로 봉인됐다. 그런데 쪼개 보면
  **첫 13앵커(6/06~6/24, 급락→반등) IC +0.187, 이후 27앵커 −0.027**. 주블록 CI로 다시 재면 누적도 [−0.033, +0.124]로 0을 걸친다(각주③).
- 즉 "v30이 지금도 작동하는가"는 첫 창만으로는 답이 안 된다. **판정 이후 새 데이터로 한 번 더** 봐야 한다.

## 2. 창 두 개 — 하나는 못 쓰고 하나는 지금 등록해야 한다

| 창 | 정의 | 성격 | 지금 수치 |
|---|---|---|---|
| W2a | 6/25~ (lv_b 공통 창) | **post-hoc** — 첫 13앵커가 결과를 만든 걸 *본 뒤* 자른 경계. 판정 근거로 못 씀 | v30 h20 n=27 IC −0.027 [−0.057,+0.008] · lv_b +0.031 [−0.042,+0.107] · lv_b−v30 +0.058 iid CI>0, 주블록 걸침 |
| **W2b** | **8/10~ (8/09 정본 판정 이후 적재분)** | 판정 대비 **진짜 OOS**. h20 완결 앵커 아직 **0개**(OOS 18일) → 데이터를 보기 전이라 **지금 등록하면 깨끗한 사전등록** | 표본 없음. 40 도달 = 22거래일 뒤(≈10/07) |

## 3. 제안 — W2b 를 지금 사전등록 (사용자 승인 필요, 판정 규칙에 영향)

- 문서: `PREREGISTER_v30_w2.md`(초안은 아래). 스펙 변경 없음(v30 spec_hash 그대로) — 바뀌는 건 **판정 창의 시작일**뿐.
- 규칙: §11 그대로(h20 주지표·iid+주블록 CI·주별 일관 ≥60%·Bonferroni 분모 = v3_scores distinct 7). 시작 20260810, 40거래일.
- 결과 해석(미리 적어 둔다):
  - CI>0 → '유의 재확인' — 챔피언 유지, 1차 창 의존 우려 해소.
  - CI 걸침·IC≥0 → '기움 이하' — **챔피언 강등은 아님**(기각 조건 아님). 리더보드 정본 라벨은 "유의(8/09) / 2차 창 노이즈"로 병기. lv_b 짝비교 우위가 주블록 CI>0 이면 표시 기준 재검토 안건.
  - 역작동(CI<0) → §11 기각 절차(VERDICT). 표시 기준은 lv_b 단독으로.
- 판정 도구: 이 스크립트의 W2b 블록 그대로. VERDICT_2026100x_v30_w2.md 로 기록.
- 동시검정: v30 재판정은 새 모델이 아니므로 **분모 불변**. 단 같은 창의 lv_b 짝비교를 함께 적으면 그건 '보조'.

## 4. 오늘 한 것 / 안 한 것
- 한 것: 창 정의·현재 수치(관측). 코드·판정·원장 무변경.
- 안 한 것: PREREGISTER_v30_w2.md 생성(판정 규칙 문서 — 사용자 승인 후). 승인되면 MODELS_LEDGER '판정 예정'에 "v30 2차 창 ~10/07" 추가.
