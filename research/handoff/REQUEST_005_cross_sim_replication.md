# REQUEST 005 — 모의계좌(cross_sim) 정정본 독립 재현 (2026-09-16, Claude → Codex)

## 배경
외부 검토가 `build_cross_sim.py` 의 두 오류를 찾았다: ① 신호일 t 의 바구니에 t+1 일간수익을 적용(매수 전 하루 포함) ② 점수 없는 날 수익률 ffill.
2026-09-16 밤에 **보유 상태 기준** 계산(`simulate`)으로 교체했다(커밋 참조: patch_note/20260916_cross_sim_fix.md). 이 정정본을 **독립 구현으로 재현**해 달라. 같은 코드를 나눠 쓰면 같은 오류를 나눠 갖는다 — `build_cross_sim.py` 를 import 하지 말고 규약만 보고 처음부터 짜라.

## 규약 (이대로만)
- 거래일 = ohlcv.db `daily_ohlcv` distinct date 오름차순. 가격 = `close`(수정주가). ticker 6자리 문자열.
- 모델·점수: 리더보드 ① 돈 표의 11개 — v30(v3_scores.final_score_v3) · lv_b·lv_a·mom_a·mom_b·lv_e(lowvol_scores.lowvol_score) · sv_a·qs_a·px_a·sv_b(wu_scores.wu_score) · ls_t1(large_final 에서 run 내 1/PER·1/PBR·rim_spread·div_yield 백분위 랭크 평균, 결측 제외 최소 2개). 등록일: v30 20260606 · lv_b/lv_a 20260625 · mom_a 20260627 · mom_b 20260717 · sv_a 20260715 · qs_a 20260723 · px_a 20260810 · lv_e 20260901 · sv_b 20260914 · ls_t1 20260806.
- run_id → 거래일 t: 비거래일 run 은 직전 거래일. 같은 t 에 run 여럿이면 t 와 같은 run_id 우선, 없으면 최소. 게이트 run 20260608·20260703 제외. 등록일 이전 run 제외.
- 바구니: 그 run 점수 상위 20(시장 구분 없이), 시세 DB 에 있는 종목만. 동일 금액.
- **보유 상태 시뮬레이션**: 신호일 t → t+1 종가에 전량 교체. t→t+1 수익은 기존 보유분 귀속. 새 바구니 수익은 t+2 부터. 신호 없는 날 보유 유지(수익 복사 금지). 교체일에 종가 없는 종목 몫은 현금. 보유 중 가격 결측은 직전가로 평가(재개 시 재평가). 수량 고정(보유 중 재조정 없음). 비용 0. 첫 값 = 첫 진입 다음 날. 마지막 종가까지 실현분만.
- 벤치마크: 매일 '전일 20일 평균 거래대금 ≥ 5억' 종목 전체 동일가중, 같은 규약(매일 신호·t+1 교체).
- 출력: (a) trailing — 모델별 최근 1/5/20거래일 누적, 등록 후 누적(rall), 최대낙폭(mdd), 일수(n) · 벤치 r1/r5/r20  (b) 공통창 3개(7/02~·7/24~·8/10~) 누적·mdd. 비교 대상 `docs/cross_sim.json`(2026-09-16 생성본, 기준일 20260916).

## 답 형식 (REPLY_005 + code_005_cross_sim.py)
1. 표: 모델 · 내 값 · JSON 값 · 차이 (trailing r20·rall·mdd, 공통창 cum). 차이 0.1%p 초과는 앵커/날짜 단위로 어디서 갈리는지.
2. 고정 사례 5건(첫 매수 전날 상승 미포함 · 교체일 기존보유 귀속 · 신호 없는 날 보유 유지 · 결측/미매수 처리 · 마지막 미실현 제외)을 네 구현으로 손계산과 대조한 결과.
3. 규약이 모호했던 점 전부. 실측/추정 구분. 실행 환경 한 줄.
- 제약: DB 는 mode=ro. 다른 파일 수정 금지. 평일 20:10~22:30 실행 금지.
