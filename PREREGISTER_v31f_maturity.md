# 사전등록 — v31f(macd 이격도) + 성숙도 보류 (2026-06-20)

> 골대 고정용. 결과 보고 기준을 나중에 바꾸지 않으려 **미리** 못 박는다.
> PROJECT_KNOWLEDGE.md §11(40거래일·h=20d·부트스트랩 CI·다중검정)을 그대로 상속.

---

## A. v31f · F1 macd 이격도 가산 (섀도우 챌린저)

**팩터 정의**
- `macd_proxy = vs_SMA20_% − vs_SMA50_%` — 단기-중기 추세 이격도. "단기선이 중기선 위로 막 전환"의 **연속 proxy**.
- MACD 본래의 종목별 EMA(fast/slow)는 history.db에 연속 OHLC가 없어 구성 불가 → 본질(fast MA가 slow MA 위)을 이격도 차로 대리. **근사임을 명시.**

**스펙 (동결 — 불변규칙 2)**
- v30 에서 **한 가지만** 변경: `final_score_v3` 합산에 항 1개 추가.
- 항 = 셀(run×market) 내 z-score(±3 clip) × `MACD_TILT_W = 6.0`.
- 6.0 보정 근거: 셀내 z std ≈ 0.92, 기존 성분 std 중앙값 ≈ 5.93 → 기여 std가 **"성분 1개" 크기**가 되도록. (macd가 합성식을 지배하지도, 무시되지도 않게.)
- 동시점 데이터 z-score라 룩어헤드 없음.

**적재 / 격리**
- `shadow_run.py`가 MODELS 자동 순회 → `v31f_archive/`에 매일 누적(섀도우, 네트워크 0).
- **챔피언 v30 출력 불변(0-diff 검증 완료).** 사용자 노출(텔레그램/대시보드/필터)은 v30 그대로.

**발견 경로 정직성 (중요)**
- macd_proxy는 영상 룰 4개를 번역해 본 뒤 **사후(post-hoc)로 고른 1개.**
- 다중비교 체리피킹을 피하려 **여기서 골대를 새로 박는다.** 이전 18거래일 결과는 '기움'일 뿐, **이 사전등록의 증거로 재사용하지 않는다**(같은 표본 재사용 금지).

**주력 지표 (사전 고정)**
- 챔피언 대비 **h=20d 한계 IC** = 평균(IC_v31f − IC_v30). (run×market) 셀별 Spearman, 날짜 부트스트랩.

**채택 조건 (모두 충족)**
1. v31f 섀도우 누적 **40거래일** OOS에서 평균(IC_v31f − IC_v30)의 **부트스트랩 95% CI(날짜 리샘플)가 전부 0 초과**(h=20d).
   - h=20d는 가장 이른 picks가 ~20거래일 성숙해야 계산됨 → **첫 판정은 누적 시작에서 ≥40거래일 뒤.**
2. 주별 방향 일관성 **≥ 60%.**
3. 보조(BUY+WAIT 시장초과수익, h=20d) 부호가 주력과 **상충하지 않음.**

**다중검정 (챌린저 6개: v31a~d + v31f + v31g)**
- CI를 **≈99%(=0.05/6, Bonferroni)**로 올리거나, **별도 보유기간(예 h=10d)에서 재현**될 때만 **최종 채택.** 못 넘으면 '기움'까지만 표기.

**보조 호라이즌**: h=5d (부호 일치 확인용).

**레짐 경고**
- 검증 18일 창은 **평균회귀 레짐**(reversal IC −0.07, vs_SMA200 −0.06, v2.6 raw −0.046가 전부 음). macd의 +IC가 **레짐 인공물**일 수 있음 — 추세장 표본이 섞이는 40거래일까지 **부호 유지 여부가 진짜 크럭스.**

**무판정도 결론**
- 기준 미달이면 **v30 유지(정상·바람직).** 채택 시에도 즉시 교체 금지 — **1주 관전** 후 반영, 반영분은 spec 동결.

---

## B. 성숙도(maturity) 팩터 — 보류 (SHELVED)

**가설(지난 세션)**: 과매도 종목 중 **장기 추세가 건강한 쪽**(vs_SMA200 높음 = 회복 진행)을 가점하면 더 낫다.

**결과: 가설과 부호 반대.**
- 이번 history.db 재현에서 maturity proxy인 `vs_SMA200_%` 단일 IC = **−0.064(h=3) / −0.065(h=5)**, IC>0 27%/19%.
- 지난 세션 보고(조건부 IC −0.105, 틸트 Δ 음)와 **부호 일치.** (단 성숙도 정확한 정의는 본 세션에서 별도 재현하지 않음 — vs_SMA200으로 대리 확인.)

**메커니즘**: 스냅백(평균회귀) 엔진엔 "이미 많이 회복됨 = 남은 반등 여력 적음"이라 가점이 **거꾸로** 작동. 사전직관·초기데이터·메커니즘이 모두 같은 음(−) 방향 → 40거래일 섀도우로 "죽음을 지켜볼" 가치 낮음(**불필요한 복잡성 회피**).

**처분: 챌린저 미배선, 한 줄 기록으로 보류.**
- 골대 옮기기 방지를 위해 명시: **"현 평균회귀 레짐에선 방향 반대라 보류. 시장이 추세장으로 전환되면 부호가 뒤집힐 수 있으니 그때 재검토."**
- 부활 시 **자체 새 사전등록 + 새 모델 id**로만.

**'v31e' 이름 처리**
- 지난 세션은 v31e를 성숙도용으로 예약했다고 했으나, **이번 핸드오프 어느 파일에도 v31e는 없다**(코드 `MODELS`에도 미존재) — 그 예약은 **미확인.**
- 충돌 방지로 macd_proxy는 **v31f**를 쓴다. 성숙도를 살릴 땐 그때 이름을 새로 사전등록한다.

---

## C. 검증 기록 (오프라인, perf/history.db)

- **0-diff**: `v30·v31a·v31b·v31c·v31d` 전부 원본 == 패치.
  각 32,373행, `max|Δfinal_score_v3| = 0`, `max|Δentry_score| = 0`, grade/bucket/main_candidate 불일치 0.
- **v31f 작동**: 챔피언 대비 평균 |Δfs| = 2.75, 최대 18.10, grade 6·bucket 4 이동(n = 32,373).
- **배선 방향**: 단기 IC v31f > v30 (h=3 +0.032 vs +0.023, h=5 +0.050 vs +0.037) — 가산이 맞는 방향. (판정 아님, 부호 확인용.)
- **자동 배선**: `shadow_run.py`·`compare_models.py` 모두 `v3_rescore.MODELS` 자동 순회 → **두 파일 수정 불필요.**

---

## D. 운영 메모

- `.gitignore`에 한 줄 추가: `v31f_archive/` (불변규칙 §7 — 챌린저 아카이브 비추적).
- 누적 시작: 다음 일일 실행부터 자동.
- **소급(backfill) 금지 — v31f는 forward-only 누적만.**
  - 과거 18거래일(2026-05-23~06-19)은 macd_proxy를 **발견한 표본**이다. 그 날짜에 v31f를 소급 적재해 OOS에 넣으면 **모델을 자기 발견 데이터로 채점**하는 순환논리 → §11이 막으려는 체리피킹. (A절 "발견 경로 정직성"과 동일 원칙.)
  - `shadow_run.py --run_id`로 과거 run을 적재할 수 있으나 **v31f엔 쓰지 않는다.** OOS 시계는 누적 시작 시점부터.
  - 발견 단계 in-sample 참고치(단기 IC v31f>v30: h=3 +0.032 vs +0.023, h=5 +0.050 vs +0.037)는 **부호 확인용이며 판정에 미사용.**
- 첫 §11 1차 판정: v31f 누적 **40거래일 + h=20d 성숙** 후 `python compare_models.py`.

---

## E. 관측 팩터 사전등록 — 거래량팽창 · 실현변동성 (2026-06-20)

> 둘 다 **가중치 0 관측 컬럼**(점수식 미투입). `validate_scores.py` FACTOR_COLUMNS 등록 → IC 하니스가 측정.
> 일관 양(+)이고 직교가 확인되면 **그때 챌린저로 승격**(v31f식 2단계). **forward-only.**

### E-1. 거래량팽창 (`vol_1w_vs_1m_ratio`)
- **정의**: 최근 5일 평균거래량 / 21일 평균거래량 (stage3 기존 컬럼, 100%). 값>1 = 거래량 팽창.
- **가설(부호)**: 팽창 → 고수익(+IC) = 거래량 동반 반등(매물 소진/capitulation, high-volume premium 계열).
- **발견 경로 정직성**: 후보 3+개를 **발견표본에서 훑어 고른 사후(post-hoc) 1개** = 다중비교 낚시. 아래 in-sample 수치는 **증거 아님, 가설.** OOS 판정에 재사용 금지.
  - (참고치·판정 아님) 단일 IC h=5 +0.116 [+0.071,+0.156]. **챔피언 거래량플래그(`reversal_vol_up_candle`) 통제 후 편-IC +0.119 [+0.074,+0.161], IC>0 96%** → 이진 플래그 너머 직교로 보임.
- **직교성 목표**: oversold(이미 +0.01 확인) + **`reversal_vol_up_candle`**(연속 vs 이진 중복 여부 — 이게 진짜 크럭스).
- **주력/게이트**: 챔피언 §11 그대로 — **forward 40거래일, h=20d Spearman IC, 날짜 부트스트랩 CI.** 낚시 출신이라 더 보수적: Bonferroni(관측팩터군 포함) + **별도 호라이즌(h=10d) 재현**까지 봐야 '유의'.
- **승격**: 충족 시 → `vol_1w_vs_1m_ratio`를 가중 챌린저(예 v31g)로 배선해 **챔피언 대비 한계 IC** 재검정.

### E-2. 실현변동성 (`realized_vol`)
- **정의**: trailing 21 활성런 종가수익률 표준편차 (`observe_vol.py`, 동결 WINDOW=21·MIN_OBS=8). PIT 안전(미래 미사용).
- **가설(부호)**: low-vol anomaly면 **raw vol IC 음(−)**(저변동→고수익). 단 단기 반등 맥락에선 반대 가능 → **부호는 데이터에 맡김.**
- **현 상태**: in-sample 결론 못 냄 — h=3 −0.065 [−0.177,+0.041], h=5 −0.028 [−0.120,+0.065], **셀 10~14로 매우 얇음**(20260605부터 채워짐). 약한 음(−) 기움이 저변동 가설과 방향은 일치하나 0과 구분 불가.
- **커버리지 한계**: 가격이 과매도 패널에만 있어 ~43%만 채워짐(최근 윈도우 머문 종목). 신규 편입은 pending. 기존 IC와 동일 제약 — 데이터 쌓이면 확대.
- **게이트**: 챔피언 §11(forward 40거래일·h=20d). 얇아서 첫 판정은 한참 뒤 — 정상.

### E-3. 검증 기록 (오프라인, perf/history.db)
- **0-diff**: realized_vol 컬럼 추가 후에도 final_score_v3/grade/bucket 불변(n=32,373, max|Δ|=0). 두 컬럼 모두 v3_rescore 미참조 확인.
- **observe_vol.py**: `--full` 13,999행 채움, 재실행 0행(idempotent), 값 0.001~0.19(중앙값 0.031, 일변동성 타당).
- **자동 측정**: FACTOR_COLUMNS에 둘 다 등록 → IC 하니스 자동 산출.

### E-4. 운영
- 파이프라인: `catalyst_observe.py` 뒤에 `python observe_vol.py` 한 줄(증분). 최초 1회 `python observe_vol.py --full`로 과거 채움.
- **컬럼 백필(--full) 허용 ≠ v31f 소급 금지.** 관측 컬럼 채움은 점수 불변이고 OOS 판정은 등록일 이후 forward IC만 쓰므로 무해. (v31f '소급 금지'는 archive를 OOS로 *세는* 문제였음 — 차원이 다름.)
- IC 확인: 평소 `validate_scores` 경로(사용자 네트워크 실행)에서 두 팩터가 'pending→측정'으로 전환.

---

## F. v31g · F2 거래량팽창 가중 챌린저 (섀도우) — 2026-06-20

> E-1(관측 컬럼)의 **가중 챌린저 병행판.** 둘은 다른 질문을 잰다:
> 관측 컬럼 = "거래량팽창이 **단독으로** 예측력?", v31g = "챔피언에 **더하면** 한계 IC↑?".
> **둘 다 검증 전 채택 아님.** 관측 컬럼은 그대로 유지(중복 측정 무해).

- **팩터/스펙(동결)**: v30 + 항 1개 = 셀(run×market) 내 z-score(±3 clip)의 `vol_1w_vs_1m_ratio` × `VOLEXP_TILT_W = 7.0`. 7.0 = z(±3)std≈0.863, 성분 std 중앙값≈5.93 → '성분 1개' 크기. (우편향이라 +3 클램프가 거래량 스파이크 정규화.)
- **격리/노출**: `shadow_run.py` 자동 순회 → `v31g_archive/` 일일 누적. **챔피언 v30 불변(0-diff 검증 완료).** 사용자 노출은 v30 그대로.
- **발견 경로 정직성**: E-1과 동일 — 거래량팽창은 **사후(낚시) 발견.** in-sample 수치(단일 IC h=5 +0.116, 가중 챌린저 방향 Δ +0.013~+0.015)는 **증거 아님, 가설.** OOS 판정 재사용 금지·소급 금지(forward-only).
- **주력/게이트**: 챔피언 §11 — **forward 40거래일, h=20d 한계 IC = 평균(IC_v31g − IC_v30), 날짜 부트스트랩 CI.** 낚시 출신이라 더 보수적: **Bonferroni 0.05/6**(챌린저 6개) + **별도 호라이즌(h=10d) 재현**까지 봐야 '유의'.
- **직교성 주의**: 거래량팽창은 챔피언 `reversal_vol_up_candle`(이진)의 연속판. 관측 단계 편-IC(통제 후 +0.119, in-sample)가 살아있었으나 **forward로 재확인** 필수. 안 살아남으면 중복.
- **무판정도 결론**: 미달이면 v30 유지. 채택 시에도 1주 관전 후 반영, spec 동결.

### F-검증 기록 (오프라인, perf/history.db)
- **0-diff**: v31g 추가 후 v30·v31a·v31b·v31c·v31d·**v31f** 전부 불변(각 32,373행, max|Δfs|=0, grade/bucket/main 불일치 0). v30·v31a~d 는 원본과도 동일.
- **v31g 작동**: 챔피언 대비 |Δfs| 평균 3.29(최대 21.10), 단기 IC v31g>v30 (h=3 +0.036 vs +0.023, h=5 +0.052 vs +0.037) — 가산 방향 정상. (이 표본선 grade/bucket 경계 미통과 = 버킷 내 재랭킹.)
- **자동 배선**: `shadow_run.py`·`compare_models.py` 모두 MODELS 자동 순회 → 두 파일 수정 불필요.
- `.gitignore`: `v31g_archive/` 한 줄 추가.
