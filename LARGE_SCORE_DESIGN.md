# LARGE_SCORE 설계노트 — 대형 가치주 트랙 (dh-q7m3k)

> **용도**: 기존 스크리너 프로젝트(PROJECT_KNOWLEDGE.md)의 **두 번째 지식 문서**. 같은 repo·같은
> 인프라 위에 얹는 **별도 트랙(대형 가치주)**의 청사진이다. 아직 *설계 단계*이며, 코드는 단계적으로
> 만든다. v3 스크리너 문서를 먼저 읽고 이 문서를 본다.
>
> **상태**: 0단계(설계 확정). 데이터 점검 완료(§3). 다음 = 1단계(시총 붙여 유니버스).

---

## 0. 한 줄 정의
기존 v3가 **중소형 낙폭과대 반등 사냥꾼**이라면, large_score는 **대형주 중 "싸고 좋은(저평가+우량)"**
종목을 찾는 **별도 가치 트랙**이다. 두 엔진은 **데이터·운영 배관만 공유**하고 **유니버스·점수·표시는
완전히 분리**한다.

---

## 1. 정체성 — v3와 무엇이 다른가 (섞지 말 것)

| | v3 (기존) | large_score (신규) |
|---|---|---|
| 대상 | 중소형 과매도 | 대형 우량/저평가 |
| 핵심 축 | 낙폭 + 턴어라운드 + 단기수급 | **밸류(절대·상대) × 질(ROE) × 주주환원** |
| 과매도 게이트 | ≥40 필수 | **없음** (안 빠진 게 안정 신호일 수 있음) |
| 금융주 | 제외 | **포함** (은행·지주 = 밸류업 핵심) |
| 수급 | 거래대금 대비 5일 강도 | 외인/기관 **장기 소외→단기 전환**(리버설) |
| 검증 호라이즌 | 40거래일(h=20d) | **60~120일** (가치는 분기 단위로 발현) |
| 리밸런스 | 매일 | **주간/월간** (일 단위 교체 = 노이즈) |

→ **large_score는 v3 점수에 절대 섞이지 않는다.** 별도 테이블·별도 점수·별도 탭.

---

## 2. 유니버스
- **시가총액 상위 N**(예: top 200~300). 정확한 N은 1단계에서 분포 보고 결정.
- **금융주 포함**(은행·증권·보험·지주). v3와 정반대 — 가치 트랙에선 핵심.
- **과매도 게이트 없음.**
- **포인트-인-타임**(§8): 유니버스 편입도 "그날 시점 시총" 기준이어야 백테스트가 정직.

---

## 3. 데이터 인벤토리 (실측 — run 14일 20260523~0608 기준)

| 재료 | 상태 | 출처/메모 |
|---|---|---|
| PBR / PER / BPS / EPS | ✅ 있음 | valuation_{mkt}_{run}.csv |
| 배당수익률 (DIV) | ✅ 있음 | valuation csv — **주주환원 축 즉시 가능** |
| ROE 근사 (roe_value=EPS/BPS×100) | ✅ 있음 | stage3_final (대형주 커버리지 양호) |
| OCF/영업이익 (ocf_to_op_ratio) | ✅ 있음(≈80%) | stage3_final — **이익의 질 재사용** |
| 외인/기관 20일 (foreign_20d/inst_20d_억) | ✅ 있음 | stage3_final — 단기 수급 즉시 가능 |
| 분기/연간 YoY(영업이익) | ✅ 있음(100%) | stage3_final |
| 자사주 소각 (buyback_cancel_flag) | ✅ 있음 | catalyst — 주주환원 보조 |
| **시가총액 / 상장주식수** | ❌ **없음** | **유니버스 최대 병목.** 라이브=FDR `StockListing.Marcap`(이미 호출), 백테스트=당시 price×shares(§8) |
| **매출 → 영업이익률** | ❌ 없음 | 영업이익만 있고 매출 없음 → 마진 YoY 불가 (v2) |
| **장기(120일) 수급 이력** | △ 부분 | 전용 테이블 없음. stage3 스냅샷은 대형주 누락 잦음 → **KIS 적재로 해결**(§7) |
| **선행 컨센서스(Forward EPS)** | ❌ 없음 | 무료 소스 없음 → v2(네이버 스크래핑) |

**결론**: 막힌 건 **시총(유니버스)**과 **장기 수급 구멍** 둘뿐. 밸류·ROE·배당·OCF질·단기수급은 *지금* v1 가능.

---

## 4. 코어 팩터 v1 — 전부 "관측 우선"(가중치 0 → IC 검증 후 가중)

### ① RIM 스프레드 (코어 — 가장 엄밀)
- **공식**: 정당PBR = (ROE − g) / (COE − g). 점수는 "**정당PBR 대비 실제 PBR이 얼마나 낮은가**"(스프레드).
- **핵심 원리**: ROE가 자본비용(COE)을 못 넘으면 PBR<1은 **합리적 평가지 저평가가 아님**. 단순 저PBR 가점 금지.
- **최고 가중 사분면**: **ROE > 10% & PBR < 1**.
- **구현 주의**: g는 0~2%로 **보수적 고정**, 분모 (COE−g)가 0 근처면 폭주 → **클램프**. COE는 단순 상수(8~10%)로 시작(나중에 베타·업종별 정교화는 v2). ROE는 roe_value(EPS/BPS) — **근사라 노이즈 있음 명시**.

### ② 주주환원
- **배당수익률(DIV)** + **자사주 소각(buyback_cancel_flag)**. 둘 다 *지금* 보유.
- (밸류업 지수 기준의 "2년 연속 배당 or 소각"을 근사 — 연속성은 누적되면 강화.)

### ③ 품질 게이트 (연속점수 아님, 게이트)
- **OCF/영업이익(ocf_to_op_ratio)이 건전 구간**(예 0.7~5배) + **최근 2년 흑자**(DART NI).
- 장부이익만 좋고 현금 안 도는 밸류트랩·흑자도산 방어. (v3의 quality 로직 재사용.)

### ④ 수급 리버설 (진짜 새 무기 — KIS 필요)
- **장기(120일) 누적 순매수 강도 < 0 (소외)** AND **단기(20일) 누적 > 0 (전환)** = 재평가 초기 트리거.
- KIS daily_flows 적재 전까진 **단기(foreign_20d/inst_20d)만**, 또는 **2구간 플래그**(직전 60일<0 & 최근 20일>0)로 시작.
- ⚠️ flow(순매수)는 stock(지분율)의 근사 — "바닥 지분율"과는 다름을 명시.

> v3에서 **재사용**: OCF/OP 질, 분기/연간 YoY. **버림**: 과매도 가중, 단기 거래대금 비율 수급.

---

## 5. 플래그 (관측 — 하드코딩 페널티 금지)
근거 없는 임의 감점(예: "지주 30~50% 깎기")은 **불변 규칙 위반**. 플래그로 쌓고 IC로 검증 후에만 가중.
- **시클리컬 플래그**: 반도체·화학·철강·해운 등. 이익 정점에서 PER 최저(=상투) 착시 → **PER 비중 down**, PBR×ROE 메인. (정교화: 정상화 EPS / PBR 역사밴드 = v2)
- **지주/물적분할 플래그**: 사명 '홀딩스/지주' + 알려진 물적분할 모회사 매핑. (본질 해법 NAV 합산은 v2.)

---

## 6. 아키텍처 — 인프라 공유, 머리 분리 (repo는 안 나눔)
- **공유(재사용)**: DART 배관(corp_code·dart_cache)·valuation·FDR StockListing·history.db·대시보드·텔레그램·git push.
- **분리(신규)**:
  - `large_universe.py` — 시총 상위 N(금융 포함, 과매도 게이트 없음)
  - `large_score.py` — v3와 **완전 독립** 점수. 관측 팩터 적재 포함.
  - 새 테이블 **`large_final`**(별도. stage3_final 재활용 X — 유니버스·컬럼이 다름)
  - 새 테이블 **`daily_flows`**(§7 KIS 수급 적재)
  - 표시: 대시보드 **"대형 가치" 탭** 또는 `docs/large.html` (검증 후 노출)
- 검증: 기존 챔피언/챌린저 틀 재사용하되 **긴 호라이즌**(§9).

---

## 7. KIS API 사용 규칙 (조회 전용)
- **용도**: ① **투자자별 매매동향(외인·기관 일별)** → 수급 리버설 장기 이력을 *즉시* 확보(가장 강한 효용). ② (선택) 시총·상장주식수. ③ (선택) 일별 과거 시세 → PBR/PER 역사밴드.
- **안 씀**: 선행 컨센서스(KIS도 없음), 실시간/주문. **자동매매·주문 함수는 절대 붙이지 않는다.** 이 트랙은 "발굴·관찰"이지 집행이 아님.
- **캐싱/적재 = dart_cache + catalyst_observe 증분의 결합**:
  - 새 테이블 `daily_flows(ticker, date, foreign_net, inst_net, …)`. 배치 때 **"DB에 없는 날짜만"** 긁어 append. 과거 확정 수급은 불변 → 재호출 X. 120일은 처음 한 번만 백필.
  - **Rate limit**: KIS는 빡빡 → DART보다 **더 보수적**(초당 1~2건)으로 sleep/병렬 제한. **에러는 저장 안 함**(다음 배치 재시도) — dart_cache 원칙 그대로.
- **키**: app_key/secret → 토큰. `.env`에만(깃헙 금지). 공식 LLM 샘플 repo: github.com/koreainvestment/open-trading-api (참고용).

---

## 8. 백테스트 정직성 규칙 (포인트-인-타임 — 한국 특유 함정 포함)
> 룩어헤드가 섞이면 IC가 **거짓으로 좋게** 나와 더 위험하다. *관측 적재(현재진행형)*는 무관, *과거
> 백테스트(소급 재계산)*에서만 엄격 적용.

- **(a) 시총 = 당시 price × 당시 주식수.** "현재 시총"을 끌어오지 말 것. history.db에 각 run_id의 `price`(당시 종가)가 이미 있으니 **당시 price 기반으로 재구성** → 가격 룩어헤드 자동 차단. (주식수 변동은 2차 오차.)
- **(b) 재무 룩어헤드(더 치명적).** 한국 분기보고서 제출 시한 = 분기말 **+45일**. 예: 3월 공시된 4Q 실적을 1~2월 백테스트에 쓰면 미래를 본 것. → **"기준일 시점에 *이미 공시된* 재무만 사용"**. 가능하면 재무에 **공시일(rcept_dt) 컬럼을 동반 적재**해 `재무공시일 ≤ 기준일`로 필터. (가치 지표가 전부 재무 기반이라 특히 중요.)
- **(c) 유니버스 편입도 당시 시총 기준**(편입 자체의 룩어헤드 차단).

---

## 9. 검증 기준 (사전등록 — v3와 다른 "긴 호라이즌")
- **관측 → IC 검증 → 그다음 가중.** 검증 전 어떤 팩터도 점수에 가중 안 함(v3 교훈: 정교해도 IC≈0 가능).
- **호라이즌**: 전 유니버스 Spearman IC **h=60일·120일**(가치 프리미엄은 분기 단위). 40거래일은 가치 모델엔 **너무 짧을 수 있음** → 인내가 전략의 일부.
- **표본 작음**: 대형주 top-200~300이라 n이 작아 CI 넓음 → **더 보수적**으로 판단. 경계값은 '채택'이 아니라 '기움'.
- **섹터 상대**: PBR·ROE는 **업종 내 백분위**로(시클리컬/디펜시브 구조 차이 보정).
- **리밸런스**: 주간/월간 기준으로 평가(일 단위 교체는 노이즈).
- **무판정도 결론**: 호라이즌이 차도 기준 미달이면 **노출 안 함**(정상).

---

## 10. 불변 규칙 (v3 원칙 상속 + 대형 특화)
1. **관측 팩터는 점수식에 안 들어감**(검증 전). — 상속
2. **v3 엔진·테이블·표시 절대 안 건드림.** large는 별도 테이블/점수/탭으로 완전 분리.
3. **매직넘버 금지**: 임의 하드코딩 페널티(지주/시클리컬 등) 금지 → 플래그→IC→가중.
4. **KIS 조회 전용. 자동매매·주문 절대 금지.**
5. **백테스트 포인트-인-타임 엄수**(§8): 가격·재무 룩어헤드 차단.
6. **긴 호라이즌(60/120일)으로 판정, 작은 n 보수적.**
7. **출력 동일성/검증 오프라인 우선**: 점수·동작 변경은 history.db로 "결과 0 diff" 증명 후. — 상속

---

## 11. 단계별 로드맵
| 단계 | 내용 | 누가 |
|---|---|---|
| **0 (지금)** | 이 설계노트 확정 + 데이터 갭 확인(완료) | — |
| **1** | FDR `StockListing.Marcap`로 **시총 컬럼 추가** → `large_universe`(시총 top-N, 금융 포함) 생성 | 사용자 실행 → Claude 검증 |
| **2** | v1 관측 팩터(①RIM ②주주환원 ③품질게이트) 계산 → `large_final` **관측 적재**(추천 미표시) | Claude 코드 → 사용자 실행 |
| **3** | KIS `daily_flows` 적재 → **④수급 리버설** 추가. 60/120일 IC 검증 시작 | 사용자(KIS) + Claude(검증) |
| **4** | 검증된 팩터만 가중 → **"대형 가치" 탭** 노출 | Claude + 사용자 |

---

## 12. v2 (데이터 소싱 후)
- 선행 컨센서스(네이버 금융 스크래핑 — 대형주는 애널 커버리지 존재) + "최근 1~3개월 상향" (스냅샷 누적 필요).
- 매출 → 영업이익률 YoY (마진 방향성; ΔROE보다 깨끗).
- PBR/PER **역사 밴드**(장기 이력 누적) + **정상화 EPS**(시클리컬 착시 해소).
- 지주 **NAV**(순자산가치 합산) 평가.
- COE 정교화(업종별/베타).

---

## 13. Claude 협업 메모 (대형 트랙 한정)
- 나(Claude)는 네트워크 없음 → KIS/FDR/DART 호출은 사용자 실행, 나는 history.db·zip으로 **오프라인 검증**.
- large_score는 **관측 적재 코드부터**(추천 로직·탭은 검증 후). "돈 버는 모델"로 확정하지 말 것.
- 데이터 갭(시총·매출·컨센서스)이 있으면 **"상상 속 팩터" 만들지 말고** 갭을 명시하고 v2로 미룸.
- 백테스트 코드를 짤 땐 §8(포인트-인-타임)을 **먼저** 박고 시작.
